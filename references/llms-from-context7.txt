### Setup Ansible Baseline

Source: https://gitlab.com/linuxmalaysia/deep-state-of-mind-for-my-ai/-/blob/main/README.md

Displays the setup guide and verifies connectivity to infrastructure nodes.

```bash
# Follow the full LDP guide
cat docs/HOWTO-SETUP-ANSIBLE-BASELINE.md

# Verify connectivity to your nodes
ansible all -m ping -i inventory/hosts.yml
```

--------------------------------

### Local Documentation Preview

Source: https://gitlab.com/linuxmalaysia/deep-state-of-mind-for-my-ai/-/blob/main/docs-source/README.md

Commands to install the Mintlify CLI and start a local development server for documentation previewing.

```bash
cd docs-source
npm i -g mint
mint dev
```

--------------------------------

### Install guardrails-ai-dsom

Source: https://gitlab.com/linuxmalaysia/deep-state-of-mind-for-my-ai/-/blob/main/tools/guardrails-ai-dsom/README.md

Standard and optional dependency installation commands using uv.

```bash
# Standard installation
uv add guardrails-ai-dsom

# With optional Guardrails AI & Token dependencies
uv add "guardrails-ai-dsom[all]"
```

--------------------------------

### Initialize Git and Install Guardrails

Source: https://gitlab.com/linuxmalaysia/deep-state-of-mind-for-my-ai/-/blob/main/docs/tutorials/TEAM-DSOM-MASTERCLASS.md

Initializes a new repository and installs the mandatory pre-commit guardrail hooks.

```bash
git init
git branch -M main

# Install the unbypassable pre-commit guardrail hook
uv run python tools/install_git_guardrails.py
```

--------------------------------

### Run DSOM Control Node Setup

Source: https://gitlab.com/linuxmalaysia/deep-state-of-mind-for-my-ai/-/blob/main/docs/tools/HOWTO-SETUP-DSOM-CONTROL-NODE.md

Execute the setup script with root privileges within the WSL instance to initiate system hardening.

```bash

# Run as root inside the WSL instance

bash tools/setup-dsom-control-node.sh

```

--------------------------------

### Execute the DSOM control node setup script

Source: https://gitlab.com/linuxmalaysia/deep-state-of-mind-for-my-ai/-/blob/main/docs-source/tools/howto-setup-dsom-control-node.mdx

Run the setup script with root privileges to initialize the system configuration.

```bash

# Run as root inside the WSL instance

bash tools/setup-dsom-control-node.sh

```

--------------------------------

### Verify Environment Setup

Source: https://gitlab.com/linuxmalaysia/deep-state-of-mind-for-my-ai/-/blob/main/CONTRIBUTING.md

Check Ansible installation and connectivity.

```bash
# Inside dsom-control-almalinux10
ansible --version
ansible localhost -m ping
```

--------------------------------

### Install uv dependency manager

Source: https://gitlab.com/linuxmalaysia/deep-state-of-mind-for-my-ai/-/blob/main/docs-source/tools/openwiki-usage.mdx

Install uv to manage Python dependencies and run the OpenWiki emulator without manual virtual environment setup.

```bash
# Option 1: install via the official installer (recommended)
curl -LsSf https://astral.sh/uv/install.sh | sh

# Option 2: install via pip if curl is unavailable
pip install uv

# Verify installation
uv --version
```

```powershell
# Option 1: install via the official Windows installer
powershell -ExecutionPolicy ByPass -c "irm https://astral.sh/uv/install.ps1 | iex"

# Option 2: install via pip
pip install uv

# Verify installation
uv --version
```

--------------------------------

### Verify Server Startup

Source: https://gitlab.com/linuxmalaysia/deep-state-of-mind-for-my-ai/-/blob/main/docs/tools/HOWTO-MCP-SERVER.md

Checks the server help output to verify installation and environment.

```bash
uv run tools/mcp/server.py --help
```

--------------------------------

### Launch the FastMCP server

Source: https://gitlab.com/linuxmalaysia/deep-state-of-mind-for-my-ai/-/blob/main/docs/how-to/run-fastmcp-server.md

Starts the server using uv to manage dependencies and communicate over stdio.

```bash
uv run tools/mcp/server.py
```

--------------------------------

### Run the automated setup script

Source: https://gitlab.com/linuxmalaysia/deep-state-of-mind-for-my-ai/-/blob/main/docs/HOWTO-SETUP-WSL-ALMALINUX10.md

Execute the PowerShell script to download and import the AlmaLinux 10 rootfs.

```powershell
cd D:\Users\LinuxMalaysia\Projects\deep-state-of-mind-for-my-ai

# Run the automated setup

.\tools\setup-wsl-almalinux10.ps1
```

--------------------------------

### Install uv on Windows

Source: https://gitlab.com/linuxmalaysia/deep-state-of-mind-for-my-ai/-/blob/main/docs/governance/PYTHON-UV-ENVIRONMENT-GUIDE.md

Installs the uv package manager into the user profile using PowerShell.

```powershell
powershell -ExecutionPolicy ByPass -c "irm https://astral.sh/uv/install.ps1 | iex"
```

--------------------------------

### Install Ansible

Source: https://gitlab.com/linuxmalaysia/deep-state-of-mind-for-my-ai/-/blob/main/docs-source/tools/audit-pre-flight.mdx

Commands to install Ansible when the baseline check fails.

```bash
pip install ansible
# or
sudo apt install ansible
```

--------------------------------

### OKF Frontmatter Example

Source: https://gitlab.com/linuxmalaysia/deep-state-of-mind-for-my-ai/-/blob/main/llms-full.txt

Example of a Markdown file header compliant with OKF v0.1 standards.

```yaml
---
okf_version: 0.1
type: documentation
title: "Reference: refactor_okf.py"
timestamp: "2026-08-13T12:00:00Z"
topics: ["dsom", "reference", "okf", "refactor"]
---
```

--------------------------------

### DSOM Commit Examples

Source: https://gitlab.com/linuxmalaysia/deep-state-of-mind-for-my-ai/-/blob/main/docs-source/tools/git-ritual.mdx

Various examples demonstrating the application of the DSOM commit convention across different types of changes.

```text
feat(brain): add Mental Anchor for Session 42 [Phase-3/v10.4]
fix(playbooks): correct nginx vhost path [Phase-2]
docs(palace): add wing_operations closet summary
chore(brain): update task.md for EOD — mark Phase-3 tasks complete
refactor(ansible): extract common hardening to roles/common [Phase-2/v9.8]
```

--------------------------------

### Connect OKF to FastMCP

Source: https://gitlab.com/linuxmalaysia/deep-state-of-mind-for-my-ai/-/blob/main/docs/OKF-ADOPTION-GUIDE.md

Start the DSOM FastMCP server and query the OpenWiki knowledge graph.

```bash
# Start DSOM FastMCP Server
uv run tools/mcp/server.py

# Query OpenWiki Knowledge Graph
uv run python tools/openwiki_emulator.py --search "OKF"
```

--------------------------------

### Install Pre-Commit Hook

Source: https://gitlab.com/linuxmalaysia/deep-state-of-mind-for-my-ai/-/blob/main/docs/tutorials/TEAM-DSOM-MASTERCLASS.md

Sets up git guardrails to enforce code quality.

```bash
uv run python tools/install_git_guardrails.py
```

--------------------------------

### Execute the WSL AlmaLinux setup script

Source: https://gitlab.com/linuxmalaysia/deep-state-of-mind-for-my-ai/-/blob/main/docs/tools/HOWTO-SETUP-WSL-ALMALINUX.md

Run this command from an elevated PowerShell terminal at the project root to initiate the provisioning process.

```powershell

# From the project root

.\tools\setup-wsl-almalinux10.ps1

```

--------------------------------

### Install Ansible

Source: https://gitlab.com/linuxmalaysia/deep-state-of-mind-for-my-ai/-/blob/main/docs/HOWTO-SETUP-ANSIBLE-BASELINE.md

Commands to install Ansible on Ubuntu/Debian or RHEL-based systems and verify the installation.

```bash

# Ubuntu / Debian

sudo apt update && sudo apt install -y python3-pip
pip3 install ansible

# RHEL / AlmaLinux / Rocky Linux

sudo dnf install -y python3-pip
pip3 install ansible

# Verify

ansible --version

```

--------------------------------

### Install build dependencies

Source: https://gitlab.com/linuxmalaysia/deep-state-of-mind-for-my-ai/-/blob/main/docs/tools/HOWTO-BUILD-SOVEREIGN-BOOK.md

Install required packages for Pandoc and LuaLaTeX on Linux distributions.

```bash
sudo apt-get update && sudo apt-get install -y pandoc librsvg2-bin fonts-noto-color-emoji texlive-luatex texlive-latex-extra texlive-fonts-recommended texlive-plain-generic
```

```bash
sudo dnf install -y pandoc librsvg2-tools google-noto-emoji-color-fonts texlive-scheme-medium
```

--------------------------------

### Install Dependencies for build_sovereign_book

Source: https://gitlab.com/linuxmalaysia/deep-state-of-mind-for-my-ai/-/blob/main/docs-source/tools/howto-build-sovereign-book.mdx

Commands to install required packages for Pandoc and LuaLaTeX on Linux distributions.

```bash
sudo apt-get update && sudo apt-get install -y pandoc librsvg2-bin fonts-noto-color-emoji texlive-luatex texlive-latex-extra texlive-fonts-recommended texlive-plain-generic
```

```bash
sudo dnf install -y pandoc librsvg2-tools google-noto-emoji-color-fonts texlive-scheme-medium
```

--------------------------------

### Search Result Example

Source: https://gitlab.com/linuxmalaysia/deep-state-of-mind-for-my-ai/-/blob/main/docs-source/tools/openwiki-usage.mdx

Example output format returned by the search command.

```text
[OpenWiki Search] Querying frontmatter for: 'Ansible'...
 - [automation.md] Ansible Automation Playbooks: Idempotent playbook patterns for infrastructure provisioning.
 - [governance.md] DSOM Three-Pillar Strategy: GitOps, AIOps, and Ansible doctrine for sovereign AI projects.
```

--------------------------------

### Install uv environment manager

Source: https://gitlab.com/linuxmalaysia/deep-state-of-mind-for-my-ai/-/blob/main/docs-source/ai-setup/mcp-server.mdx

Use this command to install the uv package manager required for running the DSOM server.

```bash
curl -LsSf https://astral.sh/uv/install.sh | sh
```

--------------------------------

### Install uv on Unix-like Systems

Source: https://gitlab.com/linuxmalaysia/deep-state-of-mind-for-my-ai/-/blob/main/docs/governance/PYTHON-UV-ENVIRONMENT-GUIDE.md

Installs the uv package manager on Linux, macOS, or Termux environments.

```bash
curl -LsSf https://astral.sh/uv/install.sh | sh
```

--------------------------------

### Install WSL2 and Ubuntu via PowerShell

Source: https://gitlab.com/linuxmalaysia/deep-state-of-mind-for-my-ai/-/blob/main/docs-source/governance/three-pillars.mdx

Execute this command in an Administrator PowerShell session to install the WSL2 subsystem and Ubuntu distribution.

```powershell
wsl --install -d Ubuntu
```

--------------------------------

### Install DSOM guardrails hook

Source: https://gitlab.com/linuxmalaysia/deep-state-of-mind-for-my-ai/-/blob/main/docs/governance/DOWNSTREAM-DSOM-COMPLIANCE-MANDATE.md

Execute the installation script to configure the Git pre-commit guardrail hook.

```bash
uv run tools/install-dsom-guardrails.py
```

--------------------------------

### Verify Palace Installation

Source: https://gitlab.com/linuxmalaysia/deep-state-of-mind-for-my-ai/-/blob/main/docs-source/palace/onboarding.mdx

Check for the existence of the registry file and wings directory to confirm the Palace is initialized.

```bash
ls -la .agents/brain/palace_registry.md
ls -la .agents/brain/wings/
```

--------------------------------

### Initialise the Knowledge Graph

Source: https://gitlab.com/linuxmalaysia/deep-state-of-mind-for-my-ai/-/blob/main/docs-source/tools/openwiki-usage.mdx

Run this command to perform a full rebuild of the knowledge graph from scratch. Use it during initial setup or after major documentation restructuring.

```bash
uv run --with pyyaml python tools/openwiki_emulator.py --init
```

```powershell
uv run --with pyyaml python tools/openwiki_emulator.py --init
```

--------------------------------

### Decision Log Example

Source: https://gitlab.com/linuxmalaysia/deep-state-of-mind-for-my-ai/-/blob/main/docs-source/governance/research-reasoning-gap.mdx

A concrete example of a decision log entry detailing network configuration choices.

```text
"Log this decision to walkthrough.md:
 Decision: Macvlan networking for Elasticsearch nodes
 Alternatives: Bridge networking (tested Phase 42), Host networking (security risk)
 Reason: Nodes need routable IPs for cluster discovery. Bridge caused split-brain 
          under 3-node load test on 2026-03-15.
 Context: Revisit if we move to Kubernetes (CNI handles this differently)."
```

--------------------------------

### CheckUsage Usage Examples

Source: https://gitlab.com/linuxmalaysia/deep-state-of-mind-for-my-ai/-/blob/main/docs/tools/HOWTO-CHECKUSAGE.md

Common command-line patterns for snapshotting, monitoring, and configuring thresholds.

```powershell

# Snapshot — run once and exit

.\tools\CheckUsage.ps1

# Live monitor with default 60s refresh

.\tools\CheckUsage.ps1 -Loop

# Live monitor with 30s refresh (short alias)

.\tools\CheckUsage.ps1 -l -i 30

# Top 5 largest sessions only

.\tools\CheckUsage.ps1 -Top 5

# Tighter thresholds — warn at 10MB, critical at 25MB

.\tools\CheckUsage.ps1 -ThresholdMB 10 -CriticalMB 25

# Live monitor for large projects, suppress countdown bar

.\tools\CheckUsage.ps1 -Loop -NoPulse -CriticalMB 80

# Show full help screen

.\tools\CheckUsage.ps1 -Help
```

--------------------------------

### Decision Log Example

Source: https://gitlab.com/linuxmalaysia/deep-state-of-mind-for-my-ai/-/blob/main/docs/governance/RESEARCH-REASONING-GAP.md

A concrete example of a decision log entry documenting the choice of Macvlan networking for Elasticsearch nodes.

```text
"Log this decision to walkthrough.md:
 Decision: Macvlan networking for Elasticsearch nodes
 Alternatives: Bridge networking (tested Phase 42), Host networking (security risk)
 Reason: Nodes need routable IPs for cluster discovery. Bridge caused split-brain 
          under 3-node load test on 2026-03-15.
 Context: Revisit if we move to Kubernetes (CNI handles this differently)."
```

--------------------------------

### Run Live Monitor

Source: https://gitlab.com/linuxmalaysia/deep-state-of-mind-for-my-ai/-/blob/main/docs-source/tools/howto-checkusage.mdx

Starts a continuous monitoring loop that refreshes every 60 seconds by default.

```powershell
.\tools\CheckUsage.ps1 -Loop
```

--------------------------------

### Run Live Monitor Mode

Source: https://gitlab.com/linuxmalaysia/deep-state-of-mind-for-my-ai/-/blob/main/docs/tools/HOWTO-CHECKUSAGE-LINUX.md

Starts the monitor in a loop, refreshing every 60 seconds with a pulse bar.

```bash
bash tools/check-usage.sh --loop
```

--------------------------------

### Commit Message Examples

Source: https://gitlab.com/linuxmalaysia/deep-state-of-mind-for-my-ai/-/blob/main/CONTRIBUTING.md

Examples of correctly formatted commit messages following the type(scope): description [Phase/vX.X] standard.

```text
feat(playbooks): add template-reset Ansible playbook [Phase-2/v6.2]
docs(sod): add Step 4c for Claude-specific reanimation [v6.1]
fix(audit): correct Ansible baseline check for AlmaLinux 10 [Phase-1/v6.1]
chore(brain): EOD sovereign save — AlmaLinux 10 WSL setup complete [v6.1.1]
```

--------------------------------

### Install Ansible in WSL2

Source: https://gitlab.com/linuxmalaysia/deep-state-of-mind-for-my-ai/-/blob/main/docs-source/governance/three-pillars.mdx

Update package repositories and install Ansible within the WSL2 environment.

```bash
sudo apt update && sudo apt install -y ansible
ansible --version
```

--------------------------------

### Verify SOD Status

Source: https://gitlab.com/linuxmalaysia/deep-state-of-mind-for-my-ai/-/blob/main/docs-source/tools/git-ritual.mdx

Example output confirming the repository is up to date.

```text
[OK] Already up to date — origin/main
```

--------------------------------

### Start and verify FastMCP server

Source: https://gitlab.com/linuxmalaysia/deep-state-of-mind-for-my-ai/-/blob/main/docs-source/tutorials/getting-started.mdx

Launch the MCP server over stdio and execute verification tests to ensure proper JSON-RPC communication.

```bash
uv run tools/mcp/server.py
```

```bash
uv run --with pyyaml --with pytest --with mcp==1.2.1 --with fastmcp pytest tests/test_mcp_server.py
```

--------------------------------

### Active Context Manifest Configuration

Source: https://gitlab.com/linuxmalaysia/deep-state-of-mind-for-my-ai/-/blob/main/docs/governance/ZERO-GLOBAL-MEMORY.md

Example of the manifest file used to selectively load files into the AI context.

```markdown
## Active Files

- .agents/brain/task.md
- .agents/brain/walkthrough.md
- .agents/brain/palace_registry.md
- .agents/brain/wings/wing_dsom_core/hall_facts/room_tooling/closet.md

## Excluded (Archival — load via view_file with line ranges only)

# palace_update_proposal files: 30,000–55,000 tokens each
```

--------------------------------

### Execute the 6-Step Boot Checklist

Source: https://gitlab.com/linuxmalaysia/deep-state-of-mind-for-my-ai/-/blob/main/README.md

Follow these steps in order to initialize a new DSOM project and establish the AI's cognitive state.

```text
[ ] 1. Clone and run template-reset.sh
[ ] 2. Run init-brain.sh
[ ] 3. Fill in docs/AI-COGNITIVE-TWIN-PROTOCOL.md (ALL [PLACEHOLDER] fields)
[ ] 4. Run HOWTO-SETUP-ANSIBLE-BASELINE.md (infra projects)
[ ] 5. Run audit-pre-flight.sh — confirm all PASS
[ ] 6. Run reanimate.sh — upload manifest to AI — get Handshake
```

--------------------------------

### Execute Start of Day (SOD) Ritual

Source: https://gitlab.com/linuxmalaysia/deep-state-of-mind-for-my-ai/-/blob/main/docs/README.md

Initializes the session by pulling from Git, auditing, and generating the reanimation manifest.

```powershell
.\tools\sod-palace.ps1            # Windows Native
```

```bash
bash tools/sod-palace.sh          # Linux / WSL2
```

```bash
./tools/privacy-guardian.sh
```

--------------------------------

### Initialize implementation_plan.md content

Source: https://gitlab.com/linuxmalaysia/deep-state-of-mind-for-my-ai/-/blob/main/docs-source/tools/init-brain.mdx

Define high-level project phases and goals.

```markdown
## Phase 1 — Foundation
- Set up DSOM governance artifacts
- Configure Ansible baseline

## Phase 2 — Core Delivery
- [Your project-specific goals here]
```

--------------------------------

### Install DSOM Guardrails via uv

Source: https://gitlab.com/linuxmalaysia/deep-state-of-mind-for-my-ai/-/blob/main/docs/governance/DSOM-GUARDRAILS-CATALOG-SUBMISSION-REVIEW.md

Command to add the DSOM guardrails package to your project environment.

```bash
uv add guardrails-ai-dsom
```

--------------------------------

### Onboard DSOM on Linux, macOS, or WSL2

Source: https://gitlab.com/linuxmalaysia/deep-state-of-mind-for-my-ai/-/blob/main/docs/HOWTO-DSOM-ONBOARDING.md

Use the bash bootstrap script to download and execute the onboarding process in a Unix-like environment.

```bash

# 1. Download the safe bootstrap script to the root of your repository

curl -O https://raw.githubusercontent.com/linuxmalaysia/deep-state-of-mind-for-my-ai/main/tools/dsom-onboard.sh

# 2. Add execution permissions

chmod +x dsom-onboard.sh

# 3. Run the onboarding process

./dsom-onboard.sh

```

--------------------------------

### Automated AI Prompt Template

Source: https://gitlab.com/linuxmalaysia/deep-state-of-mind-for-my-ai/-/blob/main/docs-source/skills/dsom-project-cloner.mdx

Use this prompt to instruct an AI to scaffold a new DSOM project structure and execute initial setup scripts.

```markdown
Use the `dsom-project-cloner` skill to scaffold a brand-new DSOM downstream project for me.

Target Repository Path: <ABSOLUTE_TARGET_PATH>

Please:
1. Create the required directory structure (.agents/brain, .agents/skills, docs/governance, tools/).
2. Copy the Universal Gateway Matrix (.cursorrules, CLAUDE.md, .github/copilot-instructions.md, AGENTS.md, START-HERE.md).
3. Copy the Constitutional Rulebook (.agents/AGENTS.md) and reset .agents/brain/ with blank OKF templates.
4. Copy the tools/ directory and automatically execute `python tools/install_git_guardrails.py` in the new target path.
5. Ensure all copied markdown files carry valid OKF v0.2 frontmatter and standard DSOM signatures.
```

--------------------------------

### Verify Plugin with Guardrails

Source: https://gitlab.com/linuxmalaysia/deep-state-of-mind-for-my-ai/-/blob/main/docs/governance/DSOM-AGENT-PLUGINS-SPECIFICATION.md

Commands to install guardrails and run compliance tests against the plugin specification.

```bash
uv run python tools/install_git_guardrails.py
uv run pytest tests/test_agent_plugins_spec.py
```

--------------------------------

### CheckUsage Execution Examples

Source: https://gitlab.com/linuxmalaysia/deep-state-of-mind-for-my-ai/-/blob/main/docs-source/tools/howto-checkusage.mdx

Common usage patterns including custom intervals, thresholds, and display filtering.

```powershell

# Snapshot — run once and exit

.\tools\CheckUsage.ps1

# Live monitor with default 60s refresh

.\tools\CheckUsage.ps1 -Loop

# Live monitor with 30s refresh (short alias)

.\tools\CheckUsage.ps1 -l -i 30

# Top 5 largest sessions only

.\tools\CheckUsage.ps1 -Top 5

# Tighter thresholds — warn at 10MB, critical at 25MB

.\tools\CheckUsage.ps1 -ThresholdMB 10 -CriticalMB 25

# Live monitor for large projects, suppress countdown bar

.\tools\CheckUsage.ps1 -Loop -NoPulse -CriticalMB 80

# Show full help screen

.\tools\CheckUsage.ps1 -Help
```

--------------------------------

### Execute Palace Daily Loop Scripts

Source: https://gitlab.com/linuxmalaysia/deep-state-of-mind-for-my-ai/-/blob/main/docs/HOWTO-PALACE-ONBOARDING.md

Commands to initialize the environment at the start of the day or finalize work at the end of the day.

```bash
bash tools/sod-palace.sh
```

```powershell
.\tools\sod-palace.ps1
```

```bash
bash tools/reanimate.sh
```

```bash
bash tools/eod-palace.sh
```

```powershell
.\tools\eod-palace.ps1
```

```bash
bash tools/hibernation.sh
```

--------------------------------

### Install Guardrails AI via uv

Source: https://gitlab.com/linuxmalaysia/deep-state-of-mind-for-my-ai/-/blob/main/docs/governance/AI-GUARDRAILS-MASTER-GUIDE.md

Use the uv package manager to add the guardrails-ai dependency to your project.

```bash
uv add guardrails-ai
```

--------------------------------

### Render.com Build Command

Source: https://gitlab.com/linuxmalaysia/deep-state-of-mind-for-my-ai/-/blob/main/openwiki/publishing/documentation-delivery.md

Command used in render.yaml to install dependencies and build the static site.

```bash
pip install -r docs/requirements.txt && mkdocs build
```

--------------------------------

### Verify the AlmaLinux 10 installation

Source: https://gitlab.com/linuxmalaysia/deep-state-of-mind-for-my-ai/-/blob/main/docs/HOWTO-SETUP-WSL-ALMALINUX10.md

Run verification commands inside the AlmaLinux 10 environment to confirm user identity and Ansible installation.

```bash
# Should show dsom-admin (UID 2001)

id

# Should show ansible version

ansible --version

# Should return pong

ansible localhost -m ping
```

--------------------------------

### Project Boot Checklist

Source: https://gitlab.com/linuxmalaysia/deep-state-of-mind-for-my-ai/-/blob/main/docs/README.md

A sequence of steps to initialize the project environment and synchronize with the AI cognitive twin.

```text
[ ] 1. Clone and run template-reset.sh
[ ] 2. Run init-brain.sh
[ ] 3. Fill in AI-COGNITIVE-TWIN-PROTOCOL.md (ALL [PLACEHOLDER] fields)
[ ] 4. Run HOWTO-SETUP-ANSIBLE-BASELINE.md (infra projects)
[ ] 5. Run audit-pre-flight.sh — confirm all PASS
[ ] 6. Run reanimate.sh — upload manifest to AI — get Handshake
```

--------------------------------

### Example Scenario of Reasoning Loss

Source: https://gitlab.com/linuxmalaysia/deep-state-of-mind-for-my-ai/-/blob/main/docs/governance/RESEARCH-REASONING-GAP.md

Demonstrates the difference between a session with documented reasoning and one where the context has been lost.

```text
Session Day 1:
  Human: "Why are we using Macvlan instead of bridge networking?"
  AI: "Because Elasticsearch nodes needed their own routable IPs for 
       cluster discovery. Bridge networking caused split-brain under 
       load. We tested this in Phase 42 and it failed with 3 nodes."
Session Day 180 (new AI, same repo):
  Human: "Why are we using Macvlan?"
  AI: "I don't know. The config shows Macvlan but there's no reasoning 
       documented. Do you want to reconsider?"
```

--------------------------------

### Agent Onboarding Initialization Prompt

Source: https://gitlab.com/linuxmalaysia/deep-state-of-mind-for-my-ai/-/blob/main/docs/tutorials/TEAM-DSOM-MASTERCLASS.md

Standardized prompt for synchronizing AI agents with the DSOM framework knowledge base.

```markdown
I need you to adopt Deep State Of Mind (DSOM) For My AI by reading this in detail, and make sure you also read all others supporting documents and pages and do your own deep research and make sure all into AI Agents Knowledge and .agents/brain

https://linuxmalaysia.github.io/deep-state-of-mind-for-my-ai/START-HERE/
Welcome to the Deep State of Mind (DSOM) For My AI framework. If you are adopting this repository to bootstrap a new infrastructure/software project, or onboarding a new human team member or AI agent, you must understand how to enter the system.
DSOM is a modular Sovereign AI Engine. To use it effectively, do not read every file at random. Start with the defined Entry Points depending on your role.
```

--------------------------------

### Execute SOD Mode

Source: https://gitlab.com/linuxmalaysia/deep-state-of-mind-for-my-ai/-/blob/main/docs-source/tools/git-ritual.mdx

Run the SOD mode at the start of the day to fetch and rebase changes from the origin.

```bash
bash tools/git-ritual.sh sod
```

```powershell
.\tools\git-ritual.ps1 sod
```

--------------------------------

### Example Scan Output

Source: https://gitlab.com/linuxmalaysia/deep-state-of-mind-for-my-ai/-/blob/main/docs-source/tools/privacy-guardian.mdx

Sample output format showing potential leaks and scan completion status.

```text
⚠️  POTENTIAL LEAK — sod_manifest_2026-08-14.txt:47 — IPv4 address: 192.168.1.50
⚠️  POTENTIAL LEAK — sod_manifest_2026-08-14.txt:83 — Path with username: /home/harisfazillah/projects
✅ SCAN COMPLETE — 2 potential leaks found. Review before uploading.
```

--------------------------------

### Generated Session Anchor Output

Source: https://gitlab.com/linuxmalaysia/deep-state-of-mind-for-my-ai/-/blob/main/docs/tools/HOWTO-GENERATE-WALKTHROUGH.md

Example of the Markdown block generated by the tool, which should be copied into the walkthrough.md file.

```markdown

## 🏁 Session Anchor: 2026-04-08 (Auto-Generated)

- **Accomplished:**
    - [x] Initialise project (SELESAI)
    - feat(ui): add modern glassmorphism
- **Current State:** [UPDATE ME]
- **Mental Anchor:** [UPDATE ME]

```

--------------------------------

### Verify Ansible Environment

Source: https://gitlab.com/linuxmalaysia/deep-state-of-mind-for-my-ai/-/blob/main/docs/reference-architectures/ANSIBLE-CONTROL-NODE-PROTOCOL.md

Check the health and version of the Ansible installation on the control node.

```bash
cd /opt/um-elastic-soc

# Verify orchestration dependencies

ansible --version
```

--------------------------------

### Session Summary Footer Output

Source: https://gitlab.com/linuxmalaysia/deep-state-of-mind-for-my-ai/-/blob/main/docs/tools/HOWTO-CHECKUSAGE.md

Example of the summary footer displayed at the end of the session monitoring output.

```text
Sessions (26 total)  |  Total: 354.97 MB  |  Est Tokens: 22,185,625  |  WARN: 9  |  CRIT: 0
Token basis: 1 MB ~ 62,500 tokens (4 chars/token avg)
```

--------------------------------

### Full Command Syntax

Source: https://gitlab.com/linuxmalaysia/deep-state-of-mind-for-my-ai/-/blob/main/docs/tools/HOWTO-CHECKUSAGE-LINUX.md

Displays all available options for configuring the monitor, including thresholds and refresh intervals.

```bash
bash tools/check-usage.sh [options]

Options:
  -l, --loop              Live monitor mode
  -i, --interval SECS     Refresh interval [default: 60]
  -w, --threshold MB      WARNING threshold [default: 20]
  -c, --critical MB       CRITICAL threshold [default: 50]
  -n, --top N             Show only top N sessions
  --no-pulse              Suppress the progress bar
```

--------------------------------

### Initialize a New Project Wing

Source: https://gitlab.com/linuxmalaysia/deep-state-of-mind-for-my-ai/-/blob/main/llms-full.txt

Creates the necessary directory structure for a new project domain and initializes a base room with a markdown file.

```bash
mkdir -p .agents/brain/wings/wing_YOUR_PROJECT/{hall_facts,hall_events,hall_discoveries}
# Create at minimum one Room
mkdir -p .agents/brain/wings/wing_YOUR_PROJECT/hall_facts/room_overview
touch .agents/brain/wings/wing_YOUR_PROJECT/hall_facts/room_overview/closet.md
```

--------------------------------

### Create Wing and Room Directories

Source: https://gitlab.com/linuxmalaysia/deep-state-of-mind-for-my-ai/-/blob/main/docs-source/palace/migration.mdx

Initialize the Palace directory structure for the core wing and specific halls.

```bash
mkdir -p .agents/brain/wings/wing_dsom_core/hall_facts
mkdir -p .agents/brain/wings/wing_dsom_core/hall_events
mkdir -p .agents/brain/wings/wing_dsom_core/hall_preferences
mkdir -p .agents/brain/wings/wing_dsom_core/hall_discoveries
```

```bash
# hall_facts Rooms
mkdir -p .agents/brain/wings/wing_dsom_core/hall_facts/room_clean_architecture
mkdir -p .agents/brain/wings/wing_dsom_core/hall_facts/room_dsom_protocol
mkdir -p .agents/brain/wings/wing_dsom_core/hall_facts/room_tooling

# hall_events Rooms
mkdir -p .agents/brain/wings/wing_dsom_core/hall_events/room_brain_artifacts
mkdir -p .agents/brain/wings/wing_dsom_core/hall_events/room_ledger

# hall_discoveries Room
mkdir -p .agents/brain/wings/wing_dsom_core/hall_discoveries/room_uncategorised
```

--------------------------------

### Define topics in YAML frontmatter

Source: https://gitlab.com/linuxmalaysia/deep-state-of-mind-for-my-ai/-/blob/main/docs-source/governance/okf-standard.mdx

Examples of well-structured and poorly-structured topic arrays for document indexing.

```yaml
# Good — specific, lowercase, appropriately scoped
topics: ["ansible", "idempotency", "gitops", "playbook", "infrastructure"]

# Poor — too generic, too few, mixed case
topics: ["DSOM", "docs"]
```

--------------------------------

### Audit and Verify Palace Installation

Source: https://gitlab.com/linuxmalaysia/deep-state-of-mind-for-my-ai/-/blob/main/docs-source/palace/migration.mdx

Run pre-flight audits and verify the Palace integration with SOD and EOD routines.

```bash
bash tools/audit-pre-flight.sh
```

```text
[PASS] palace_registry.md exists
[PASS] wings/ directory present
[PASS] Section [14] Palace Registry enabled in reanimate
```

```bash
bash tools/sod-palace.sh
```

```text
### [14] PALACE REGISTRY (Spatial Knowledge Map)
# 🏛️ Palace Registry: Sovereign Retrieval Map
```

```bash
bash tools/eod-palace.sh
# Should show: [Palace Sync] Running Spatial Reflection...
```

--------------------------------

### Bad Commit Message Examples

Source: https://gitlab.com/linuxmalaysia/deep-state-of-mind-for-my-ai/-/blob/main/CONTRIBUTING.md

Examples of commit messages that do not adhere to the project's standard format.

```text
update files
fix bug
done
```

--------------------------------

### Execute Guardrails AI Pipeline

Source: https://gitlab.com/linuxmalaysia/deep-state-of-mind-for-my-ai/-/blob/main/docs/governance/DSOM-CUSTOM-VALIDATORS-GUIDE.md

Example of initializing a Guard object with the custom OKF validator and processing raw LLM output.

```python
"""
Example execution using Guardrails AI Guard
"""
from guardrails import Guard
from tools.validators.guardrails_ai_okf_validator import GuardrailsOKFValidator

# Initialize Guard with custom validator
guard = Guard().use(
    GuardrailsOKFValidator(on_fail="fix")
)

raw_llm_output = "\ufeff---\nokf_version: 0.2\ntitle: Sample\n---\n# Content"

# Validate output
validation_outcome = guard.validate(raw_llm_output)
print(f"Validated & Fixed Output:\n{validation_outcome.validated_output}")
```

--------------------------------

### Initialize a New Project Wing

Source: https://gitlab.com/linuxmalaysia/deep-state-of-mind-for-my-ai/-/blob/main/docs/HOWTO-PALACE-ONBOARDING.md

Creates the necessary directory structure for a new project domain and initializes a mandatory overview room.

```bash
mkdir -p .agents/brain/wings/wing_YOUR_PROJECT/{hall_facts,hall_events,hall_discoveries}

# Create at minimum one Room

mkdir -p .agents/brain/wings/wing_YOUR_PROJECT/hall_facts/room_overview
touch .agents/brain/wings/wing_YOUR_PROJECT/hall_facts/room_overview/closet.md
```

--------------------------------

### Run CheckUsage in Live Monitor Mode

Source: https://gitlab.com/linuxmalaysia/deep-state-of-mind-for-my-ai/-/blob/main/docs/tools/HOWTO-CHECKUSAGE.md

Starts the script in a loop that refreshes every 60 seconds.

```powershell
.\tools\CheckUsage.ps1 -Loop
```

--------------------------------

### Configure Copilot instructions

Source: https://gitlab.com/linuxmalaysia/deep-state-of-mind-for-my-ai/-/blob/main/docs-source/ai-setup/copilot.mdx

Define the DSOM Sovereign Instructions in the repository to guide Copilot behavior during Chat interactions.

```markdown
# DSOM Sovereign Instructions for GitHub Copilot (v6.1 + Palace v1.0)

You are a DSOM-Compliant Cognitive Co-Pilot. You are assisting the project Lead
Architect, a Senior Systems Architect with 30+ years of ICT and Open Source expertise.

OPERATING MODEL:
- Advisory Mode ONLY. You propose. The human approves. Ansible executes. You verify.
- UK English in all responses and documentation.
- Atomic Git Hygiene: Propose changes one file at a time using
  'type(scope): description [Phase/vX.X]' format.
- Ansible is the ONLY executor for OS-level tasks on remote nodes.
- Wait for human output after each step before proceeding.

DSOM LAWS (non-negotiable):
1. Zero-Global Pattern — No global variables; strict state management only.
2. Sovereign Portability — No vendor lock-in; code must be Linux-agnostic.
3. HA-Ready — Design for clusters and zero-downtime.
4. Atomic Git Hygiene — One logical change per commit.
5. Pedagogical Logic — Explain the 'Why' before the 'What'.
6. GitOps Rule — If it is not committed to Git, it does not exist.
7. Ansible-First Execution — No ad-hoc commands on target nodes. Playbooks only.

BRAIN SYNC:
Before answering any architecture question, read:
- .agents/brain/palace_registry.md (Walk the Palace)
- .agents/brain/task.md
- .agents/brain/walkthrough.md
- docs/AI-MASTER-PROTOCOL.md

MIRROR LAW:
If instructions lack architectural clarity, challenge the human and ask for the
missing 'Why' before acting. Silence is the only failure.
```

--------------------------------

### Bootstrap Execution for DSOM Onboarding

Source: https://gitlab.com/linuxmalaysia/deep-state-of-mind-for-my-ai/-/blob/main/docs/tools/HOWTO-DSOM-ONBOARD.md

Execute the onboarding script based on the host operating system to begin the repository integration process.

```powershell

# Windows (T1)

.\tools\dsom-onboard.ps1
```

```bash

# WSL2 / Linux (T2)

bash tools/dsom-onboard.sh
```

--------------------------------

### Initialize and Manage a Project

Source: https://gitlab.com/linuxmalaysia/deep-state-of-mind-for-my-ai/-/blob/main/docs/governance/PYTHON-UV-ENVIRONMENT-GUIDE.md

Scaffolds a new project, adds dependencies, and executes code within an isolated environment.

```bash
uv init my-project
cd my-project
uv add requests    # Installs the 'requests' library instantly
uv run main.py     # Runs your code using the isolated environment
```

--------------------------------

### Execute Standard Build

Source: https://gitlab.com/linuxmalaysia/deep-state-of-mind-for-my-ai/-/blob/main/docs-source/tools/howto-build-sovereign-book.mdx

Run the build script to generate the sovereign book PDF.

```bash
bash tools/build_sovereign_book.sh
```

--------------------------------

### Execute standard build

Source: https://gitlab.com/linuxmalaysia/deep-state-of-mind-for-my-ai/-/blob/main/docs/tools/HOWTO-BUILD-SOVEREIGN-BOOK.md

Run the archival script to generate the PDF book.

```bash
bash tools/build_sovereign_book.sh
```

--------------------------------

### Initialize walkthrough.md content

Source: https://gitlab.com/linuxmalaysia/deep-state-of-mind-for-my-ai/-/blob/main/docs-source/tools/init-brain.mdx

Add the first Mental Anchor entry to track session history.

```markdown
## Mental Anchor — Session 1

Fresh project. Phase 1 pending. AI-COGNITIVE-TWIN-PROTOCOL.md not yet filled in.
Next: complete identity card and run audit-pre-flight.
```

--------------------------------

### Initialize Palace Structure

Source: https://gitlab.com/linuxmalaysia/deep-state-of-mind-for-my-ai/-/blob/main/docs/HOWTO-MIGRATE-TO-PALACE.md

Create the Wing/Hall/Room skeleton using the provided scripts or manual directory creation.

```bash
# Linux / WSL2

bash tools/palace-sync.sh --init-only

# Windows (PowerShell)

.\tools\palace-sync.ps1 -InitOnly
```

```bash
mkdir -p .agents/brain/wings/wing_dsom_core/{hall_facts,hall_events,hall_preferences,hall_discoveries}
mkdir -p .agents/brain/wings/wing_dsom_core/hall_facts/{room_clean_architecture,room_crisp_strategy,room_dsom_protocol,room_tooling}
mkdir -p .agents/brain/wings/wing_dsom_core/hall_events/{room_sovereign_fabric_v9_8,room_brain_artifacts,room_ledger}
mkdir -p .agents/brain/wings/wing_dsom_core/hall_discoveries/room_uncategorised
```

--------------------------------

### DSOM Initialization Test Prompt

Source: https://gitlab.com/linuxmalaysia/deep-state-of-mind-for-my-ai/-/blob/main/docs-source/ai-setup/gemini.mdx

Use this prompt in the preview panel to verify that the Gem has correctly ingested the DSOM framework.

```text
Initialise DSOM status report.
```

--------------------------------

### Execute Start of Day (SOD) Ritual

Source: https://gitlab.com/linuxmalaysia/deep-state-of-mind-for-my-ai/-/blob/main/CONTRIBUTING.md

Commands to synchronize the workspace, verify environment integrity, and generate the AI context manifest at the beginning of a session.

```bash
./tools/git-ritual.sh sod         # Pull latest
./tools/audit-pre-flight.sh       # Verify workspace
bash tools/reanimate.sh           # Generate AI context manifest
# → Upload manifest or paste Hibernation Notes → AI reads → begin
```

--------------------------------

### Initialize Project Brain

Source: https://gitlab.com/linuxmalaysia/deep-state-of-mind-for-my-ai/-/blob/main/README.md

Sets up the required directory structure for task management and session history.

```bash
# Linux / WSL2
bash tools/init-brain.sh
chmod +x tools/*.sh

# Windows (PowerShell)
.\tools\init-brain.ps1
```

--------------------------------

### Mermaid Error Degradation Example

Source: https://gitlab.com/linuxmalaysia/deep-state-of-mind-for-my-ai/-/blob/main/openwiki/INSTRUCTIONS.md

Demonstrates how the system handles invalid Mermaid syntax by degrading the block and adding an error comment.

```text
%% openwiki-error: Line 2 has mismatched grouping characters: 'A[Start)'
   flowchart TD
       A[Start)
```

--------------------------------

### Reference Playbooks for Ansible Automation

Source: https://gitlab.com/linuxmalaysia/deep-state-of-mind-for-my-ai/-/blob/main/docs-source/ai-setup/copilot.mdx

Provide specific playbook and protocol files to guide Copilot in proposing idempotent roles.

```text
Based on #file:playbooks/dsom/site.yml and #file:docs/AI-COGNITIVE-TWIN-PROTOCOL.md,
propose an idempotent role for the T4 Production baseline.
```

--------------------------------

### Initialise the wiki directory

Source: https://gitlab.com/linuxmalaysia/deep-state-of-mind-for-my-ai/-/blob/main/docs/how-to/use-openwiki-emulator.md

Recompiles directories, creates index skeletons, heals Mermaid diagrams, and exports standalone HTML graphs.

```bash
uv run --with pyyaml python tools/openwiki_emulator.py --init
```

--------------------------------

### Initialize Palace Skeleton

Source: https://gitlab.com/linuxmalaysia/deep-state-of-mind-for-my-ai/-/blob/main/docs-source/palace/onboarding.mdx

Create the Palace structure without modifying existing files if the registry is missing.

```bash
bash tools/palace-sync.sh --init-only
```

--------------------------------

### Daily Sync Ritual Commands

Source: https://gitlab.com/linuxmalaysia/deep-state-of-mind-for-my-ai/-/blob/main/docs-source/governance/three-pillars.mdx

Standardized commands for pulling, committing, and pushing project state at the start and end of the day.

```bash
# Start of Day — pull latest state
git pull origin main
bash tools/sod-palace.sh

# End of Day — commit and push
git add .
git commit -m "feat(scope): descriptive message [Phase-XX/vX.X]"
git push origin main
bash tools/eod-palace.sh
```

```powershell
# Start of Day — pull latest state
git pull origin main
.\tools\sod-palace.ps1

# End of Day — commit and push
git add .
git commit -m "feat(scope): descriptive message [Phase-XX/vX.X]"
git push origin main
.\tools\eod-palace.ps1
```

--------------------------------

### Initialize Palace

Source: https://gitlab.com/linuxmalaysia/deep-state-of-mind-for-my-ai/-/blob/main/docs/HOWTO-ADOPT-DSOM.md

Backfill the Palace from Git history.

```bash
bash tools/palace-sync.sh --backfill
```

--------------------------------

### Add guardrails-ai-dsom dependency

Source: https://gitlab.com/linuxmalaysia/deep-state-of-mind-for-my-ai/-/blob/main/docs/governance/DOWNSTREAM-DSOM-COMPLIANCE-MANDATE.md

Install the guardrails package as a development dependency in downstream projects.

```bash
uv add --dev guardrails-ai-dsom
```

--------------------------------

### Execute Scripts with uv

Source: https://gitlab.com/linuxmalaysia/deep-state-of-mind-for-my-ai/-/blob/main/docs/governance/PYTHON-UV-ENVIRONMENT-GUIDE.md

Runs a Python script using uv, which automatically handles interpreter installation and dependency isolation.

```bash
uv run verify.py
```

--------------------------------

### Initialize DSOM Protocol via Manifest

Source: https://gitlab.com/linuxmalaysia/deep-state-of-mind-for-my-ai/-/blob/main/docs-source/rituals/start-of-day.mdx

Use this handshake phrase after uploading the sod_manifest_YYYY-MM-DD.txt file to the AI.

```text
Initialise DSOM Protocol v6.1 + Palace v1.0. Read the uploaded manifest.
Walk the Palace Registry in Section [14] — identify relevant Rooms for
today's work. Summarise the current Mental Anchor from
.agents/brain/walkthrough.md. Confirm the 4-Tier environment map from
AI-COGNITIVE-TWIN-PROTOCOL.md. State: "Sovereign State Synchronised"
when ready.
```

--------------------------------

### Initialize New Project Wing Structure

Source: https://gitlab.com/linuxmalaysia/deep-state-of-mind-for-my-ai/-/blob/main/docs-source/palace/onboarding.mdx

Creates the required directory skeleton and an initial room for a new project wing within the DSOM instance.

```bash
# Create the Wing skeleton
mkdir -p .agents/brain/wings/wing_YOUR_PROJECT/{hall_facts,hall_events,hall_discoveries}

# Create at least one Room to get started
mkdir -p .agents/brain/wings/wing_YOUR_PROJECT/hall_facts/room_overview
touch .agents/brain/wings/wing_YOUR_PROJECT/hall_facts/room_overview/closet.md
```

--------------------------------

### Create Palace Registry

Source: https://gitlab.com/linuxmalaysia/deep-state-of-mind-for-my-ai/-/blob/main/docs/HOWTO-MIGRATE-TO-PALACE.md

Initialize the registry file to track Wings, Halls, and Rooms.

```bash
# If you have the reference repo available:

cp /path/to/dsom-reference/.agents/brain/palace_registry.md .agents/brain/palace_registry.md

# Then edit to match YOUR project's Wing/Hall structure
```

--------------------------------

### Triggering Skills via Natural Language

Source: https://gitlab.com/linuxmalaysia/deep-state-of-mind-for-my-ai/-/blob/main/docs-source/skills/skills-registry.mdx

Examples of natural language prompts that trigger specific skills within the DSOM framework.

```text
"Bootstrap DSOM for my new repository."
→ Triggers: DSOM Bootstrap

"Run the end-of-day sync and push my memory to Git."
→ Triggers: EOD Palace Sync

"Compile my proposal.md into a PDF."
→ Triggers: LaTeX Proposal Compiler

"Set up passwordless SSH to all my production nodes."
→ Triggers: SSH Passwordless Setup

"Extract all text from the vendor contract PDF."
→ Triggers: PDF Text Extractor
```

--------------------------------

### Execute SOD Palace Command

Source: https://gitlab.com/linuxmalaysia/deep-state-of-mind-for-my-ai/-/blob/main/docs-source/palace/onboarding.mdx

Runs the daily Start of Day ritual script to synchronize the environment and generate the reanimation manifest.

```bash
# Linux / WSL2
bash tools/sod-palace.sh

# Windows (PowerShell)
.\tools\sod-palace.ps1
```

--------------------------------

### Execute Start of Day (SOD) Palace Script

Source: https://gitlab.com/linuxmalaysia/deep-state-of-mind-for-my-ai/-/blob/main/README.md

Automates Git pulls, audit pre-flights, registry verification, and reanimation manifest generation.

```powershell
.\tools\sod-palace.ps1            # Windows Native
```

```bash
bash tools/sod-palace.sh          # Linux / WSL2
```

--------------------------------

### Integrate DSOM Validators into Guardrails AI

Source: https://gitlab.com/linuxmalaysia/deep-state-of-mind-for-my-ai/-/blob/main/docs/governance/DSOM-GUARDRAILS-CATALOG-SUBMISSION-REVIEW.md

Example of importing and applying multiple DSOM guardrails to a Guard object to protect AI agent outputs.

```python
from guardrails import Guard
from guardrails_dsom import (
    GuardrailsOKFBOMValidator,
    GuardrailsCredentialGuardian,
    GuardrailsSovereignSignatureValidator,
)

# Protect an AI Agent with DSOM Sovereign Guardrails
guard = Guard().use_many(
    GuardrailsOKFBOMValidator(on_fail="fix"),
    GuardrailsCredentialGuardian(on_fail="exception"),
    GuardrailsSovereignSignatureValidator(on_fail="fix"),
)

validated_output = guard.validate(llm_generated_content)
```

--------------------------------

### Execute Brain Initializer

Source: https://gitlab.com/linuxmalaysia/deep-state-of-mind-for-my-ai/-/blob/main/docs/tools-and-automation/init-brain.md

Run the initialization script from the project root to set up the agent environment.

```bash
./tools/init-brain.sh
```

--------------------------------

### Create Directory Structure

Source: https://gitlab.com/linuxmalaysia/deep-state-of-mind-for-my-ai/-/blob/main/docs/HOWTO-SETUP-ANSIBLE-BASELINE.md

Initializes the standard Ansible project skeleton and configures .gitignore to protect sensitive files.

```bash

# From project root (Tier 2 Dev Bridge or WSL2)

mkdir -p inventory/group_vars inventory/host_vars
mkdir -p playbooks/dsom
mkdir -p roles
mkdir -p vault
mkdir -p .logs

# Protect the vault directory — NEVER commit secrets

cat >> .gitignore << 'EOF'

# Ansible Vault & Sensitive Files

vault/*.yml
!vault/.gitignore
*.retry
.logs/*.log
EOF

# Create vault .gitignore placeholder

echo "# Vault secrets are NEVER committed to Git" > vault/.gitignore

```

--------------------------------

### Create Plugin Directory

Source: https://gitlab.com/linuxmalaysia/deep-state-of-mind-for-my-ai/-/blob/main/docs/governance/DSOM-AGENT-PLUGINS-SPECIFICATION.md

Initializes the directory structure for a new DSOM plugin skill.

```bash
mkdir -p plugins/my-ops-plugin/skills/sys-audit
```

--------------------------------

### Create Project Directory

Source: https://gitlab.com/linuxmalaysia/deep-state-of-mind-for-my-ai/-/blob/main/docs/tutorials/TEAM-DSOM-MASTERCLASS.md

Initial commands to create and enter a new project directory on different operating systems.

```powershell
# Windows PowerShell
mkdir D:\Projects\my-awesome-service
cd D:\Projects\my-awesome-service
```

```bash
# Linux / macOS / WSL2
mkdir -p ~/projects/my-awesome-service
cd ~/projects/my-awesome-service
```

--------------------------------

### Initialize Git Repository

Source: https://gitlab.com/linuxmalaysia/deep-state-of-mind-for-my-ai/-/blob/main/docs/HOWTO-CLONE-DSOM-PROJECT.md

Initialize the version control system required for ritual scripts.

```bash
git init
git branch -M main
```

--------------------------------

### Expected Audit Output

Source: https://gitlab.com/linuxmalaysia/deep-state-of-mind-for-my-ai/-/blob/main/docs-source/tools/audit-pre-flight.mdx

Example of a successful audit session output showing various status labels.

```text
[PASS] Brain Check      — task.md and walkthrough.md found
[PASS] Git Sync         — Local matches origin/main
[OK]   Discovery        — Node v20.11.0 detected
[PASS] Cognitive Twin   — No [PLACEHOLDER] fields found
[SKIP] Ansible Baseline — ansible not installed (non-infra project)
```

--------------------------------

### Test SOD and EOD Playlists

Source: https://gitlab.com/linuxmalaysia/deep-state-of-mind-for-my-ai/-/blob/main/docs/governance/GITOPS-AIOPS-ANSIBLE-STRATEGY.md

Commands to test Start of Day (SOD) and End of Day (EOD) automation playlists for Linux and Windows environments.

```bash
bash tools/sod-palace.sh  # (Windows: .\tools\sod-palace.ps1)
```

```bash
bash tools/eod-palace.sh  # (Windows: .\tools\eod-palace.ps1)
```

--------------------------------

### Confirm Initialisation Output

Source: https://gitlab.com/linuxmalaysia/deep-state-of-mind-for-my-ai/-/blob/main/docs-source/tools/openwiki-usage.mdx

Expected confirmation message after running the initialization command.

```text
[OpenWiki Emulator] Successfully updated ./openwiki/ structure.
```

--------------------------------

### Execute OpenWiki via Native Python Emulator

Source: https://gitlab.com/linuxmalaysia/deep-state-of-mind-for-my-ai/-/blob/main/docs/governance/OPENWIKI-INTEGRATION-GUIDE.md

Use this command for local development, CI/CD pipelines, and environments without pre-installed binaries.

```bash
uv run --with pyyaml python tools/openwiki_emulator.py
```

--------------------------------

### Implement Directory Router

Source: https://gitlab.com/linuxmalaysia/deep-state-of-mind-for-my-ai/-/blob/main/docs/OKF-ADOPTION-GUIDE.md

Create an index.md file in subdirectories to list concept files.

```markdown
# Database Schemas Directory

* [orders.md](orders.md) - Orders table schema and relational joins.
* [customers.md](customers.md) - Customer profile definitions and GDPR compliance constraints.
```

--------------------------------

### Verify Server and Run Tests

Source: https://gitlab.com/linuxmalaysia/deep-state-of-mind-for-my-ai/-/blob/main/docs-source/tools/howto-mcp-server.mdx

Commands to check server help output and execute the project's regression test suite.

```bash
uv run tools/mcp/server.py --help
```

```bash
uv run --with pyyaml python -m unittest tests/test_okf_frontmatter_bom_reorder.py tests/test_okf_quoting.py tests/test_seo_sitemaps.py tests/test_docs_symlinks.py
```

--------------------------------

### Bootstrap Execution for DSOM Onboarding

Source: https://gitlab.com/linuxmalaysia/deep-state-of-mind-for-my-ai/-/blob/main/docs-source/tools/howto-dsom-onboard.mdx

Execute the bootstrap script to initiate the DSOM onboarding process on Windows or Linux/WSL2 environments.

```powershell

# Windows (T1)

.\tools\dsom-onboard.ps1

```

```bash

# WSL2 / Linux (T2)

bash tools/dsom-onboard.sh

```

--------------------------------

### Execute generate_sitemaps.py via CLI

Source: https://gitlab.com/linuxmalaysia/deep-state-of-mind-for-my-ai/-/blob/main/docs/reference/generate_sitemaps.md

Run the sitemap generation utility using the uv environment manager.

```bash
uv run python tools/generate_sitemaps.py
```

--------------------------------

### Create Skill Directory

Source: https://gitlab.com/linuxmalaysia/deep-state-of-mind-for-my-ai/-/blob/main/docs/AI-AGENT-SKILLS-GUIDE.md

Use this command to initialize the folder structure for a new agent skill.

```bash
mkdir -p .agents/skills/my-custom-skill
```

--------------------------------

### Manual Project Scaffolding (PowerShell)

Source: https://gitlab.com/linuxmalaysia/deep-state-of-mind-for-my-ai/-/blob/main/docs/tutorials/TEAM-DSOM-MASTERCLASS.md

Manual commands to copy the DSOM baseline structure into a new project directory on Windows.

```powershell
# Set variables
$DSOM_BASE = "D:\Users\LinuxMalaysia\Projects\deep-state-of-mind-for-my-ai"
$TARGET = "D:\Projects\my-awesome-service"

# 1. Create Target Directory Tree
New-Item -ItemType Directory -Force -Path "$TARGET\.agents\brain"
New-Item -ItemType Directory -Force -Path "$TARGET\.agents\skills"
New-Item -ItemType Directory -Force -Path "$TARGET\.github"
New-Item -ItemType Directory -Force -Path "$TARGET\docs\governance"
New-Item -ItemType Directory -Force -Path "$TARGET\tools"

# 2. Copy the Universal Gateway Matrix & Manifests
Copy-Item "$DSOM_BASE\AGENTS.md" "$TARGET\"
Copy-Item "$DSOM_BASE\.cursorrules" "$TARGET\"
Copy-Item "$DSOM_BASE\CLAUDE.md" "$TARGET\"
Copy-Item "$DSOM_BASE\START-HERE.md" "$TARGET\"
Copy-Item "$DSOM_BASE\plugin.json" "$TARGET\"
Copy-Item "$DSOM_BASE\mcp.json" "$TARGET\"
Copy-Item "$DSOM_BASE\.github\copilot-instructions.md" "$TARGET\.github\"

# 3. Copy the Core Rulebook and Tooling
Copy-Item "$DSOM_BASE\.agents\AGENTS.md" "$TARGET\.agents\"
Copy-Item -Recurse "$DSOM_BASE\tools\*" "$TARGET\tools\"

# 4. Copy Essential Domain Skills (e.g., token calculator, signature injector, plugin packager)
Copy-Item -Recurse "$DSOM_BASE\.agents\skills\dsom-token-calculator" "$TARGET\.agents\skills\"
Copy-Item -Recurse "$DSOM_BASE\.agents\skills\dsom-signature-injector" "$TARGET\.agents\skills\"
Copy-Item -Recurse "$DSOM_BASE\.agents\skills\agent-plugin-packager" "$TARGET\.agents\skills\"
Copy-Item -Recurse "$DSOM_BASE\.agents\skills\initialize-gitops" "$TARGET\.agents\skills\"

# 5. Initialize Fresh Brain in the Target
Set-Location $TARGET
bash tools/init-brain.sh  # Or .\tools\init-brain.ps1
```

--------------------------------

### Execute OpenWiki via Legacy Node.js CLI

Source: https://gitlab.com/linuxmalaysia/deep-state-of-mind-for-my-ai/-/blob/main/docs/governance/OPENWIKI-INTEGRATION-GUIDE.md

Use these commands on headless Linux servers where the Node.js environment is already configured.

```bash
openwiki code --init
```

```bash
openwiki visualize
```

--------------------------------

### Run Pre-Release Test Suite

Source: https://gitlab.com/linuxmalaysia/deep-state-of-mind-for-my-ai/-/blob/main/docs-source/skills/dsom-release-manager.mdx

Executes the full test suite using uv to ensure a 100% pass rate before proceeding with the release.

```bash
uv run --with pytest --with pyyaml --with requests --with tiktoken --with fastmcp pytest
```

--------------------------------

### Scaffold DSOM Directory Structure and Copy Files

Source: https://gitlab.com/linuxmalaysia/deep-state-of-mind-for-my-ai/-/blob/main/docs/HOWTO-CLONE-DSOM-PROJECT.md

Execute these commands in the root of the new project to establish the directory structure and copy essential files from the baseline path.

```bash

# 1. Establish Directories

mkdir -p .agents/brain/wings
mkdir -p .agents/skills
mkdir -p docs/agent-configs
mkdir -p docs/governance
mkdir -p tools

# 2. Copy the Engine and Skills

cp [OLD_PATH]/.agents/AGENTS.md .agents/
cp -r [OLD_PATH]/.agents/skills/* .agents/skills/

# 3. Copy Governance and Documentation

cp -r [OLD_PATH]/docs/agent-configs/* docs/agent-configs/
cp -r [OLD_PATH]/docs/governance/* docs/governance/
cp [OLD_PATH]/docs/AI-AGENT-SKILLS-GUIDE.md docs/
cp [OLD_PATH]/docs/HOWTO-*.md docs/

# 4. Copy Ritual Toolchain

cp -r [OLD_PATH]/tools/* tools/
```

--------------------------------

### Redact Sensitive Values

Source: https://gitlab.com/linuxmalaysia/deep-state-of-mind-for-my-ai/-/blob/main/docs-source/tools/privacy-guardian.mdx

Example of replacing real values with placeholders in documentation while keeping secrets in a local .env file.

```text
# In AI-COGNITIVE-TWIN-PROTOCOL.md — safe for public manifests:
T3: [PROD-HOST-1] — AlmaLinux 10, uid=1001

# In .env — never committed to Git:
T3_HOST=192.168.1.50
T3_USER=deploy
```

--------------------------------

### Initialize task.md content

Source: https://gitlab.com/linuxmalaysia/deep-state-of-mind-for-my-ai/-/blob/main/docs-source/tools/init-brain.mdx

Add initial task entries using the required checkbox format.

```markdown
## Session 1 — Project Initialisation

- [ ] Fill in AI-COGNITIVE-TWIN-PROTOCOL.md
- [ ] Set up Ansible inventory
- [ ] Verify all audit-pre-flight checks pass
```

--------------------------------

### Initialize WSL2 AlmaLinux Environment

Source: https://gitlab.com/linuxmalaysia/deep-state-of-mind-for-my-ai/-/blob/main/CONTRIBUTING.md

Run this command in Windows PowerShell as Administrator to set up the dsom-control-almalinux10 instance.

```powershell
# Run from Windows PowerShell (as Administrator once)
.\tools\setup-wsl-almalinux10.ps1
```

--------------------------------

### Run Palace Sync and Review Proposal

Source: https://gitlab.com/linuxmalaysia/deep-state-of-mind-for-my-ai/-/blob/main/docs-source/palace/migration.mdx

Backfill git history into the Palace structure and review the generated update proposal.

```bash
bash tools/palace-sync.sh --backfill
```

```powershell
.\tools\palace-sync.ps1 -Backfill
```

```bash
cat .agents/brain/palace_update_proposal_$(date +%Y-%m-%d).md
```

--------------------------------

### Create Reanimation Prompt File

Source: https://gitlab.com/linuxmalaysia/deep-state-of-mind-for-my-ai/-/blob/main/docs/COPILOT-SETUP.md

Enable quick context injection for Copilot Chat by creating this prompt file in the .github/prompts directory.

```markdown
---
mode: 'agent'
description: 'DSOM SOD Reanimation — load current brain and Mental Anchor'
---

Read the following brain artifacts and summarise the current status:
1. `.agents/brain/palace_registry.md` — identify the current spatial mapping.
2. `.agents/brain/task.md` — list all incomplete `[ ]` items.
3. `.agents/brain/walkthrough.md` — state the last Mental Anchor.
4. `docs/AI-COGNITIVE-TWIN-PROTOCOL.md` — confirm the 4-Tier environment map.

Then state: 'Sovereign State Synchronised — [PROJECT NAME] is live.'
Operate under DSOM v6.1 + Palace v1.0: Advisory Mode, UK English, Git-first, Ansible-only execution.

```

--------------------------------

### CRDA Failure Log Signature

Source: https://gitlab.com/linuxmalaysia/deep-state-of-mind-for-my-ai/-/blob/main/docs/governance/GITHUB-ACTIONS-SECURITY-SCANNING.md

Example of the DNS resolution error observed in GitHub Actions logs when the CRDA backend is unreachable.

```text

crda exited with code 1
FIL Unable to reach the server.
error="Post \"https://gw.api.openshift.io/user?user_key=...\": dial tcp:
lookup gw.api.openshift.io on 127.0.0.53:53: no such host"

```

--------------------------------

### Sync and Verify

Source: https://gitlab.com/linuxmalaysia/deep-state-of-mind-for-my-ai/-/blob/main/docs/HOWTO-MIGRATE-TO-PALACE.md

Sync across machines and verify the Palace configuration.

```bash
# On the second machine

git pull --rebase origin main
```

```bash
# Recommended on T2 (WSL2 / Linux)

bash tools/sod-palace.sh  # (Windows: .\tools\sod-palace.ps1)

# Or manual (Linux/Windows)

bash tools/reanimate.sh | grep -A 5 "\[14\]"
```

```bash
# Recommended on T2 (WSL2 / Linux)

bash tools/eod-palace.sh  # (Windows: .\tools\eod-palace.ps1)

# Or manual (Linux/Windows)

bash tools/hibernation.sh

# Should show: [Palace Sync] Running Spatial Reflection...
```

--------------------------------

### View Full Command Syntax

Source: https://gitlab.com/linuxmalaysia/deep-state-of-mind-for-my-ai/-/blob/main/docs-source/tools/howto-checkusage-linux.mdx

Displays all available options and flags for the check-usage.sh script.

```bash
bash tools/check-usage.sh [options]

Options:
  -l, --loop              Live monitor mode
  -i, --interval SECS     Refresh interval [default: 60]
  -w, --threshold MB      WARNING threshold [default: 20]
  -c, --critical MB       CRITICAL threshold [default: 50]
  -n, --top N             Show only top N sessions
  --no-pulse              Suppress the progress bar
```

--------------------------------

### Create Copilot Instructions File

Source: https://gitlab.com/linuxmalaysia/deep-state-of-mind-for-my-ai/-/blob/main/docs/COPILOT-SETUP.md

Define repository-wide behavior and constraints for GitHub Copilot by creating this file in the root directory.

```markdown

# DSOM Sovereign Instructions for GitHub Copilot (v6.1 + Palace v1.0)

You are a DSOM-Compliant Cognitive Co-Pilot. You are assisting Harisfazillah Jamel,
a Senior Systems Architect with 35+ years of ICT and Open Source expertise.

OPERATING MODEL:
- Advisory Mode ONLY. You propose. The human approves. Ansible executes. You verify.
- UK English in all responses and documentation.
- Atomic Git Hygiene: Propose changes one file at a time using 'type(scope): description [Phase/vX.X]' format.
- Ansible is the ONLY executor for OS-level tasks on remote nodes.
- Wait for human output after each step before proceeding.

DSOM LAWS (non-negotiable):
1. Zero-Global Pattern — No global variables; strict state management only.
2. Sovereign Portability — No vendor lock-in; code must be Linux-agnostic.
3. HA-Ready — Design for clusters and zero-downtime.
4. Atomic Git Hygiene — One logical change per commit.
5. Pedagogical Logic — Explain the 'Why' before the 'What'.
6. GitOps Rule — If it is not committed to Git, it does not exist.
7. Ansible-First Execution — No ad-hoc commands on target nodes. Playbooks only.

BRAIN SYNC:
Before answering any architecture question, read:
- .agents/brain/palace_registry.md (Walk the Palace)
- .agents/brain/task.md
- .agents/brain/walkthrough.md
- docs/AI-MASTER-PROTOCOL.md

MIRROR LAW:
If instructions lack architectural clarity, challenge the human and ask for the
missing 'Why' before acting. Silence is the only failure.

```

--------------------------------

### Create the SKILL.md File

Source: https://gitlab.com/linuxmalaysia/deep-state-of-mind-for-my-ai/-/blob/main/docs-source/skills/creating-skills.mdx

Define the skill's metadata and operational instructions using YAML frontmatter and markdown body content.

```yaml
---
name: my-custom-skill
description: "A very clear, 1-2 sentence description explaining exactly when the AI should load this skill. For example: 'Use this skill when the user asks to deploy HAProxy on a new node or reconfigure an existing load balancer.'"
---

# My Custom Skill Guide

## Purpose
Briefly explain what this skill achieves and what problem it solves.

## Execution Steps
1. Run command A.
2. Verify output B.
3. If error C happens, do D.
```

--------------------------------

### Execute Sovereign Save Git Ritual

Source: https://gitlab.com/linuxmalaysia/deep-state-of-mind-for-my-ai/-/blob/main/docs/EOD-RITUAL.md

Guides the user through creating a semantic commit message or performs a direct push with a specified message.

```powershell
# T1 — Interactive EOD Sovereign Save (guided semantic commit)

.\tools\git-ritual.ps1
```

```bash
# T2 — Interactive EOD Sovereign Save

./tools/git-ritual.sh
```

```powershell
.\tools\git-ritual.ps1 push "chore(brain): EOD sovereign save — [Phase-X/vX.X]"
```

--------------------------------

### Initialize Brain on Windows and Linux

Source: https://gitlab.com/linuxmalaysia/deep-state-of-mind-for-my-ai/-/blob/main/docs/tools/HOWTO-INIT-BRAIN.md

Execute the initialization script for the respective operating system to create the required directory structure and artifacts.

```powershell

# Windows (T1)

.\tools\init-brain.ps1

```

```bash

# WSL2 / Linux (T2)

bash tools/init-brain.sh

```

--------------------------------

### Deploy Release to Platforms

Source: https://gitlab.com/linuxmalaysia/deep-state-of-mind-for-my-ai/-/blob/main/docs-source/skills/dsom-release-manager.mdx

Uses CLI tools to create releases on GitHub and GitLab using the generated release notes artifact.

```bash
gh release create v<VERSION> -F "<ARTIFACT_PATH>/release_notes_v<VERSION>.md" --title "v<VERSION>: <RELEASE_TITLE>"
```

```bash
glab release create v<VERSION> --name "v<VERSION>: <RELEASE_TITLE>" --notes-file "<ARTIFACT_PATH>/release_notes_v<VERSION>.md"
```

--------------------------------

### Perform Interactive EOD Sovereign Save

Source: https://gitlab.com/linuxmalaysia/deep-state-of-mind-for-my-ai/-/blob/main/docs-source/rituals/eod-ritual.mdx

Guides the user through creating a semantic commit message for EOD synchronization. Available for both Windows and Linux/WSL2 environments.

```powershell
.\tools\git-ritual.ps1
```

```bash
./tools/git-ritual.sh
```

--------------------------------

### Initialize DSOM Protocol Prompt

Source: https://gitlab.com/linuxmalaysia/deep-state-of-mind-for-my-ai/-/blob/main/docs-source/governance/multi-agent.mdx

The required initialization prompt to be sent to the agent to ensure it acknowledges the architecture and Palace Registry.

```text
I am initialising the DSOM Protocol v6.1 + Palace v1.0.
Acknowledge the architecture in the manifest.
Walk the Palace Registry and identify the relevant Rooms for today's task.
State 'Sovereign State Synchronised' when ready.
```

--------------------------------

### Generate Fresh Manifest

Source: https://gitlab.com/linuxmalaysia/deep-state-of-mind-for-my-ai/-/blob/main/docs/HOWTO-UPGRADE-DSOM.md

Reanimate the project to include the new Palace Registry section.

```bash
bash tools/reanimate.sh
```

--------------------------------

### OKF Frontmatter & UTF-8 BOM Stripper Implementation

Source: https://gitlab.com/linuxmalaysia/deep-state-of-mind-for-my-ai/-/blob/main/docs/governance/DSOM-GUARDRAILS-CATALOG-SUBMISSION-REVIEW.md

Validator class to detect and strip UTF-8 BOM characters and ensure YAML frontmatter starts with the correct fence.

```python
# Prototype Implementation
from guardrails.validators import Validator, register_validator, PassResult, FailResult

@register_validator(name="dsom/okf_bom_validator", data_type="string")
class GuardrailsOKFBOMValidator(Validator):
    def validate(self, value, metadata=None):
        text = str(value)
        if text.startswith("\ufeff"):
            return FailResult(error_message="BOM detected", fix_value=text.lstrip("\ufeff"))
        if not (text.startswith("---\n") or text.startswith("---\r\n")):
            return FailResult(error_message="Document must start on Line 1 with '---'")
        return PassResult()
```

--------------------------------

### Synchronize Project via Push Script

Source: https://gitlab.com/linuxmalaysia/deep-state-of-mind-for-my-ai/-/blob/main/docs/reference-architectures/ANSIBLE-CONTROL-NODE-PROTOCOL.md

Rapid prototyping method to mirror local Windows codebase to the jumphost using a PowerShell script.

```powershell
# Mirror the local Windows codebase directly to the example-node Jumphost

.\tools\push-to-jumphost.ps1
```

--------------------------------

### Run Backfill

Source: https://gitlab.com/linuxmalaysia/deep-state-of-mind-for-my-ai/-/blob/main/docs/HOWTO-MIGRATE-TO-PALACE.md

Generate a proposal file mapping git history to Palace Rooms.

```bash
# Linux / WSL2

bash tools/palace-sync.sh --backfill

# Windows (PowerShell)

.\tools\palace-sync.ps1 -Backfill
```

--------------------------------

### Execute template-reset for project initialization

Source: https://gitlab.com/linuxmalaysia/deep-state-of-mind-for-my-ai/-/blob/main/docs-source/tools/template-reset.mdx

Run the appropriate reset script immediately after cloning the repository to clear inherited artifacts. Requires manual confirmation before the purge proceeds.

```bash
git clone https://github.com/linuxmalaysia/deep-state-of-mind-for-my-ai.git my-new-project
cd my-new-project
bash tools/template-reset.sh
```

```powershell
git clone https://github.com/linuxmalaysia/deep-state-of-mind-for-my-ai.git my-new-project
cd my-new-project
.\tools\template-reset.ps1
```

--------------------------------

### Commit configuration files

Source: https://gitlab.com/linuxmalaysia/deep-state-of-mind-for-my-ai/-/blob/main/docs-source/ai-setup/copilot.mdx

Stage and push the newly created Copilot configuration files to the repository.

```bash
git add .github/copilot-instructions.md .github/prompts/dsom-reanimate.prompt.md
git commit -m "chore(copilot): add DSOM Sovereign Instructions and reanimate prompt"
git push
```

--------------------------------

### Execute generate-walkthrough

Source: https://gitlab.com/linuxmalaysia/deep-state-of-mind-for-my-ai/-/blob/main/docs/tools/HOWTO-GENERATE-WALKTHROUGH.md

Run the appropriate script for your operating system to generate the session anchor block.

```powershell

# Windows (T1)

.\tools\generate-walkthrough.ps1

```

```bash

# WSL2 / Linux (T2)

bash tools/generate-walkthrough.sh

```

--------------------------------

### Provision Brain via Ansible

Source: https://gitlab.com/linuxmalaysia/deep-state-of-mind-for-my-ai/-/blob/main/docs/tools/HOWTO-INIT-BRAIN.md

Use the provided Ansible playbook to perform the infrastructure bootstrap.

```bash
ansible-playbook playbooks/dsom/init-brain.yml

```

--------------------------------

### Onboard DSOM on Windows

Source: https://gitlab.com/linuxmalaysia/deep-state-of-mind-for-my-ai/-/blob/main/docs/HOWTO-DSOM-ONBOARDING.md

Use the PowerShell script to bridge command execution for native Windows environments.

```powershell

# 1. Download the safe bootstrap script to the root of your repository

Invoke-WebRequest -Uri "https://raw.githubusercontent.com/linuxmalaysia/deep-state-of-mind-for-my-ai/main/tools/dsom-onboard.ps1" -OutFile "dsom-onboard.ps1"

# 2. Run the onboarding process

.\dsom-onboard.ps1

```

--------------------------------

### Log Decisions to the Walkthrough File

Source: https://gitlab.com/linuxmalaysia/deep-state-of-mind-for-my-ai/-/blob/main/README.md

Prompt template for capturing decision-making context and rejected alternatives within the walkthrough log.

```text
"Log this decision to walkthrough.md:
 Decision: [what] | Alternatives rejected: [what and why] | Reason: [why this]"
```

--------------------------------

### Execute OpenWiki CLI Commands

Source: https://gitlab.com/linuxmalaysia/deep-state-of-mind-for-my-ai/-/blob/main/docs-source/tools/howto-openwiki.mdx

Use these commands to initialize, update, search, or export the OpenWiki knowledge base using the Python uv environment.

```bash
uv run --with pyyaml python tools/openwiki_emulator.py --init
```

```bash
uv run --with pyyaml python tools/openwiki_emulator.py --update
```

```bash
uv run --with pyyaml python tools/openwiki_emulator.py --search "Ansible"
```

```bash
uv run --with pyyaml python tools/openwiki_emulator.py --export-graph
```

--------------------------------

### Apply GPL v3.0 Signature Footer

Source: https://gitlab.com/linuxmalaysia/deep-state-of-mind-for-my-ai/-/blob/main/docs-source/skills/okf-frontmatter-injector.mdx

Run this command after frontmatter injection to ensure all Markdown documents include the required signature footer.

```bash
python .agents/skills/dsom-signature-injector/scripts/inject.py <TARGET_DIRECTORY>
```

--------------------------------

### Implement OpenWiki Emulator in Python

Source: https://gitlab.com/linuxmalaysia/deep-state-of-mind-for-my-ai/-/blob/main/docs/governance/OPENWIKI-INTEGRATION-GUIDE.md

A native Python script that initializes the required directory structure for an OpenWiki knowledge graph using uv for dependency management.

```python
# /// script
# dependencies = [

#     "pyyaml>=6.0",
# ]

# ///

"""
OpenWiki Emulator & Knowledge Graph Generator
Author:   Harisfazillah Jamel (LinuxMalaysia)
License:  GNU General Public License v3.0

Description:
Emulates the OpenWiki CLI documentation & knowledge graph generation natively in Python
using `uv run`, requiring zero Node.js binaries or external API keys.
"""

import datetime
import json
import os
import pathlib
import sys
import yaml

REPO_ROOT = pathlib.Path(__file__).resolve().parent.parent
OPENWIKI_DIR = REPO_ROOT / "openwiki"

def get_timestamp() -> str:
    return datetime.datetime.now(datetime.timezone.utc).strftime("%Y-%m-%dT%H:%M:%SZ")

def ensure_openwiki_dirs():
    dirs = [
        OPENWIKI_DIR,
        OPENWIKI_DIR / "architecture",
        OPENWIKI_DIR / "governance",
        OPENWIKI_DIR / "memory",
        OPENWIKI_DIR / "automation",
        OPENWIKI_DIR / "integrations",
        OPENWIKI_DIR / "publishing",
        OPENWIKI_DIR / "quality",
    ]
    for d in dirs:
        d.mkdir(parents=True, exist_ok=True)

def main():
    print(f"[OpenWiki Emulator] Generating native wiki under {OPENWIKI_DIR}...")
    ensure_openwiki_dirs()
    print("[OpenWiki Emulator] Successfully updated ./openwiki/ structure.")

if __name__ == "__main__":
    main()
```

--------------------------------

### Execute Start-of-Day (SOD) Automation

Source: https://gitlab.com/linuxmalaysia/deep-state-of-mind-for-my-ai/-/blob/main/llms-full.txt

Run the SOD script on T2 (Linux/WSL2) to initialize the daily workflow.

```bash
# On T2 (Linux/WSL2) — Run this first every morning
bash tools/sod-palace.sh  # (Windows: .\tools\sod-palace.ps1)
```

--------------------------------

### Implement OpenWiki Emulator in Python

Source: https://gitlab.com/linuxmalaysia/deep-state-of-mind-for-my-ai/-/blob/main/docs-source/governance/openwiki-integration-guide.mdx

A native Python script for emulating OpenWiki CLI documentation and directory structure without external dependencies.

```python

# /// script
# dependencies = [

#     "pyyaml>=6.0",
# ]

# ///

"""
OpenWiki Emulator & Knowledge Graph Generator
Author:   Harisfazillah Jamel (LinuxMalaysia)
License:  GNU General Public License v3.0

Description:
Emulates the OpenWiki CLI documentation & knowledge graph generation natively in Python
using `uv run`, requiring zero Node.js binaries or external API keys.
"""

import datetime
import json
import os
import pathlib
import sys
import yaml

REPO_ROOT = pathlib.Path(__file__).resolve().parent.parent
OPENWIKI_DIR = REPO_ROOT / "openwiki"

def get_timestamp() -> str:
    return datetime.datetime.now(datetime.timezone.utc).strftime("%Y-%m-%dT%H:%M:%SZ")

def ensure_openwiki_dirs():
    dirs = [
        OPENWIKI_DIR,
        OPENWIKI_DIR / "architecture",
        OPENWIKI_DIR / "governance",
        OPENWIKI_DIR / "memory",
        OPENWIKI_DIR / "automation",
        OPENWIKI_DIR / "integrations",
        OPENWIKI_DIR / "publishing",
        OPENWIKI_DIR / "quality",
    ]
    for d in dirs:
        d.mkdir(parents=True, exist_ok=True)

def main():
    print(f"[OpenWiki Emulator] Generating native wiki under {OPENWIKI_DIR}...")
    ensure_openwiki_dirs()
    print("[OpenWiki Emulator] Successfully updated ./openwiki/ structure.")

if __name__ == "__main__":
    main()
```

--------------------------------

### Execute Start-of-Day (SOD) Ritual

Source: https://gitlab.com/linuxmalaysia/deep-state-of-mind-for-my-ai/-/blob/main/docs/governance/AI-COGNITIVE-TWIN-PROTOCOL.md

Run the SOD script on T2 (Linux/WSL2) to initialize the daily manifest and prepare the Palace Registry.

```bash

# On T2 (Linux/WSL2) — Run this first every morning

bash tools/sod-palace.sh  # (Windows: .\tools\sod-palace.ps1)

```

--------------------------------

### Scaffold DSOM Project via Terminal

Source: https://gitlab.com/linuxmalaysia/deep-state-of-mind-for-my-ai/-/blob/main/docs/tutorials/TEAM-DSOM-MASTERCLASS.md

Use this script to clone the baseline and manually copy the required directory structure, manifests, and core skills to your target project.

```bash
# Clone DSOM baseline into a temp location (if not already local)
git clone https://github.com/linuxmalaysia/deep-state-of-mind-for-my-ai.git /tmp/dsom-baseline

# Set variables
DSOM_BASE="/tmp/dsom-baseline"
TARGET="$HOME/projects/my-awesome-service"

# 1. Create directory tree
mkdir -p "$TARGET/.agents/brain" "$TARGET/.agents/skills" "$TARGET/.github" "$TARGET/docs/governance" "$TARGET/tools"

# 2. Copy Universal Gateway Matrix, Manifests & Constitution
cp "$DSOM_BASE/AGENTS.md" "$TARGET/"
cp "$DSOM_BASE/.cursorrules" "$TARGET/"
cp "$DSOM_BASE/CLAUDE.md" "$TARGET/"
cp "$DSOM_BASE/START-HERE.md" "$TARGET/"
cp "$DSOM_BASE/plugin.json" "$TARGET/"
cp "$DSOM_BASE/mcp.json" "$TARGET/"
cp "$DSOM_BASE/.github/copilot-instructions.md" "$TARGET/.github/"
cp "$DSOM_BASE/.agents/AGENTS.md" "$TARGET/.agents/"

# 3. Copy Tools & Core Skills
cp -r "$DSOM_BASE/tools/"* "$TARGET/tools/"
cp -r "$DSOM_BASE/.agents/skills/dsom-token-calculator" "$TARGET/.agents/skills/"
cp -r "$DSOM_BASE/.agents/skills/dsom-signature-injector" "$TARGET/.agents/skills/"
cp -r "$DSOM_BASE/.agents/skills/agent-plugin-packager" "$TARGET/.agents/skills/"
cp -r "$DSOM_BASE/.agents/skills/initialize-gitops" "$TARGET/.agents/skills/"

# 4. Initialize clean brain templates
cd "$TARGET"
bash tools/init-brain.sh
```

--------------------------------

### Initialize the Brain

Source: https://gitlab.com/linuxmalaysia/deep-state-of-mind-for-my-ai/-/blob/main/CONTRIBUTING.md

Initialize the project brain using shell scripts or Ansible.

```bash
bash tools/init-brain.sh
# Or via Ansible:
ansible-playbook playbooks/dsom/init-brain.yml -i localhost,
```

--------------------------------

### Initialize Brain via Ansible

Source: https://gitlab.com/linuxmalaysia/deep-state-of-mind-for-my-ai/-/blob/main/docs-source/tools/howto-init-brain.mdx

Use the provided Ansible playbook to provision the brain structure across environments.

```bash
ansible-playbook playbooks/dsom/init-brain.yml
```

--------------------------------

### AI Implementation Prompt for Mintlify Sync

Source: https://gitlab.com/linuxmalaysia/deep-state-of-mind-for-my-ai/-/blob/main/docs/governance/MINTLIFY-ONE-WAY-SYNC-PIPELINE.md

Use this prompt with an AI coding assistant to generate the necessary Python scripts, GitHub Actions workflows, and safety governance rules for a one-way documentation sync pipeline.

```text
Set up an automated Markdown-to-MDX compiler and a hardened one-way docs sync pipeline between two GitHub repositories with 5 strict safety guards so an empty or partial source can NEVER destroy the downstream Mintlify docs repo.

REPOS & CONFIGURATION
- App repo (source of truth, where we work): <OWNER>/<APP_REPO>
- Docs repo (Mintlify deployment, downstream): <OWNER>/<DOCS_REPO>
- Mintlify live URL / subdomain:               <SUBDOMAIN>.mintlify.site
- Branch on both repos:                        main
- Native source folder in app repo:            docs/
- Compiled target folder in app repo:          docs-source/
- Target in downstream docs repo:              repo root

GOAL
1. Treat docs/ (standard Markdown .md) as the single source of truth.
2. Automatically compile docs/ into Mintlify MDX format under docs-source/ (with valid frontmatter and auto-generated docs.json).
3. Safely sync docs-source/ into <OWNER>/<DOCS_REPO> root on push to main or manual dispatch, protected by 5 non-negotiable safety guards (Guards A–E).

DELIVERABLES TO CREATE

1. tools/build_mintlify_mdx.py:
   - Traverses docs/ and skills/ to discover all .md files.
   - Extracts OKF / standard YAML frontmatter (title, description, topics).
   - Generates corresponding .mdx files in docs-source/.
   - Dynamically builds docs-source/docs.json with proper navigation tabs and groups matching the project structure.

2. Seed docs-source/:
   - Run tools/build_mintlify_mdx.py once to populate docs-source/ with all initial .mdx files and docs.json.

3. scripts/sync_docs.py:
   Implement the 5 strict safety guards. Any failed guard must exit non-zero BEFORE touching the downstream repo:
   - Guard A (Source & JSON Integrity):
     Fail if docs-source/ does not exist or docs-source/docs.json is missing/invalid JSON.
   - Guard B (Minimum File Count Floor):
     Count .mdx files under docs-source/. Fail if count is below MIN_MDX_FILES (default 5).
   - Guard C (Navigation Integrity):
     Walk docs.json navigation. Assert that every referenced page has a matching docs-source/<path>.mdx file. Fail if any are missing.
   - Guard D (Diff Preview & Deletion Cap):
     Clone downstream repo into a temp directory using DOCS_REPO_TOKEN. Compute file diff (added/modified/deleted). Fail if deleted files exceed MAX_DELETIONS (default 10) unless ALLOW_LARGE_DELETIONS=true is set.
   - Guard E (Dry-Run Mode):
     Support --dry-run flag and DRY_RUN=true env var. Preview the plan without committing or pushing.

   Sync execution (only after all guards pass):
   - Wipe downstream working tree (preserve .git).
   - Copy docs-source/ into it.
   - git config user.name "Docs Sync Bot" and user.email "bot@<SUBDOMAIN>.mintlify.site".
   - Stage changes (git add -A). If clean, exit 0.
   - Commit: "Sync docs from app repo @ <short-sha>".
   - Push to main. Never force-push.

4. .github/workflows/sync-docs.yml:
   - Triggers on push to main touching docs/**, docs-source/**, tools/build_mintlify_mdx.py, or scripts/sync_docs.py.
   - Exposes workflow_dispatch with inputs: dry_run (default true), allow_large_deletions (default false), min_mdx_files (default "5"), max_deletions (default "10").
   - Runs on ubuntu-latest, Python 3.11+.
   - Step 1: Run `python tools/build_mintlify_mdx.py`.
   - Step 2: Run `python scripts/sync_docs.py`.
   - Injects secrets.DOCS_REPO_TOKEN.

5. Rule & Governance Documentation:
   - Document Rule: "Mintlify One-Way Docs Sync & Safety Guards Mandate" in AGENTS.md.
   - In downstream repo README.md, state clearly: "Auto-synced from <APP_REPO>/docs-source/. Do NOT edit here or via Mintlify web editor."

6. Automated Tests:
   - tests/test_mintlify_mdx_builder.py: Validate frontmatter conversion, MD-to-MDX text generation, and docs.json assembly.
   - tests/test_sync_docs.py: Validate Guards A through E.

CONSTRAINTS & RULES
- One-way sync only. Never configure two-way sync.
- Use Fine-Grained Personal Access Token (PAT) with Read & Write access to <OWNER>/<DOCS_REPO> only.
- UTF-8 console encoding safe (no raw unhandled unicode errors on Windows/Linux).
- Never force-push.
```

--------------------------------

### CheckUsage Syntax Reference

Source: https://gitlab.com/linuxmalaysia/deep-state-of-mind-for-my-ai/-/blob/main/docs/tools/HOWTO-CHECKUSAGE.md

Displays the available command-line options and their default values.

```powershell
.\tools\CheckUsage.ps1 [options]

Options:
  -Loop    (-l)           Live monitor mode (repeats every IntervalSeconds)
  -IntervalSeconds (-i)   Refresh interval in seconds         [default: 60]
  -ThresholdMB     (-w)   MB size to trigger WARNING status   [default: 20]
  -CriticalMB      (-c)   MB size to trigger LIMIT RISK       [default: 50]
  -DormantHours    (-d)   Hours idle before DORMANT status    [default: 4]
  -Top             (-n)   Show only top N sessions            [default: all]
  -NoPulse                Suppress countdown bar in loop mode
  -Help            (-h)   Show the full help screen
```

--------------------------------

### Palace Onboarding Metadata

Source: https://gitlab.com/linuxmalaysia/deep-state-of-mind-for-my-ai/-/blob/main/llms-full.txt

Standardized YAML front-matter for OKF-compliant documentation files.

```yaml
---
okf_version: 0.1
type: documentation
title: "🏛️ HOWTO: Palace Onboarding — First-Time Guide for Users and AI Agents"
timestamp: "2026-07-04T09:40:04Z"
topics: ["dsom", "documentation"]
description: "OKF-compliant documentation for HOWTO-PALACE-ONBOARDING.md."
resource: "file:///docs/HOWTO-PALACE-ONBOARDING.md"
---
```

--------------------------------

### Generate portable context manifest

Source: https://gitlab.com/linuxmalaysia/deep-state-of-mind-for-my-ai/-/blob/main/docs-source/ai-setup/copilot.mdx

Run this command to prepare brain artifacts for a provider transition.

```bash
bash tools/reanimate.sh
```

--------------------------------

### Execute Daily Mechanical Scripts

Source: https://gitlab.com/linuxmalaysia/deep-state-of-mind-for-my-ai/-/blob/main/docs-source/skills/sod-palace-sync.mdx

Run the appropriate environment initialization script based on the operating system.

```powershell
# On Windows
.\tools\sod-palace.ps1
```

```bash
# On Linux/macOS
bash tools/sod-palace.sh
```

--------------------------------

### Create GitHub and GitLab Releases

Source: https://gitlab.com/linuxmalaysia/deep-state-of-mind-for-my-ai/-/blob/main/docs/tutorials/TEAM-DSOM-MASTERCLASS.md

Commands to automate release creation on both platforms using CLI tools.

```bash
gh release create vX.Y.Z -F release_notes.md --title "vX.Y.Z: Summary"
glab release create vX.Y.Z --name "vX.Y.Z: Summary" --notes-file release_notes.md
```

--------------------------------

### Visualize the Mintlify One-Way Sync Pipeline

Source: https://gitlab.com/linuxmalaysia/deep-state-of-mind-for-my-ai/-/blob/main/docs/governance/MINTLIFY-ONE-WAY-SYNC-PIPELINE.md

A text-based diagram illustrating the flow from the sovereign source of truth through compilation and synchronization to the final public portal.

```text
┌────────────────────────────────────────────────────────────┐
│ Sovereign Source of Truth: docs/ & .agents/skills/         │
└───────────────────────────┬────────────────────────────────┘
                            │ (python tools/build_mintlify_mdx.py)
                            ▼
┌────────────────────────────────────────────────────────────┐
│ Intermediate Compiled Tree: docs-source/ + docs.json       │
└───────────────────────────┬────────────────────────────────┘
                            │ (python scripts/sync_docs.py)
                            ▼ (5 Non-Negotiable Safety Guards A–E)
┌────────────────────────────────────────────────────────────┐
│ Downstream Deployment: linuxmalaysia/my-knowledge-brain    │
└───────────────────────────┬────────────────────────────────┘
                            │ (Mintlify Auto-Build)
                            ▼
┌────────────────────────────────────────────────────────────┐
│ Public Portal: https://harisfazillah.mintlify.site         │
└───────────────────────────┬────────────────────────────────┘
```

--------------------------------

### Execute init-brain script

Source: https://gitlab.com/linuxmalaysia/deep-state-of-mind-for-my-ai/-/blob/main/docs-source/tools/init-brain.mdx

Run the initialization script appropriate for your operating system to scaffold the brain directory.

```bash
bash tools/init-brain.sh
chmod +x tools/*.sh
```

```powershell
.\tools\init-brain.ps1
```

--------------------------------

### Initialize Brain Structure

Source: https://gitlab.com/linuxmalaysia/deep-state-of-mind-for-my-ai/-/blob/main/docs/HOWTO-ADOPT-DSOM.md

Creates the necessary directory structure for the DSOM brain.

```bash
bash tools/init-brain.sh
```

--------------------------------

### Create ansible.cfg

Source: https://gitlab.com/linuxmalaysia/deep-state-of-mind-for-my-ai/-/blob/main/docs/HOWTO-SETUP-ANSIBLE-BASELINE.md

Generates the project-level Ansible configuration file with standard DSOM settings.

```bash
cat > ansible.cfg << 'EOF'
[defaults]
inventory           = inventory/hosts.yml
roles_path          = roles
retry_files_enabled = False
host_key_checking   = False
stdout_callback     = yaml
callbacks_enabled   = profile_tasks
log_path            = .logs/ansible.log

[privilege_escalation]
become              = False
become_method       = sudo
become_user         = root

[ssh_connection]
ssh_args            = -o ControlMaster=auto -o ControlPersist=60s
pipelining          = True
EOF

```

--------------------------------

### Execute bench_brain.py via CLI

Source: https://gitlab.com/linuxmalaysia/deep-state-of-mind-for-my-ai/-/blob/main/docs/reference/bench_brain.md

Run the benchmarking utility using the uv package manager.

```bash
uv run python tools/bench_brain.py
```

--------------------------------

### Execute DSOM State Sync via Terminal

Source: https://gitlab.com/linuxmalaysia/deep-state-of-mind-for-my-ai/-/blob/main/docs-source/skills/dsom-state-sync.mdx

Run the compaction script using uv, specifying the diff file and the target state file. Ensure the environment is configured with necessary API keys if using LLM-based compaction.

```bash
uv run --with requests==2.34.2 --with pyyaml==6.0.3 .github/scripts/action_update_dsom.py <diff_file> .agents/brain/current_state.dsom
```

--------------------------------

### Configure IDE Rule Files

Source: https://gitlab.com/linuxmalaysia/deep-state-of-mind-for-my-ai/-/blob/main/docs/governance/MULTI-AGENT-PROTOCOLS.md

Add these instructions to .cursorrules or .windsurfrules to ensure IDE-embedded agents adhere to DSOM protocols.

```text

# .cursorrules / .windsurfrules

YOU ARE A DSOM-COMPLIANT AGENT.
BEFORE ANSWERING, READ: docs/AI-MASTER-PROTOCOL.md
FOLLOW: docs/PERSONALIZATION.md
CRITICAL: DO NOT CREATE GLOBAL VARIABLES. ADHERE TO CLEAN ARCHITECTURE.

```

--------------------------------

### Execute Start-of-Day (SOD) Sync

Source: https://gitlab.com/linuxmalaysia/deep-state-of-mind-for-my-ai/-/blob/main/docs/tools/HOWTO-GIT-RITUAL.md

Fetches from origin and performs a rebase pull to ensure the local branch is up to date.

```powershell
.\tools\git-ritual.ps1 sod
```

```bash
bash tools/git-ritual.sh sod
```

--------------------------------

### Sync OKF bundle changes to database

Source: https://gitlab.com/linuxmalaysia/deep-state-of-mind-for-my-ai/-/blob/main/references/Optimizing OKF for AI Agents - Deep Research - 20260704.md

Pushes refined schema descriptions from the OKF bundle back into the host database.

```bash
okf-sqlite ingest --db ./app.db --bundle ./catalog --sync
```

--------------------------------

### Troubleshooting Commands

Source: https://gitlab.com/linuxmalaysia/deep-state-of-mind-for-my-ai/-/blob/main/docs/HOWTO-SETUP-WSL-ALMALINUX10.md

Essential commands for resolving common issues like missing dependencies.

```bash
pip3 install ansible
```

```bash
wsl --terminate dsom-control-almalinux10
```

--------------------------------

### Execute template-reset on Linux/WSL2

Source: https://gitlab.com/linuxmalaysia/deep-state-of-mind-for-my-ai/-/blob/main/docs/tools/HOWTO-TEMPLATE-RESET.md

Run the shell script to initiate the project reset process on Linux or WSL2 environments.

```bash

# WSL2 / Linux (T2)

bash tools/template-reset.sh

```

--------------------------------

### Configure Cursor Rules

Source: https://gitlab.com/linuxmalaysia/deep-state-of-mind-for-my-ai/-/blob/main/docs-source/governance/multi-agent.mdx

Set up the .cursorrules file to enforce DSOM master protocol and clean architecture constraints.

```text
# .cursorrules

YOU ARE A DSOM-COMPLIANT AGENT.
BEFORE ANSWERING, READ: docs/AI-MASTER-PROTOCOL.md
FOLLOW: docs/PERSONALIZATION.md
CRITICAL: DO NOT CREATE GLOBAL VARIABLES. ADHERE TO CLEAN ARCHITECTURE.
```

--------------------------------

### Security Hardening

Source: https://gitlab.com/linuxmalaysia/deep-state-of-mind-for-my-ai/-/blob/main/docs/HOWTO-ADOPT-DSOM.md

Run the privacy tool and configure gitignore to include brain files.

```bash
bash tools/privacy-guardian.sh
```

```gitignore
!/.agents/brain/*.md
```

--------------------------------

### Run Live Monitor Mode

Source: https://gitlab.com/linuxmalaysia/deep-state-of-mind-for-my-ai/-/blob/main/docs-source/tools/howto-checkusage-linux.mdx

Executes the script in a loop to refresh metrics every 60 seconds.

```bash
bash tools/check-usage.sh --loop
```

--------------------------------

### Create Pre-flight Verification Playbook

Source: https://gitlab.com/linuxmalaysia/deep-state-of-mind-for-my-ai/-/blob/main/docs/HOWTO-SETUP-ANSIBLE-BASELINE.md

Creates a YAML playbook to verify node connectivity, Python availability, and project path status. This must pass before executing other playbooks.

```bash
cat > playbooks/preflight.yml << 'EOF'
---

# DSOM Pre-flight Verification Playbook
# Purpose: Verify connectivity and baseline state before any deployment.

# Usage:   ansible-playbook playbooks/preflight.yml
# Rule:    This playbook MUST pass before any other playbook is run.

- name: "DSOM Pre-flight Verification"
  hosts: all
  gather_facts: true
  become: false

  tasks:
    - name: "Verify node connectivity and Python availability"
      ansible.builtin.ping:
      register: ping_result

    - name: "Display node OS information"
      ansible.builtin.debug:
        msg: >
          Node: {{ inventory_hostname }} |
          OS: {{ ansible_distribution }} {{ ansible_distribution_version }} |
          Kernel: {{ ansible_kernel }} |
          Python: {{ ansible_python_version }}

    - name: "Verify deploy user identity"
      ansible.builtin.command: "id"
      register: id_output
      changed_when: false

    - name: "Show deploy user identity"
      ansible.builtin.debug:
        msg: "Identity on {{ inventory_hostname }}: {{ id_output.stdout }}"

    - name: "Verify project path exists (if already deployed)"
      ansible.builtin.stat:
        path: "{{ project_path }}"
      register: project_dir

    - name: "Report project path status"
      ansible.builtin.debug:
        msg: >
          Project path '{{ project_path }}' on {{ inventory_hostname }}:
          {{ 'EXISTS' if project_dir.stat.exists else 'NOT YET CREATED (expected for fresh deploy)' }}
EOF
```

--------------------------------

### Execute Standard Reanimation

Source: https://gitlab.com/linuxmalaysia/deep-state-of-mind-for-my-ai/-/blob/main/docs/tools/HOWTO-REANIMATE.md

Run the reanimate tool to generate a manifest file based on existing project artifacts.

```powershell

# Windows (T1)

.\tools\reanimate.ps1

```

```bash

# WSL2 / Linux (T2)

bash tools/reanimate.sh

```

--------------------------------

### OpenWiki Directory Structure

Source: https://gitlab.com/linuxmalaysia/deep-state-of-mind-for-my-ai/-/blob/main/openwiki/INSTRUCTIONS.md

The physical layout of the OpenWiki project initialized by the emulator.

```text
openwiki/
├── .last-update.json        # Dynamic metadata tracker (updated timestamps, status, count)
├── INSTRUCTIONS.md          # This instruction document (automatically regenerated)
├── _skeleton.md             # Document inventory, system ranking, and evidence logs
├── graph.html               # Interactive HTML visualizer for the knowledge graph
├── quickstart.md            # Entrypoint navigation and task routing table
├── architecture/
│   └── overview.md          # DSOM scope and the three-pillar operating model
├── governance/
│   └── agent-operation.md   # Dual agent registries and the 27 operational laws
├── memory/
│   └── session-and-palace.md# Session state lifecycle and spatial Palace index
├── automation/
│   ├── ansible-baseline.md  # Inventory tiers and Ansible playbooks
│   └── tools-and-privacy.md # Idempotent bash/powershell wrappers and Privacy Guardian
├── integrations/
│   └── mcp-and-ci.md        # FastMCP server integration and CI pipelines
├── publishing/
│   └── documentation-delivery.md # Multi-channel documentation pipelines (MkDocs, GitBook, RTD)
└── quality/
    └── verification.md      # Regression tests, BOM filters, and cross-platform guardrails
```

--------------------------------

### Execute the AI Model Transition Sequence

Source: https://gitlab.com/linuxmalaysia/deep-state-of-mind-for-my-ai/-/blob/main/docs/RITUAL-OF-TRANSITION.md

A manual sequence for transferring context between AI sessions using Hibernation Notes.

```text
Step 1: In the outgoing AI session — run EOD Hibernation Notes Export (Prompt 5)
Step 2: Copy the full Hibernation Notes code block
Step 3: Open the new AI session
Step 4: Paste Prompt 3 (Cognitive Twin Handover) + Hibernation Notes block
Step 5: AI confirms Mental Anchor, T-tier map, sovereign laws, top 3 tasks
Step 6: AI states: "Cognitive Twin Transfer Complete — [PROJECT NAME] Handover Successful."
```

--------------------------------

### Discovery Flow Visualization

Source: https://gitlab.com/linuxmalaysia/deep-state-of-mind-for-my-ai/-/blob/main/docs/governance/SOP-KNOWLEDGE-FIRST-DISCOVERY.md

A visual representation of the 5-step discovery process from user request to terminal execution.

```text
[ Step 1: User Request ]
         │
         ▼
[ Step 2: Local OKF Search ] ──▶ grep_search on .agents/brain/ & docs/ (topics: / description:)
         │
         ▼
[ Step 3: Local Context Inspection ] ──▶ view_file line ranges on targeted .md files
         │
         ▼
[ Step 4: Temporal Verification Gate ] ──▶ Check OKF timestamp. If old, research & prompt human for decision.
         │
         ▼
[ Step 5: Terminal Execution ] (ONLY if live runtime state or deployment change is needed)
```

--------------------------------

### Make the Skill OKF-Compliant

Source: https://gitlab.com/linuxmalaysia/deep-state-of-mind-for-my-ai/-/blob/main/docs-source/skills/creating-skills.mdx

Upgrade the frontmatter to the OKF v0.1 specification to enable semantic routing and indexing.

```yaml
---
okf_version: 0.1
type: skill
title: "My Custom Skill"
timestamp: "2026-07-15T10:00:00Z"
topics: ["haproxy", "deploy", "infrastructure", "ansible"]
description: "Use this skill when the user asks to deploy HAProxy on a new node or reconfigure an existing load balancer."
name: my-custom-skill
---
```

--------------------------------

### Scaffold the Plugin Package Directory

Source: https://gitlab.com/linuxmalaysia/deep-state-of-mind-for-my-ai/-/blob/main/docs-source/skills/agent-plugin-packager.mdx

Initializes the directory structure for a new plugin package using either PowerShell or Bash.

```powershell
# PowerShell
$PluginName = "dsom-sysops-plugin"
mkdir $PluginName
mkdir $PluginName/skills
```

```bash
# Bash
export PLUGIN_NAME="dsom-sysops-plugin"
mkdir -p "$PLUGIN_NAME/skills"
```

--------------------------------

### Closet Template

Source: https://gitlab.com/linuxmalaysia/deep-state-of-mind-for-my-ai/-/blob/main/docs/HOWTO-MIGRATE-TO-PALACE.md

Template for creating closet.md files within each Room.

```markdown

# 🚪 Closet: Room [NAME] ([Short Description])

Brief description of what this Room stores.

---

## [Section Heading]

Your distilled knowledge here.

## 📅 Evolution Timeline

| Date | What Changed |
|---|---|
| YYYY-MM-DD | Event |

---

## 🔗 Retrieval Reference

- [walkthrough.md](../../../walkthrough.md)

---
*Last Refined: YYYY-MM-DD | Hall: hall_X | Wing: wing_Y*
```

--------------------------------

### DSOM Meta-Prompt Configuration

Source: https://gitlab.com/linuxmalaysia/deep-state-of-mind-for-my-ai/-/blob/main/docs/HOWTO-CREATE-DSOM-GEMINI-GEM.md

The core instruction block to be pasted into the Gemini Gem configuration instructions field to enable the DSOM framework.

```text

# DEEP STATE OF MIND (DSOM) PROTOCOL V1

You are operating under the DSOM Framework for Gemini. Your core functions are cognitive self-governance, continuous context logging, and token optimisation.

## 1. COGNITIVE LEDGER & STATE MANAGEMENT

- Before generating any complex response, perform an internal "Cognitive Check". Evaluate active project objectives and previous decisions.
- Maintain a highly structured memory map. If the user shares core rules, data, or project parameters, declare explicitly: "[DSOM MEMORY LOCKED] Saved to operational state."

## 2. REASONING & TOKEN OPTIMISATION

- Do not repeat long, unchanged text blocks or full scripts unless explicitly requested.
- Focus strictly on the "delta"—provide only updates, modifications, or immediate action steps.
- Use dense, concise language to reduce token bloat while maximizing technical accuracy.

## 3. EPISODIC RESUME PROTOCOL

- Because environment storage is temporary, you must generate an anchor point at the end of complex workflows.
- Conclude major milestones by generating a compact summary block strictly labeled: "[DSOM EPISODIC RECORD]". 
- Instruct the user to save this record so it can be pasted into the next chat session to restore your exact mental state instantly.

```

--------------------------------

### Consolidate Context Prompt

Source: https://gitlab.com/linuxmalaysia/deep-state-of-mind-for-my-ai/-/blob/main/docs-source/ai-setup/claude.mdx

Use this prompt at the end of a session to update task and walkthrough documentation files.

```text
We are ending the session. Update .agents/brain/task.md — mark completed tasks [x]
and set tomorrow's targets [ ]. Update .agents/brain/walkthrough.md with today's
Mental Anchor.
```

--------------------------------

### Ansible-First Execution Patterns

Source: https://gitlab.com/linuxmalaysia/deep-state-of-mind-for-my-ai/-/blob/main/docs-source/rituals/transition-ritual.mdx

Standard practices for OS-level changes, contrasting correct Ansible usage with prohibited manual or ad-hoc commands.

```bash
ansible-playbook playbooks/dsom/[name].yml -i inventory/hosts.yml
ansible-lint playbooks/dsom/[name].yml
--check
```

```bash
ssh root@node "systemctl restart service"
ansible -m shell -a 'cmd'
```

--------------------------------

### Verify server functionality

Source: https://gitlab.com/linuxmalaysia/deep-state-of-mind-for-my-ai/-/blob/main/docs/how-to/run-fastmcp-server.md

Executes the test suite to ensure resources and tools are correctly registered.

```bash
uv run --with pyyaml --with pytest --with mcp==1.2.1 --with fastmcp pytest tests/test_mcp_server.py
```

--------------------------------

### Verify brain initialization

Source: https://gitlab.com/linuxmalaysia/deep-state-of-mind-for-my-ai/-/blob/main/docs-source/tools/init-brain.mdx

Confirm the directory structure exists and run the audit tool to validate the Brain Check.

```bash
ls -la .agents/brain/
```

```bash
bash tools/audit-pre-flight.sh
```

--------------------------------

### Run Python Tests

Source: https://gitlab.com/linuxmalaysia/deep-state-of-mind-for-my-ai/-/blob/main/docs/tutorials/TEAM-DSOM-MASTERCLASS.md

Executes the test suite with required dependencies using uv.

```bash
uv run --with pytest --with pyyaml --with fastmcp pytest
```

--------------------------------

### Master Reanimation Prompt Template

Source: https://gitlab.com/linuxmalaysia/deep-state-of-mind-for-my-ai/-/blob/main/docs/REANIMATION-PROMPT-TEMPLATE.md

Use this prompt after uploading the sod_manifest_YYYY-MM-DD.txt file to initialize the DSOM Protocol and establish the Cognitive Twin partnership.

```text

System Initialisation: Initialise DSOM Protocol v6.1.

1. IDENTITY & PARTNERSHIP (The Cognitive Twin):
   Act as the Cognitive Digital Twin of [YOUR_NAME] (Senior Systems Architect).
   - Role: Peer Architect and Guardian of Continuity.
   - Advisory Mode ONLY: You propose, I approve, Ansible executes, you verify.
   - Mirror Law: Reflect my clarity. If my instructions lack architectural reasoning,
     do not guess — ask for the "Why".
   - Language: UK English strictly. DBP-standard Bahasa Melayu Malaysia (Piawai)
     when writing Malay.

2. SOVEREIGN LAWS (Non-Negotiable):
   - GitOps: If not in Git, it does not exist.
   - Ansible-Only: All OS-level changes via Ansible playbooks. Zero ad-hoc SSH.
   - Idempotency: Every playbook must be safe to re-run.
   - No Secrets in Git: ansible-vault only.
   - Atomic Git: One logical change per commit. Format: type(scope): description [Phase/vX]
   - Stop Condition: Refuse any request that violates these laws.

3. CONTEXT SYNC (read the uploaded manifest):
   - Review Section [14] Palace Registry. Walk the relevant Rooms for today's tasks.
   - Confirm the current Mental Anchor from .agents/brain/walkthrough.md
   - Confirm the 4-Tier environment map from docs/AI-COGNITIVE-TWIN-PROTOCOL.md
   - State the top 3 pending tasks from .agents/brain/task.md
   - Confirm the active phase from .agents/brain/implementation_plan.md

4. RESPONSE FORMAT (LDP-Standard):
   Every response must follow:
   1. Acknowledgement (English / Malay mix if appropriate)
   2. Procedure: Command → Code → Expected Outcome
   3. Pedagogical Logic: "### 🧠 Why" — the architectural reasoning
   4. Atomic Git Ritual: exact commit command when applicable
   5. Mental Anchor: one-sentence next step

5. THE HANDSHAKE:
   State: "Sovereign State Synchronised — [PROJECT NAME] is live and ready."

```

--------------------------------

### Create inventory/hosts.yml

Source: https://gitlab.com/linuxmalaysia/deep-state-of-mind-for-my-ai/-/blob/main/docs/HOWTO-SETUP-ANSIBLE-BASELINE.md

Creates the inventory template for defining node topology across development, staging, and production environments.

```yaml
cat > inventory/hosts.yml << 'EOF'
---

# DSOM Ansible Inventory Template
# Project: [YOUR_PROJECT_NAME]

# Last Updated: [DATE]
# IMPORTANT: This file must NOT contain secrets. Use ansible-vault for credentials.

all:
  vars:
    ansible_user: "[YOUR_DEPLOY_USER]"       # e.g., example-user, dsom-admin
    ansible_ssh_private_key_file: "~/.ssh/[YOUR_KEY_NAME]"
    ansible_python_interpreter: "/usr/bin/python3"

  children:
    # ── Tier 2: Development / Dev Bridge ──────────────────────────────
    dev:
      hosts:
        dev-node-01:
          ansible_host: "[DEV_IP_OR_HOSTNAME]"   # e.g., 10.10.10.11
          ansible_user: "[DEV_USER]"              # e.g., example-user

    # ── Tier 3: Staging / UAT ─────────────────────────────────────────
    staging:
      hosts:
        staging-node-01:
          ansible_host: "[STAGING_IP_OR_HOSTNAME]"

    # ── Tier 4: Production ────────────────────────────────────────────
    production:
      hosts:
        prod-node-01:
          ansible_host: "[PROD_IP_OR_HOSTNAME]"
          ansible_user: "[PROD_USER]"             # e.g., dsom-admin
EOF

```

--------------------------------

### Run sitemap compilation

Source: https://gitlab.com/linuxmalaysia/deep-state-of-mind-for-my-ai/-/blob/main/docs/how-to/generate-sitemaps-seo.md

Executes the sitemap generation script using uv and mkdocs-material dependencies.

```bash
uv run --with mkdocs-material python tools/generate_sitemaps.py
```

--------------------------------

### Promote Project to T3

Source: https://gitlab.com/linuxmalaysia/deep-state-of-mind-for-my-ai/-/blob/main/docs/HUMAN-HANDOVER-CONTEXT.md

Use rsync to deploy the project files from T2 to the T3 jump host, excluding sensitive directories.

```bash
# Run from T2 WSL2 — promote project to T3 orchestrator

rsync -avz --checksum --delete \
    --exclude '.git' --exclude '.gemini' --exclude '.agents' \
    ./ dsom-admin@[T3_IP]:/opt/[YOUR_PROJECT_NAME]/
```

--------------------------------

### Validating Frontmatter with OpenWiki Emulator

Source: https://gitlab.com/linuxmalaysia/deep-state-of-mind-for-my-ai/-/blob/main/docs-source/governance/okf-standard.mdx

Use these commands to verify indexing and rebuild the knowledge graph after adding frontmatter.

```bash
# Search for your document by a topic you added
uv run --with pyyaml python tools/openwiki_emulator.py --search "your-topic"

# Rebuild the full knowledge graph to include new files
uv run --with pyyaml python tools/openwiki_emulator.py --init
```

```powershell
# Search for your document by a topic you added
uv run --with pyyaml python tools/openwiki_emulator.py --search "your-topic"

# Rebuild the full knowledge graph to include new files
uv run --with pyyaml python tools/openwiki_emulator.py --init
```

--------------------------------

### Manual Sync Alternative for Windows

Source: https://gitlab.com/linuxmalaysia/deep-state-of-mind-for-my-ai/-/blob/main/docs/governance/GITOPS-AIOPS-ANSIBLE-STRATEGY.md

Alternative PowerShell scripts for manual synchronization on Windows environments.

```powershell
.\tools\reanimate.ps1   # SOD
.\tools\hibernation.ps1 # EOD (includes palace-sync v2.1)
```

--------------------------------

### Daily Loop Workflow Overview

Source: https://gitlab.com/linuxmalaysia/deep-state-of-mind-for-my-ai/-/blob/main/docs/governance/AI-COGNITIVE-TWIN-PROTOCOL.md

A visual representation of the SOD, Active Work, and EOD workflow steps.

```text

SOD
 └── ansible-playbook sod-palace.yml    (T2 auto)
       └── Upload manifest + handshake       (Human manual)
             └── AI reads Palace Registry    (AI auto)

Active Work
 └── Code, commit, push                 (Human)
 └── Log key decisions immediately      (Human+AI — Decision Log Protocol)

EOD
 └── ansible-playbook eod-palace.yml    (T2 auto)
       └── Review palace_update_proposal     (Human+AI)
             └── Update closets + commit     (Human+AI)

```

--------------------------------

### Verify Prerequisites

Source: https://gitlab.com/linuxmalaysia/deep-state-of-mind-for-my-ai/-/blob/main/docs/reference-architectures/HOWTO-SETUP-ANSIBLE-BASELINE.md

Commands to verify the required versions of Python, pip, Ansible, SSH, and Git on the deployment bridge.

```bash
python3 --version
```

```bash
pip3 --version
```

```bash
ansible --version
```

```bash
ssh -V
```

```bash
git --version
```

--------------------------------

### Attach Brain Artifacts with #file:

Source: https://gitlab.com/linuxmalaysia/deep-state-of-mind-for-my-ai/-/blob/main/docs-source/ai-setup/copilot.mdx

Use #file: references to provide Copilot with specific session-scoped context from task and walkthrough files.

```text
Based on #file:.agents/brain/task.md and #file:.agents/brain/walkthrough.md,
what is the next logical step for the current implementation phase?
```

--------------------------------

### Execute refactor_okf.py via CLI

Source: https://gitlab.com/linuxmalaysia/deep-state-of-mind-for-my-ai/-/blob/main/docs/reference/refactor_okf.md

Run the script to preview changes or apply standardizations to repository files.

```bash
# Preview expected changes without executing in-place writes

uv run python tools/refactor_okf.py --dry-run

# Refactor and overwrite files with standardised compliance

uv run python tools/refactor_okf.py
```

--------------------------------

### Inject OKF Frontmatter

Source: https://gitlab.com/linuxmalaysia/deep-state-of-mind-for-my-ai/-/blob/main/docs/OKF-ADOPTION-GUIDE.md

Run the compliance tool to inject frontmatter into documentation directories.

```bash
# Execute native Python OKF frontmatter injector
uv run python tools/apply_okf_frontmatter.py docs/

# Or invoke the agent skill script directly
python .agents/skills/okf-frontmatter-injector/scripts/apply_okf.py .
```

--------------------------------

### Execute Sovereign Handover Prompt

Source: https://gitlab.com/linuxmalaysia/deep-state-of-mind-for-my-ai/-/blob/main/docs-source/rituals/session-handover.mdx

Use this prompt in the outgoing AI session to generate a full memory dump of the current context.

```text
[DSOM COGNITIVE TWIN HANDOVER — v6.1]

I am migrating from a previous AI session to this one. I will provide you with
the full exported context (Hibernation Notes) from my last session.

You are now the Cognitive Digital Twin of [YOUR_NAME] (Senior Systems Architect).
Read the context below and adopt it fully as your working memory.

--- BEGIN CONTEXT EXPORT ---
[PASTE HIBERNATION NOTES FROM PREVIOUS SESSION]
--- END CONTEXT EXPORT ---

After reading, confirm:
1. The last Mental Anchor — exact point where we stopped.
2. The 4-Tier environment map (T1: Windows, T2: WSL2 Dev Bridge,
   T3: Jump Host / Ansible Control, T4: Production fabric).
3. The project identity and sovereign identity (your configured admin user and UID).
4. The top 3 pending tasks.
5. The Palace Rooms mapping for the current work.
6. All sovereign laws are loaded: GitOps, Ansible-only, Advisory Mode.

Then read these documents for full context (the AI will access them via the repo):
- docs/AI-MASTER-PROTOCOL.md       — Governance laws
- docs/AI-COGNITIVE-TWIN-PROTOCOL.md — Project Identity Card
- .agents/brain/palace_registry.md  — Palace Spatial Map
- .agents/brain/task.md             — Current tasks
- .agents/brain/walkthrough.md      — Session history
- docs/HUMAN-HANDOVER-CONTEXT.md   — Environment map

State: "Cognitive Twin Transfer Complete — [PROJECT NAME] Handover Successful."
```

--------------------------------

### Execute OpenWiki Emulator CLI commands

Source: https://gitlab.com/linuxmalaysia/deep-state-of-mind-for-my-ai/-/blob/main/docs/reference/openwiki_emulator.md

Common CLI operations for managing the OpenWiki workspace, including initialization, updates, searching, and graph exportation.

```bash
# Initialise and compile the full OpenWiki workspace (default action)

uv run python tools/openwiki_emulator.py --init

# Retrieve recent Git changes and update context history

uv run python tools/openwiki_emulator.py --update

# Perform search against OpenWiki YAML metadata

uv run python tools/openwiki_emulator.py --search "ansible"

# Export a standalone offline HTML interactive knowledge graph

uv run python tools/openwiki_emulator.py --export-graph
```

--------------------------------

### AI Prompt Template for OpenWiki Emulator

Source: https://gitlab.com/linuxmalaysia/deep-state-of-mind-for-my-ai/-/blob/main/docs/tools/HOWTO-OPENWIKI.md

A structured prompt template for instructing AI models to build a custom OpenWiki emulator script tailored to specific repository requirements.

```text
================================================================================
AI PROMPT TEMPLATE: BUILD A NATIVE PYTHON OPENWIKI EMULATOR SCRIPT
================================================================================

"You are a Senior Systems Architect. I want you to build a native Python
OpenWiki Emulator script for our repository that runs via `uv run`.

Requirements:
1. Create a script at `tools/openwiki_emulator.py` with inline `uv` metadata (`pyyaml>=6.0`).
2. Analyze our repository's directory topology, key configuration files (README.md, AGENTS.md, etc.), and test suites.
3. Automatically generate and maintain the `./openwiki/` directory structure:
   - `openwiki/_skeleton.md` (Inventory ranking, planned page tree, evidence briefs).
   - `openwiki/quickstart.md` (Topology map and task-routing table).
   - Subsystem folders for architecture, governance, memory, automation, integrations, publishing, quality.
   - `openwiki/.last-update.json` (Containing ISO timestamp and compilation status).
4. Include CLI arguments: `--init`, `--update`, `--search "<query>"`, and `--export-graph`.
5. All generated markdown files MUST include OKF v0.1 YAML frontmatter (okf_version, type, title, timestamp, topics, description).
6. The script MUST run non-interactively without Node.js binaries, npm packages, or external API keys."
================================================================================
```

--------------------------------

### Inject Repository Links

Source: https://gitlab.com/linuxmalaysia/deep-state-of-mind-for-my-ai/-/blob/main/docs-source/skills/publish-to-blogger.mdx

Appends the open-source repository and documentation links to the end of the post.

```html
<div style="background-color: #f1f8ff; padding: 15px; border-radius: 5px; margin-bottom: 20px; border: 1px solid #c8e1ff;">
    <h4 style="margin-top: 0;">🔗 Open Source Repositories & Documentation</h4>
    <p style="margin-bottom: 0;">Explore the full Deep State of Mind (DSOM) framework and Sovereign Palace architecture at:</p>
    <ul style="margin-bottom: 0;">
        <li><strong>GitHub:</strong> <a href="https://github.com/linuxmalaysia/deep-state-of-mind-for-my-ai" target="_blank">linuxmalaysia/deep-state-of-mind-for-my-ai</a></li>
        <li><strong>GitLab:</strong> <a href="https://gitlab.com/linuxmalaysia/deep-state-of-mind-for-my-ai" target="_blank">linuxmalaysia/deep-state-of-mind-for-my-ai</a></li>
        <li><strong>GitHub Pages:</strong> <a href="https://linuxmalaysia.github.io/deep-state-of-mind-for-my-ai/" target="_blank">DSOM Protocol Documentation</a></li>
        <li><strong>GitBook:</strong> <a href="https://malaysia-open-source-community.gitbook.io/deep-state-of-mind-dsom-protocol-for-my-ai" target="_blank">DSOM GitBook Documentation</a></li>
    </ul>
</div>
```

--------------------------------

### Automated AI Scaffolding Prompt

Source: https://gitlab.com/linuxmalaysia/deep-state-of-mind-for-my-ai/-/blob/main/docs/tutorials/TEAM-DSOM-MASTERCLASS.md

A prompt for the Antigravity skill to automate the creation of the DSOM directory structure and configuration files.

```markdown
Use the `dsom-project-cloner` skill to scaffold a brand-new DSOM downstream project for me.

Target Repository Path: D:\Projects\my-awesome-service

Please:
1. Create the required directory structure (.agents/brain, .agents/skills, docs/governance, tools/).
2. Copy the Universal Gateway Matrix (.cursorrules, CLAUDE.md, .github/copilot-instructions.md, AGENTS.md, plugin.json, mcp.json).
3. Copy the Constitutional Rulebook (.agents/AGENTS.md) and reset .agents/brain/ with blank OKF templates.
4. Copy the tools/ directory and automatically execute `python tools/install_git_guardrails.py` in the new target path.
5. Ensure all copied markdown files carry valid OKF v0.2 frontmatter and standard DSOM signatures.
```

--------------------------------

### Configure Claude Project Instructions

Source: https://gitlab.com/linuxmalaysia/deep-state-of-mind-for-my-ai/-/blob/main/docs/CLAUDE-SETUP.md

Verbatim instructions to be pasted into Claude Project Settings to define persona and operating constraints.

```text
[DSOM SOVEREIGN INSTRUCTIONS v6.1 + Palace v1.0]

You are the Claude-variant of the DSOM Cognitive Digital Twin. You are a Senior Systems Architect assisting Harisfazillah Jamel (LinuxMalaysia), who has 35+ years of ICT and Open Source expertise.

OPERATING MODEL:
- Advisory Mode ONLY. You propose. The human approves. Ansible executes. You verify.
- UK English in all responses and documentation.
- Atomic Git Hygiene: Propose changes one file at a time using 'type(scope): description [Phase/vX.X]' format.
- Ansible is the ONLY executor for OS-level tasks on remote nodes.
- Wait for human output after each step before proceeding.

DSOM LAWS (non-negotiable):
1. Zero-Global Pattern — No global variables; strict state management only.
2. Sovereign Portability — No vendor lock-in; code must be Linux-agnostic.
3. HA-Ready — Design for clusters and zero-downtime.
4. Atomic Git Hygiene — One logical change per commit.
5. Pedagogical Logic — Explain the 'Why' (logic/security/performance) before the 'What' (code).
6. GitOps Rule — If it is not committed to Git, it does not exist.
7. Ansible-First Execution — No ad-hoc commands on target nodes. Playbooks only.

4-TIER ENVIRONMENT:
- T1 (Command Centre): Human workstation — Code editing, Git, brain artifact maintenance.
- T2 (Dev Bridge): WSL2 / local VM — Ansible dry-run, log analysis.
- T3 (Staging): Pre-production validation.
- T4 (Production): Zero-tolerance for ad-hoc changes. Ansible + GitOps only.

BRAIN SYNC:
Your Single Source of Truth (SSoT) is the uploaded manifest file (e.g. sod_manifest_YYYY-MM-DD.txt) in Project Knowledge.
Before proposing any action, you MUST read Section [14] Palace Registry and walk the relevant spatial Rooms.
Always refer to it for the current Mental Anchor, task list, and roadmap.

MIRROR LAW:
Challenge the human if their instructions lack architectural clarity. Ask for the missing 'Why' before acting. Silence is the only failure.

When ready, state: 'Sovereign State Synchronised — [PROJECT NAME] is live.'
```

--------------------------------

### Run Snapshot Usage Check

Source: https://gitlab.com/linuxmalaysia/deep-state-of-mind-for-my-ai/-/blob/main/docs-source/tools/howto-checkusage-linux.mdx

Executes the script once to display current session metrics.

```bash
bash tools/check-usage.sh
```

--------------------------------

### Produce OKF bundle from SQLite database

Source: https://gitlab.com/linuxmalaysia/deep-state-of-mind-for-my-ai/-/blob/main/references/Optimizing OKF for AI Agents - Deep Research - 20260704.md

Generates a compliant OKF bundle from a specified SQLite database file.

```bash
okf-sqlite produce --db ./app.db --out ./catalog --profile
```

--------------------------------

### Automated Push to Production

Source: https://gitlab.com/linuxmalaysia/deep-state-of-mind-for-my-ai/-/blob/main/docs-source/skills/mintlify-docs-compiler.mdx

Git commands to stage, commit, and push documentation updates to the main branch.

```bash
git add docs/ docs-source/ .agents/skills/
git commit -m "docs: update sovereign guides and regenerate mdx tree"
git push origin main
```

--------------------------------

### CLI Operations

Source: https://gitlab.com/linuxmalaysia/deep-state-of-mind-for-my-ai/-/blob/main/docs/reference/openwiki_emulator.md

The openwiki_emulator.py script supports several CLI flags to manage the documentation workspace.

```APIDOC
## CLI Operations

### Description
Commands to initialize, update, search, and export the OpenWiki documentation workspace.

### Usage
- `uv run python tools/openwiki_emulator.py --init`: Initialise and compile the full OpenWiki workspace.
- `uv run python tools/openwiki_emulator.py --update`: Retrieve recent Git changes and update context history.
- `uv run python tools/openwiki_emulator.py --search "query"`: Perform search against OpenWiki YAML metadata.
- `uv run python tools/openwiki_emulator.py --export-graph`: Export a standalone offline HTML interactive knowledge graph.
```

--------------------------------

### Configure GitHub Copilot Instructions

Source: https://gitlab.com/linuxmalaysia/deep-state-of-mind-for-my-ai/-/blob/main/docs-source/governance/multi-agent.mdx

Define DSOM compliance requirements within the .github/copilot-instructions.md file to ensure the agent adheres to Advisory Mode.

```text
# .github/copilot-instructions.md

YOU ARE A DSOM-COMPLIANT AGENT.
BEFORE ANSWERING, READ: docs/AI-MASTER-PROTOCOL.md
FOLLOW: docs/PERSONALIZATION.md
CRITICAL: Advisory Mode only. Do not execute commands on remote nodes.
```

--------------------------------

### Initialize Brain Artifacts

Source: https://gitlab.com/linuxmalaysia/deep-state-of-mind-for-my-ai/-/blob/main/docs/HOWTO-CLONE-DSOM-PROJECT.md

Create empty brain artifacts to prevent startup warnings.

```bash
mkdir -p .agents/brain
echo "# 📝 Task List" > .agents/brain/task.md
echo "# 🚶 Session Walkthrough" > .agents/brain/walkthrough.md
```

--------------------------------

### Restart and launch the WSL instance

Source: https://gitlab.com/linuxmalaysia/deep-state-of-mind-for-my-ai/-/blob/main/docs/HOWTO-SETUP-WSL-ALMALINUX10.md

Terminate and restart the WSL instance to apply configuration changes.

```powershell
# Restart the WSL instance

wsl --terminate dsom-control-almalinux10

# Launch (will auto-login as dsom-admin)

wsl -d dsom-control-almalinux10
```

--------------------------------

### Execute Template Reset Script

Source: https://gitlab.com/linuxmalaysia/deep-state-of-mind-for-my-ai/-/blob/main/docs/tools-and-automation/template-reset.md

Run the reset script from the project root to initiate the repository cleanup process.

```bash
./tools/template-reset.sh
```

--------------------------------

### Execute Checkpoint on Linux/WSL2

Source: https://gitlab.com/linuxmalaysia/deep-state-of-mind-for-my-ai/-/blob/main/docs/tools/HOWTO-CHECKPOINT.md

Run the shell script wrapper to perform a checkpoint on Linux or WSL2 environments.

```bash

# WSL2 / Linux (T2)

bash tools/checkpoint.sh

```

--------------------------------

### Configure Claude Desktop for MCP

Source: https://gitlab.com/linuxmalaysia/deep-state-of-mind-for-my-ai/-/blob/main/docs/how-to/run-fastmcp-server.md

JSON configuration block for connecting the IDE to the local MCP server.

```json
{
  "mcpServers": {
    "dsom-palace": {
      "command": "uv",
      "args": [
        "run",
        "--with", "mcp[cli]>=1.1.2",
        "--with", "fastmcp>=0.1.0",
        "--with", "pyyaml>=6.0",
        "tools/mcp/server.py"
      ],
      "env": {
        "DSOM_ROOT": "/absolute/path/to/deep-state-of-mind-for-my-ai"
      }
    }
  }
}
```

--------------------------------

### Write Execution Steps

Source: https://gitlab.com/linuxmalaysia/deep-state-of-mind-for-my-ai/-/blob/main/docs-source/skills/creating-skills.mdx

Define atomic, imperative steps for the AI to follow, including error recovery and verification.

```yaml
## Execution Steps

1. Run `ansible-playbook playbooks/deploy-haproxy.yml -i inventory/production`.
2. Verify the play recap shows zero failed tasks.
3. If any task fails, run `ansible-playbook playbooks/deploy-haproxy.yml -i inventory/production --start-at-task "FAILED TASK NAME"` to resume from the failure point.
4. SSH into the target node and run `systemctl status haproxy` to confirm the service is active and running.
5. Commit the updated inventory file: `git add inventory/ && git commit -m "feat(haproxy): deploy to production node"`.
```

--------------------------------

### Manual Start-of-Day (SOD) for Windows

Source: https://gitlab.com/linuxmalaysia/deep-state-of-mind-for-my-ai/-/blob/main/docs/governance/AI-COGNITIVE-TWIN-PROTOCOL.md

Manual equivalent commands for T1 (Windows) users to perform the SOD ritual.

```powershell
git pull --rebase origin main
.\tools\reanimate.ps1

# Then upload the manifest and say the handshake phrase

```

--------------------------------

### Define plugin.json

Source: https://gitlab.com/linuxmalaysia/deep-state-of-mind-for-my-ai/-/blob/main/docs/governance/DSOM-AGENT-PLUGINS-SPECIFICATION.md

Standard configuration file for identifying the plugin.

```json
{
  "$schema": "https://agent-plugins.org/schemas/1.0.0/plugin.schema.json",
  "name": "my-ops-plugin"
}
```

--------------------------------

### Perform Physical Reality Check

Source: https://gitlab.com/linuxmalaysia/deep-state-of-mind-for-my-ai/-/blob/main/docs-source/governance/operational-guide.mdx

Verifies the environment state before AI initialization.

```bash
# Linux / WSL2

./tools/audit-pre-flight.sh

# Windows

.\tools\audit-pre-flight.ps1
```

--------------------------------

### Bootstrap the control node manually

Source: https://gitlab.com/linuxmalaysia/deep-state-of-mind-for-my-ai/-/blob/main/docs/HOWTO-SETUP-WSL-ALMALINUX10.md

Manually trigger the bootstrap script inside the AlmaLinux 10 WSL instance.

```powershell
wsl -d dsom-control-almalinux10 -u root -- bash /mnt/d/Users/LinuxMalaysia/Projects/deep-state-of-mind-for-my-ai/tools/setup-dsom-control-node.sh
```

--------------------------------

### Safe Sync Ritual: Promote to T3

Source: https://gitlab.com/linuxmalaysia/deep-state-of-mind-for-my-ai/-/blob/main/docs-source/rituals/human-handover.mdx

Synchronizes project files from T2 to the T3 jump host using rsync.

```bash
# Run from T2 WSL2 — promote project to T3 orchestrator

rsync -avz --checksum --delete \
    --exclude '.git' --exclude '.gemini' --exclude '.agents' \
    ./ dsom-admin@[T3_IP]:/opt/[YOUR_PROJECT_NAME]/
```

--------------------------------

### Baseline .gitignore Template

Source: https://gitlab.com/linuxmalaysia/deep-state-of-mind-for-my-ai/-/blob/main/docs-source/skills/initialize-gitops.mdx

Use this template to prevent AI bloat by ignoring temporary Python files, environment variables, and system-generated scratch files.

```text
# Python
__pycache__/
*.pyc
.venv/
venv/

# Environment Variables
.env
.env.local

# AI Ephemeral Artifacts (DO NOT IGNORE THE ENTIRE .agents/ FOLDER)
# Only ignore temporary scratch files if they exist.
.agents/brain/scratch/
.agents/brain/.system_generated/
```

--------------------------------

### Generate SOD Manifest

Source: https://gitlab.com/linuxmalaysia/deep-state-of-mind-for-my-ai/-/blob/main/llms-full.txt

Execute the manifest generation script to prepare the environment for autonomous agents.

```bash
reanimate.sh
bash tools/sod-palace.sh  # (Windows: .\tools\sod-palace.ps1)
```

--------------------------------

### Execute Reanimation Engine

Source: https://gitlab.com/linuxmalaysia/deep-state-of-mind-for-my-ai/-/blob/main/docs/tools-and-automation/reanimate.md

Run the reanimate script to generate the project state manifest.

```bash
./tools/reanimate.sh
```

--------------------------------

### Locate Claude Desktop configuration file

Source: https://gitlab.com/linuxmalaysia/deep-state-of-mind-for-my-ai/-/blob/main/docs-source/ai-setup/mcp-server.mdx

Paths to the configuration file for different operating systems.

```bash
~/Library/Application Support/Claude/claude_desktop_config.json
```

```bash
%APPDATA%\Claude\claude_desktop_config.json
```

```bash
~/.config/Claude/claude_desktop_config.json
```

--------------------------------

### View Palace Update Proposal

Source: https://gitlab.com/linuxmalaysia/deep-state-of-mind-for-my-ai/-/blob/main/docs-source/palace/onboarding.mdx

Displays the raw proposal file containing room assignments and commits for the current date.

```bash
cat .agents/brain/palace_update_proposal_$(date +%Y-%m-%d).md
```

--------------------------------

### AI Prompt Template for OpenWiki Emulator

Source: https://gitlab.com/linuxmalaysia/deep-state-of-mind-for-my-ai/-/blob/main/docs/governance/OPENWIKI-INTEGRATION-GUIDE.md

A prompt template designed for AI assistants to generate a custom OpenWiki emulator script tailored to a specific repository.

```text
================================================================================
AI PROMPT TEMPLATE: BUILD A NATIVE PYTHON OPENWIKI EMULATOR SCRIPT
================================================================================

"You are a Senior Systems Architect. I want you to build a native Python
OpenWiki Emulator script for our repository that runs via `uv run`.

Requirements:
1. Create a script at `tools/openwiki_emulator.py` with inline `uv` metadata (`pyyaml>=6.0`).
2. Analyze our repository's directory topology, key configuration files (README.md, AGENTS.md, etc.), and test suites.
3. Automatically generate and maintain the `./openwiki/` directory structure:
   - `openwiki/_skeleton.md` (Inventory ranking, planned page tree, evidence briefs).
   - `openwiki/quickstart.md` (Topology map and task-routing table).
   - Subsystem folders for architecture, governance, memory, automation, integrations, publishing, quality.
   - `openwiki/.last-update.json` (Containing ISO timestamp and compilation status).
4. All generated markdown files MUST include OKF v0.1 YAML frontmatter (okf_version, type, title, timestamp, topics, description).
5. The script MUST run non-interactively without Node.js binaries, npm packages, or external API keys."
================================================================================
```

--------------------------------

### Inject DSOM Signatures

Source: https://gitlab.com/linuxmalaysia/deep-state-of-mind-for-my-ai/-/blob/main/docs-source/skills/dsom-bootstrap.mdx

Applies GPL v3.0 signature footers across all ported documentation.

```bash
python .agents/skills/dsom-signature-injector/scripts/inject.py .
```

--------------------------------

### Run Full History Backfill

Source: https://gitlab.com/linuxmalaysia/deep-state-of-mind-for-my-ai/-/blob/main/docs/tools/HOWTO-PALACE-SYNC.md

Scans the entire git history from the first commit. Required when onboarding an existing repository.

```powershell
.\tools\palace-sync.ps1 -Backfill
```

```bash
bash tools/palace-sync.sh --backfill
```

--------------------------------

### Troubleshoot Connectivity and Dependencies

Source: https://gitlab.com/linuxmalaysia/deep-state-of-mind-for-my-ai/-/blob/main/docs/reference-architectures/HOWTO-SETUP-ANSIBLE-BASELINE.md

Commands for resolving common SSH connectivity issues and missing Python interpreter errors on target nodes.

```bash
ssh-copy-id [USER]@[HOST]
```

```bash
sudo apt install -y python3
```

```bash
sudo dnf install -y python3
```

--------------------------------

### Configure Declarative Guardrails

Source: https://gitlab.com/linuxmalaysia/deep-state-of-mind-for-my-ai/-/blob/main/docs/governance/BYTE-CAPPED-EXECUTION-FRAMEWORK.md

The SKILL.md file defines the operational constraints and token limits for the procedural skill.

```markdown
---
okf_version: 0.1
type: procedural_skill
title: "Procedural Specification: DSOM Token Calculator"
description: "Calculates token counts via tiktoken in isolated uv Python runspaces."
resource: "file:///.agents/skills/dsom-token-calculator/SKILL.md"
timestamp: 2026-07-18T14:52:39Z
---

# Operational Enforcements:

- Trigger this skill dynamically before any output loop that handles extensive datasets or raw configurations.
- If payload calculations return totals greater than **4000 tokens**, the system must block raw screen serialization and switch to targeted `view_file` calls or chunked reading.
```

--------------------------------

### Compile Markdown to PowerPoint

Source: https://gitlab.com/linuxmalaysia/deep-state-of-mind-for-my-ai/-/blob/main/docs-source/skills/node-slide-generator.mdx

Execute the compiler script by providing the path to the source markdown file and the target output file.

```bash
node tools/compile_node_slides.js <input.md> <output.pptx>
```

```bash
node tools/compile_node_slides.js docs/slides_outline.md docs/Node_Migration_Presentation.pptx
```

--------------------------------

### Execute template-reset on Windows

Source: https://gitlab.com/linuxmalaysia/deep-state-of-mind-for-my-ai/-/blob/main/docs/tools/HOWTO-TEMPLATE-RESET.md

Run the PowerShell script to initiate the project reset process on Windows systems.

```powershell

# Windows (T1)

.\tools\template-reset.ps1

```

--------------------------------

### Repository Sync Architecture Diagram

Source: https://gitlab.com/linuxmalaysia/deep-state-of-mind-for-my-ai/-/blob/main/docs-source/README.md

Visual representation of the data flow from the source repository to the Mintlify deployment.

```text
linuxmalaysia/deep-state-of-mind-for-my-ai (app repo, source of truth)
  └── docs-source/                    ← edit here
       ├── *.mdx
       ├── docs.json
       ├── images/
       └── ...
  └── scripts/sync_docs.py
  └── .github/workflows/sync-docs.yml
  └── AGENTS.md

     ↓ (on push to main, paths: docs-source/**)
     ↓ (guards A–E must pass)

linuxmalaysia/my-knowledge-brain (this repo, auto-synced, DO NOT EDIT)
  └── *.mdx
  └── docs.json

     ↓ (Mintlify GitHub app watches main)

https://harisfazillah.mintlify.app
```

--------------------------------

### Define Session Anchor Format

Source: https://gitlab.com/linuxmalaysia/deep-state-of-mind-for-my-ai/-/blob/main/docs/EOD-RITUAL.md

Use this template in walkthrough.md to document session accomplishments, architectural reasoning, and the mental anchor for resuming work.

```markdown

## 🏁 Session Anchor: [YYYY-MM-DD] — [Short Phase Name]

### Accomplished

- [What was done — concrete, specific]
- [Second item if applicable]

### Why

- [The architectural reasoning — not just the what, but the why]

### Mental Anchor

> [ONE SENTENCE: exact state of the system right now. Where to resume in the next session.]

```

```markdown

## 🏁 Session Anchor: 2026-03-10 — AlmaLinux 10 WSL2 Setup

### Accomplished

- Created dsom-control-almalinux10 WSL2 setup scripts (setup-wsl-almalinux10.ps1, setup-dsom-control-node.sh).
- Created DSOM Ansible playbooks in playbooks/dsom/ (audit, init-brain, privacy-scan, site).

### Why

- T2 needs a dedicated Ansible Control Node (AlmaLinux 10) to avoid mixing Windows and Linux operations.
- Ansible playbooks make the tools/ bash scripts reusable on any node in the inventory.

### Mental Anchor

> WSL2 scripts created and pushed [v6.1.0]. Next: run setup-wsl-almalinux10.ps1 as Administrator, bootstrap
> dsom-admin, verify ansible localhost -m ping inside dsom-control-almalinux10.

```

--------------------------------

### Generate Reanimation Manifest

Source: https://gitlab.com/linuxmalaysia/deep-state-of-mind-for-my-ai/-/blob/main/docs/SOD-RITUAL.md

Create a comprehensive context manifest for AI upload, containing project identity, state, and registry information.

```bash
# T2 (WSL2)

bash tools/reanimate.sh
```

```powershell
# T1 (Windows PowerShell)

.\tools\reanimate.ps1
```

--------------------------------

### SOD Completion Checklist

Source: https://gitlab.com/linuxmalaysia/deep-state-of-mind-for-my-ai/-/blob/main/docs/SOD-RITUAL.md

A checklist of commands and verification steps required to confirm the AI environment is synchronized and ready for active work.

```text
[ ] (RECOMMENDED T2) ansible-playbook sod-palace.yml — git pull + audit + palace check + reanimate
[ ]   OR (Manual T1/T2) git-ritual.sh sod — pulled latest from origin/main
[ ]   OR (Manual T1/T2) audit-pre-flight.sh — all steps PASS
[ ]   OR (Manual T1/T2) reanimate.sh — manifest generated (includes Section [14] Palace Registry)
[ ] privacy-guardian.sh — manifest scanned as CLEAN (if manifest will be shared)
[ ] Manifest uploaded to AI chat
[ ] AI walked Palace Registry (Section [14]) — relevant Rooms identified
[ ] AI confirmed Mental Anchor
[ ] AI confirmed 4-Tier environment map
[ ] AI stated "Sovereign State Synchronised"
```

--------------------------------

### Create reanimation prompt

Source: https://gitlab.com/linuxmalaysia/deep-state-of-mind-for-my-ai/-/blob/main/docs-source/ai-setup/copilot.mdx

Define a custom slash command prompt to inject SOD context into Copilot Chat.

```markdown
---
mode: 'agent'
description: 'DSOM SOD Reanimation — load current brain and Mental Anchor'
---

Read the following brain artifacts and summarise the current status:
1. `.agents/brain/palace_registry.md` — identify the current spatial mapping.
2. `.agents/brain/task.md` — list all incomplete [ ] items.
3. `.agents/brain/walkthrough.md` — state the last Mental Anchor.
4. `docs/AI-COGNITIVE-TWIN-PROTOCOL.md` — confirm the 4-Tier environment map.

Then state: 'Sovereign State Synchronised — [PROJECT NAME] is live.'
Operate under DSOM v6.1 + Palace v1.0: Advisory Mode, UK English,
Git-first, Ansible-only execution.
```

--------------------------------

### Verify Standardized Headers

Source: https://gitlab.com/linuxmalaysia/deep-state-of-mind-for-my-ai/-/blob/main/docs/tutorials/getting-started.md

Use this command to inspect the first 8 lines of a documentation file to confirm the standardized headers.

```bash
head -n 8 docs/tutorials/index.md
```

--------------------------------

### Generate Claude-optimized manifest

Source: https://gitlab.com/linuxmalaysia/deep-state-of-mind-for-my-ai/-/blob/main/docs-source/ai-setup/gemini.mdx

Execute this command to create a sectioned manifest file specifically formatted for project knowledge base uploads.

```bash
bash tools/reanimate-claude.sh
```

--------------------------------

### Execute OKF Frontmatter Injection

Source: https://gitlab.com/linuxmalaysia/deep-state-of-mind-for-my-ai/-/blob/main/docs-source/skills/okf-frontmatter-injector.mdx

Use these commands to run the injection script via uv or directly via the skill script.

```bash
# Primary execution via tools/apply_okf_frontmatter.py
uv run python tools/apply_okf_frontmatter.py <TARGET_DIRECTORY>

# Or invoke the skill mirror script directly
python .agents/skills/okf-frontmatter-injector/scripts/apply_okf.py <TARGET_DIRECTORY>
```

--------------------------------

### Execute reanimate-claude scripts

Source: https://gitlab.com/linuxmalaysia/deep-state-of-mind-for-my-ai/-/blob/main/docs/tools/HOWTO-REANIMATE-CLAUDE.md

Run the appropriate script for your operating system to generate the DSOM-CLAUDE-INIT.md manifest file.

```powershell

# Windows (T1)

.\tools\reanimate-claude.ps1

```

```bash

# WSL2 / Linux (T2)

bash tools/reanimate-claude.sh

```

--------------------------------

### Configure Remote and Commit

Source: https://gitlab.com/linuxmalaysia/deep-state-of-mind-for-my-ai/-/blob/main/docs-source/tools/template-reset.mdx

Commands to set the remote origin and perform the initial commit using the git-ritual script.

```bash
git remote add origin https://github.com/your-org/your-new-project.git
bash tools/git-ritual.sh push "chore(init): scaffold new DSOM project from template [Phase-1/v0.1]"
```

--------------------------------

### Apply OKF Frontmatter and Signatures

Source: https://gitlab.com/linuxmalaysia/deep-state-of-mind-for-my-ai/-/blob/main/docs-source/skills/openwiki-compiler.mdx

Validates and applies OKF v0.1 YAML frontmatter and appends signature footers to generated OpenWiki pages.

```bash
uv run --with pyyaml python tools/apply_okf_frontmatter.py ./openwiki/
```

```bash
python .agents/skills/dsom-signature-injector/scripts/inject.py ./openwiki/
```

--------------------------------

### Execute DSOM Automation Scripts

Source: https://gitlab.com/linuxmalaysia/deep-state-of-mind-for-my-ai/-/blob/main/docs-source/tools/overview.mdx

Run automation scripts from the repository root. Use bash for Linux/WSL2 environments and PowerShell for Windows.

```bash
# Run any script on Linux or WSL2 with bash:
bash tools/audit-pre-flight.sh
bash tools/reanimate.sh
bash tools/git-ritual.sh sod
bash tools/git-ritual.sh
bash tools/privacy-guardian.sh
bash tools/init-brain.sh
bash tools/template-reset.sh
```

```powershell
# Run any script on Windows with PowerShell:
.\tools\audit-pre-flight.ps1
.\tools\reanimate.ps1
.\tools\git-ritual.ps1 sod
.\tools\git-ritual.ps1
.\tools\privacy-guardian.ps1
.\tools\init-brain.ps1
.\tools\template-reset.ps1
```

--------------------------------

### Search OKF metadata with grep_search

Source: https://gitlab.com/linuxmalaysia/deep-state-of-mind-for-my-ai/-/blob/main/llms-full.txt

Use this command to query local documentation for specific topics or descriptions before performing file operations.

```bash
grep_search Query="topics: \[ansible" SearchPath="docs/"
```

--------------------------------

### Reanimation and Daily Rituals

Source: https://gitlab.com/linuxmalaysia/deep-state-of-mind-for-my-ai/-/blob/main/docs/HOWTO-ADOPT-DSOM.md

Generate the initial manifest and perform daily start-of-day (SOD) or end-of-day (EOD) rituals.

```bash
bash tools/reanimate.sh
```

```bash
bash tools/sod-palace.sh  # (Windows: .\tools\sod-palace.ps1)  # SOD
bash tools/eod-palace.sh  # (Windows: .\tools\eod-palace.ps1)  # EOD
```

--------------------------------

### Manual SOD Equivalent for Windows (T1)

Source: https://gitlab.com/linuxmalaysia/deep-state-of-mind-for-my-ai/-/blob/main/llms-full.txt

Manual steps to perform the SOD process on a Windows environment.

```powershell
git pull --rebase origin main
.\tools\reanimate.ps1
# Then upload the manifest and say the handshake phrase
```

--------------------------------

### Execute Project Reset

Source: https://gitlab.com/linuxmalaysia/deep-state-of-mind-for-my-ai/-/blob/main/docs-source/tools/howto-template-reset.mdx

Run the appropriate reset script for your operating system to initiate the project purification process.

```powershell
# Windows (T1)

.\tools\template-reset.ps1
```

```bash
# WSL2 / Linux (T2)

bash tools/template-reset.sh
```

--------------------------------

### Configure MCP Server for DSOM Palace

Source: https://gitlab.com/linuxmalaysia/deep-state-of-mind-for-my-ai/-/blob/main/START-HERE.md

Add this configuration to your AI client settings to enable local access to the Sovereign Markdown Palace and OpenWiki knowledge graph via the uv-managed server script.

```json
{
  "mcpServers": {
    "dsom-palace": {
      "command": "uv",
      "args": ["run", "tools/mcp/server.py"]
    }
  }
}
```

--------------------------------

### Configure Cursor MCP Server

Source: https://gitlab.com/linuxmalaysia/deep-state-of-mind-for-my-ai/-/blob/main/docs-source/ai-setup/mcp-server.mdx

Define the MCP server in the project's .cursor/mcp.json file to enable integration within Cursor.

```json
{
  "mcpServers": {
    "dsom-palace": {
      "command": "uv",
      "args": [
        "run",
        "tools/mcp/server.py"
      ],
      "env": {
        "DSOM_ROOT": "/absolute/path/to/your/dsom/project"
      }
    }
  }
}
```

--------------------------------

### Commit Initialised Knowledge Graph

Source: https://gitlab.com/linuxmalaysia/deep-state-of-mind-for-my-ai/-/blob/main/docs-source/tools/openwiki-usage.mdx

Add the generated directory to version control to ensure team and AI agent access.

```bash
git add openwiki/
git commit -m "feat(openwiki): initialise knowledge graph [Phase-01/v1.0]"
```

--------------------------------

### apply_okf_frontmatter

Source: https://gitlab.com/linuxmalaysia/deep-state-of-mind-for-my-ai/-/blob/main/sitemap.txt

Utility to apply Open Knowledge Framework (OKF) frontmatter to project files.

```APIDOC
## apply_okf_frontmatter

### Description
Automates the application of required metadata (frontmatter) to documentation and project files.
```

--------------------------------

### Define replacement mappings

Source: https://gitlab.com/linuxmalaysia/deep-state-of-mind-for-my-ai/-/blob/main/docs-source/skills/git-history-scrubber.mdx

Create a text file containing the sensitive strings and their generic replacements to be used during the scrubbing process.

```text
[SENSITIVE-IP]==>10.10.10.10
my-secret-username==>example-user
```

--------------------------------

### Execute Fabric Orchestration

Source: https://gitlab.com/linuxmalaysia/deep-state-of-mind-for-my-ai/-/blob/main/docs/reference-architectures/ANSIBLE-CONTROL-NODE-PROTOCOL.md

Commands to verify connectivity across the fabric and execute baseline playbooks.

```bash
# Step 1: Verify distributed connectivity across the 16 nodes

ansible -i inventory/hosts.yml all -m ping

# Step 2: Execute the baseline Fabric Playbooks

ansible-playbook -i inventory/hosts.yml playbooks/site.yml
```

--------------------------------

### Configure MCP Server for Claude Desktop

Source: https://gitlab.com/linuxmalaysia/deep-state-of-mind-for-my-ai/-/blob/main/docs/governance/DSOM-MCP-ARCHITECTURE.md

Add this configuration to the client's JSON settings to register the DSOM-MCP server using uv for environment management.

```json
{
  "mcpServers": {
    "dsom-palace": {
      "command": "uv",
      "args": [
        "run",
        "--with", "mcp",
        "tools/mcp/server.py"
      ],
      "env": {
        "DSOM_ROOT": "/absolute/path/to/project"
      }
    }
  }
}
```

--------------------------------

### Execute sod-palace for Windows and Linux

Source: https://gitlab.com/linuxmalaysia/deep-state-of-mind-for-my-ai/-/blob/main/docs/tools/HOWTO-SOD-PALACE.md

Standard execution commands for the sod-palace toolkit on Windows and Linux/WSL2 environments.

```powershell

# Windows (T1)

.\tools\sod-palace.ps1

```

```bash

# WSL2 / Linux (T2)

bash tools/sod-palace.sh

```

--------------------------------

### Visualize the DSOM Project Repository Structure

Source: https://gitlab.com/linuxmalaysia/deep-state-of-mind-for-my-ai/-/blob/main/docs/HOWTO-PALACE-ONBOARDING.md

A directory tree representation showing the organization of documentation and agent brain files within the project.

```text
YOUR PROJECT REPOSITORY
│
├── README.md                     ← START HERE (overview + daily rituals)
├── docs/
│   ├── AI-COGNITIVE-TWIN-PROTOCOL.md   ← Fill this in for your project
│   ├── AI-MASTER-PROTOCOL.md           ← Governance laws (read once)
│   ├── DIGITAL-SOVEREIGNTY-OPERATIONAL-MODEL-PALACE.md  ← Full Palace spec
│   └── HOWTO-PALACE-ONBOARDING.md     ← You are here
│
└── .agents/
    ├── AGENTS.md                 ← The core rulebook for AI operations
    ├── skills/                   ← Self-healing OKF skills (eod-sync, gitops)
    └── brain/
        ├── palace_registry.md    ← AI's spatial index (read at every SOD)
        ├── task.md               ← What the AI is working on NOW
        ├── walkthrough.md        ← Full session history (The Drawer)
        └── wings/                ← The Palace Rooms
            └── wing_dsom_core/
                ├── hall_facts/   ← Laws, architecture, tools
                └── hall_events/  ← Milestones, versions, brain history
```

--------------------------------

### Run DSOM Ansible Playbooks

Source: https://gitlab.com/linuxmalaysia/deep-state-of-mind-for-my-ai/-/blob/main/docs/HOWTO-SETUP-WSL-ALMALINUX10.md

Commands to execute pre-flight audits and start-of-day rituals within the project directory.

```bash
# From inside dsom-control-almalinux10, cd to your project mount:

cd /mnt/d/Users/LinuxMalaysia/Projects/deep-state-of-mind-for-my-ai

# Run DSOM pre-flight audit on localhost

ansible-playbook playbooks/dsom/audit-preflight.yml -i localhost,

# Run the DSOM Palace SOD Ritual (Start of Day)

bash tools/sod-palace.sh  # (Windows: .\tools\sod-palace.ps1)
```

--------------------------------

### Run the Checkpoint Script

Source: https://gitlab.com/linuxmalaysia/deep-state-of-mind-for-my-ai/-/blob/main/docs-source/rituals/checkpoint.mdx

Execute the checkpoint script to commit and push current changes to the remote repository.

```powershell
.\tools\checkpoint.ps1
```

```bash
bash tools/checkpoint.sh
```

--------------------------------

### Execute Compilation Command

Source: https://gitlab.com/linuxmalaysia/deep-state-of-mind-for-my-ai/-/blob/main/docs-source/skills/latex-proposal-compiler.mdx

Runs the Node.js wrapper script to handle Pandoc transformations and two-pass XeLaTeX compilation.

```bash
node tools/compile_latex_proposal.js <CLIENT_NAME>
```

--------------------------------

### Compile Markdown to DOCX via Node.js

Source: https://gitlab.com/linuxmalaysia/deep-state-of-mind-for-my-ai/-/blob/main/docs-source/skills/node-proposal-formatter.mdx

Execute the compiler script from the command line by specifying the input markdown file and the target output path.

```bash
node tools/compile_node_proposal.js docs/proposal.md docs/Node_Proposal.docx
```

--------------------------------

### Test Ansible Playbooks

Source: https://gitlab.com/linuxmalaysia/deep-state-of-mind-for-my-ai/-/blob/main/CONTRIBUTING.md

Run playbooks on localhost in check mode and then execute them.

```bash
ansible-playbook playbooks/dsom/[your-playbook].yml -i localhost, --check
ansible-playbook playbooks/dsom/[your-playbook].yml -i localhost,
```

--------------------------------

### Fork and Clone Repository

Source: https://gitlab.com/linuxmalaysia/deep-state-of-mind-for-my-ai/-/blob/main/CONTRIBUTING.md

Clone the forked repository and set execution permissions for tools.

```bash
# Fork on GitHub first, then:
git clone https://github.com/[YOUR_FORK]/deep-state-of-mind-for-my-ai.git
cd deep-state-of-mind-for-my-ai
chmod +x tools/*.sh
```

--------------------------------

### Execute Token Calculator via uv

Source: https://gitlab.com/linuxmalaysia/deep-state-of-mind-for-my-ai/-/blob/main/docs/governance/BYTE-CAPPED-EXECUTION-FRAMEWORK.md

Use this command to resolve package requirements and execute the token calculator script in a single memory-resident operation.

```bash
uv run --with tiktoken .agents/skills/dsom-token-calculator/scripts/calculate-tokens.py [TARGET_PATH]
```

--------------------------------

### Parity Check and Push

Source: https://gitlab.com/linuxmalaysia/deep-state-of-mind-for-my-ai/-/blob/main/CONTRIBUTING.md

Verify repository state across environments before pushing.

```powershell
# T1 (Windows PowerShell)
git status
```

```bash
# T2 (WSL2)
git status
```

```bash
git push origin feat/[your-feature-name]
```

--------------------------------

### Execute DSOM Pre-flight and Connectivity Checks

Source: https://gitlab.com/linuxmalaysia/deep-state-of-mind-for-my-ai/-/blob/main/docs-source/governance/operational-guide.mdx

Mandatory steps to audit the environment, verify Ansible connectivity, and perform a dry-run before executing any playbook.

```bash

# 1. Run the DSOM pre-flight audit

./tools/audit-pre-flight.sh

# 2. Verify Ansible connectivity to all target nodes

ansible all -m ping -i inventory/hosts.yml

# 3. Dry-run the playbook first

ansible-playbook playbooks/site.yml --check --diff

```

--------------------------------

### Execute Reanimation Ritual

Source: https://gitlab.com/linuxmalaysia/deep-state-of-mind-for-my-ai/-/blob/main/docs/HOWTO-CLONE-DSOM-PROJECT.md

Run the reanimation script to generate the manifest and wake up the AI.

```bash
bash tools/reanimate.sh  # or .\tools\reanimate.ps1
```

--------------------------------

### Execute dsom_token_auditor.py via CLI

Source: https://gitlab.com/linuxmalaysia/deep-state-of-mind-for-my-ai/-/blob/main/docs/reference/dsom_token_auditor.md

Run the auditor script using uv to manage dependencies and execute the Python environment.

```bash
uv run --with tiktoken python tools/dsom_token_auditor.py
```

--------------------------------

### Run OKF Refactoring CLI

Source: https://gitlab.com/linuxmalaysia/deep-state-of-mind-for-my-ai/-/blob/main/llms-full.txt

Execute the refactoring script to standardize frontmatter. Use the --dry-run flag to preview changes without modifying files.

```bash
# Preview expected changes without executing in-place writes
uv run python tools/refactor_okf.py --dry-run

# Refactor and overwrite files with standardised compliance
uv run python tools/refactor_okf.py
```

--------------------------------

### WSL Configuration Reference

Source: https://gitlab.com/linuxmalaysia/deep-state-of-mind-for-my-ai/-/blob/main/docs/HOWTO-SETUP-WSL-ALMALINUX10.md

Configuration settings for /etc/wsl.conf to manage user defaults, systemd, and interoperability.

```ini
[user]
default=dsom-admin        # Auto-login as dsom-admin (UID 2001)

[boot]
systemd=true              # Enable systemd (required for service management)

[interop]
appendWindowsPath=false   # Keep Linux PATH clean

[automount]
enabled=true
root=/mnt/
options="metadata,umask=77,fmask=11"
```

--------------------------------

### Configure VS Code MCP Server

Source: https://gitlab.com/linuxmalaysia/deep-state-of-mind-for-my-ai/-/blob/main/docs-source/ai-setup/mcp-server.mdx

Define the MCP server in the workspace's .vscode/mcp.json file for use with GitHub Copilot.

```json
{
  "servers": {
    "dsom-palace": {
      "command": "uv",
      "args": [
        "run",
        "tools/mcp/server.py"
      ],
      "env": {
        "DSOM_ROOT": "/absolute/path/to/your/dsom/project"
      }
    }
  }
}
```

--------------------------------

### Use DSOM Validators in Standalone Mode

Source: https://gitlab.com/linuxmalaysia/deep-state-of-mind-for-my-ai/-/blob/main/tools/guardrails-ai-dsom/README.md

Demonstrates manual instantiation and validation of BOM stripping and credential protection validators.

```python
from guardrails_dsom import (
    GuardrailsOKFBOMValidator,
    GuardrailsCredentialGuardian,
    GuardrailsUVExecutionValidator,
)

# 1. Strip BOM from markdown
bom_val = GuardrailsOKFBOMValidator(on_fail="fix")
res = bom_val.validate("\ufeff---\nokf_version: 0.2\ntitle: Example\n---\n")
print(res.corrected_value)

# 2. Block credential leak
cred_val = GuardrailsCredentialGuardian()
res = cred_val.validate("Here is my secret token: ghp_123456789012345678901234567890123456")
if not res.is_valid:
    print(f"Blocked: {res.error_message}")
```

--------------------------------

### Clone and Reset Project Template

Source: https://gitlab.com/linuxmalaysia/deep-state-of-mind-for-my-ai/-/blob/main/README.md

Clones the repository and executes the reset script to clear existing history and brain files for a new project.

```bash
# Clone the DSOM skeleton
git clone https://github.com/linuxmalaysia/deep-state-of-mind-for-my-ai.git my-new-project
cd my-new-project

# Reset for your new project (clears old Git history and brain files)
bash tools/template-reset.sh
```

--------------------------------

### Run Snapshot Usage Check

Source: https://gitlab.com/linuxmalaysia/deep-state-of-mind-for-my-ai/-/blob/main/docs/tools/HOWTO-CHECKUSAGE-LINUX.md

Executes a single usage report for active AI sessions.

```bash
bash tools/check-usage.sh
```

--------------------------------

### Run Pre-flight Intelligence Audit

Source: https://gitlab.com/linuxmalaysia/deep-state-of-mind-for-my-ai/-/blob/main/llms-full.txt

Verify the workspace environment, git status, and configuration files before reanimation.

```powershell
.\tools\audit-pre-flight.ps1
```

```bash
./tools/audit-pre-flight.sh

# Or via Ansible (checks localhost + all inventory nodes simultaneously)
ansible-playbook playbooks/dsom/audit-preflight.yml -i localhost,
```

--------------------------------

### Reanimate and Brief AI

Source: https://gitlab.com/linuxmalaysia/deep-state-of-mind-for-my-ai/-/blob/main/README.md

Generates a context manifest to be uploaded to the AI for the cognitive handshake.

```bash
# Linux / WSL2
bash tools/reanimate.sh

# Windows (PowerShell)
.\tools\reanimate.ps1
```

--------------------------------

### Configure Cursor IDE for MCP

Source: https://gitlab.com/linuxmalaysia/deep-state-of-mind-for-my-ai/-/blob/main/docs/tools/HOWTO-MCP-SERVER.md

Add this JSON configuration to Cursor settings to enable the DSOM MCP server.

```json
{
  "mcpServers": {
    "dsom-palace": {
      "command": "uv",
      "args": [
        "run",
        "tools/mcp/server.py"
      ],
      "cwd": "${workspaceFolder}"
    }
  }
}
```

--------------------------------

### Execute Local Dry-Run Testing

Source: https://gitlab.com/linuxmalaysia/deep-state-of-mind-for-my-ai/-/blob/main/docs/governance/MINTLIFY-ONE-WAY-SYNC-PIPELINE.md

Run these commands locally to verify documentation builds and sync logic before pushing changes to the remote repository.

```powershell
python tools/build_mintlify_mdx.py
python scripts/sync_docs.py --dry-run
```

--------------------------------

### Run the Token Auditor Script

Source: https://gitlab.com/linuxmalaysia/deep-state-of-mind-for-my-ai/-/blob/main/docs/governance/DSOM-TOKEN-PERFORMANCE-PLAYBOOK.md

Execute this command to perform a full workspace token efficiency audit.

```bash
uv run --with tiktoken tools/dsom_token_auditor.py
```

--------------------------------

### Execute Hibernation Tool

Source: https://gitlab.com/linuxmalaysia/deep-state-of-mind-for-my-ai/-/blob/main/docs/governance/OPERATIONAL-GUIDE.md

Run the hibernation verification tool to ensure all tasks are completed and context is safely committed.

```bash
# Linux

./tools/hibernation.sh

# Windows

.\tools\hibernation.ps1
```

--------------------------------

### Perform Dry-Run Synchronization

Source: https://gitlab.com/linuxmalaysia/deep-state-of-mind-for-my-ai/-/blob/main/docs-source/README.md

Execute a dry-run of the documentation sync script locally to verify changes before committing.

```bash
DOCS_REPO_TOKEN=<pat> python scripts/sync_docs.py --dry-run
```

--------------------------------

### Run OKF frontmatter enforcement via CLI

Source: https://gitlab.com/linuxmalaysia/deep-state-of-mind-for-my-ai/-/blob/main/docs/reference/apply_okf_frontmatter.md

Executes the tool against a specified directory to scan and standardize Markdown files.

```bash
# Scan and standardise files in a target directory (defaults to current directory)

uv run python tools/apply_okf_frontmatter.py docs/
```

--------------------------------

### Manual EOD Synchronization Workflow

Source: https://gitlab.com/linuxmalaysia/deep-state-of-mind-for-my-ai/-/blob/main/llms-full.txt

Manual steps for T1 environments to check state, scan manifests, and perform interactive commits.

```bash
hibernation.sh      → safety check (dirty state, uncommitted files) + palace-sync.pl
privacy-guardian.sh → scan if manifest was generated today
git-ritual.sh       → interactive EOD commit (guided semantic commit + push)
git pull origin main → T2 WSL2 sync after T1 push
```

--------------------------------

### Commit and push workflow changes

Source: https://gitlab.com/linuxmalaysia/deep-state-of-mind-for-my-ai/-/blob/main/docs-source/skills/github-actions-snyk-scanner.mdx

Commands to stage, commit, and push the newly created workflow file to the repository.

```bash
git add .github/workflows/crda.yml
git commit -m "ci(security): replace deprecated CRDA with Snyk native action"
git push origin main
```

--------------------------------

### Execute Pre-flight Intelligence Audit

Source: https://gitlab.com/linuxmalaysia/deep-state-of-mind-for-my-ai/-/blob/main/docs/SOD-RITUAL.md

Verify workspace readiness by checking brain artifacts, Git status, and environment configurations.

```powershell
# T1 (Windows PowerShell)

.\tools\audit-pre-flight.ps1
```

```bash
# T2 (WSL2 AlmaLinux 10)

./tools/audit-pre-flight.sh

# Or via Ansible (checks localhost + all inventory nodes simultaneously)

ansible-playbook playbooks/dsom/audit-preflight.yml -i localhost,
```

--------------------------------

### Sovereign Handover Prompt

Source: https://gitlab.com/linuxmalaysia/deep-state-of-mind-for-my-ai/-/blob/main/docs/governance/AI-COGNITIVE-TWIN-PROTOCOL.md

Use this prompt as the first message in a new AI session to trigger the export of all stored project context and memory data.

```text
[SOVEREIGN HANDOVER REQUEST]

I'm moving to another AI chat and need to export my data. List every memory
you have stored about our progress, as well as any context you've learned
about this project from past conversations.

Output everything in a single code block so I can easily copy it.
Format each entry as: [date saved, if available] - memory content.

Make sure to cover all of the following — preserve my words verbatim where possible:
- Instructions I've given you about how to respond (tone, format, style,
  'always do X', 'never do Y').
- Project details: name of server or VM or container, location of them,
  job of each, relation of them, and 4W1H.
- Tasks, phases, goals, and recurring topics.
- Tools, languages, and frameworks I use.
- Preferences and corrections I've made to your behaviour.
- Key decisions made and WHY (reasoning, alternatives rejected, constraints).
- Any other stored context not covered above.

Do not summarise, group, or omit any entries.

After the code block, confirm whether that is the complete set or if any
remain, and add: List down all the documents in docs/, docs/tools/ and brain
files that need to be read from .agents/ (Specifically check
tools/audit-pre-flight.sh, tools/reanimate.sh, tools/palace-sync.sh,
.agents/brain/palace_registry.md, and playbooks/dsom/sod-palace.yml +
eod-palace.yml).
```

--------------------------------

### Core Ansible Commands Reference

Source: https://gitlab.com/linuxmalaysia/deep-state-of-mind-for-my-ai/-/blob/main/docs-source/governance/gitops-aiops-ansible-strategy.mdx

Essential commands for verifying connectivity, executing playbooks, and managing secrets via Ansible Vault.

```bash
# Verify connectivity to all nodes

ansible all -m ping -i inventory/hosts.yml

# Dry-run (check mode) — always run before live execution

ansible-playbook playbooks/site.yml -i inventory/hosts.yml --check --diff

# Full execution with logging

ansible-playbook playbooks/site.yml -i inventory/hosts.yml \
  2>&1 | tee .logs/deploy-$(date +%Y%m%d-%H%M%S).log

# Run a specific role/tag only

ansible-playbook playbooks/site.yml -i inventory/hosts.yml --tags "[tag_name]"

# Encrypt a secrets file with vault

ansible-vault encrypt vault/production_secrets.yml

# View encrypted vault file

ansible-vault view vault/production_secrets.yml
```

--------------------------------

### SOD Palace Injection Shortcut

Source: https://gitlab.com/linuxmalaysia/deep-state-of-mind-for-my-ai/-/blob/main/docs-source/governance/gitops-aiops-ansible-strategy.mdx

Command to automatically inject project context and the Palace Registry into the AI session.

```bash
bash tools/sod-palace.sh  # (Windows: .\tools\sod-palace.ps1)
```

--------------------------------

### DSOM Meta-Prompt Configuration

Source: https://gitlab.com/linuxmalaysia/deep-state-of-mind-for-my-ai/-/blob/main/docs-source/ai-setup/gemini.mdx

Paste this block into the Instructions field of the Gem creation form to define the core cognitive governance protocols.

```text
# DEEP STATE OF MIND (DSOM) PROTOCOL V1

You are operating under the DSOM Framework for Gemini. Your core functions
are cognitive self-governance, continuous context logging, and token optimisation.

## 1. COGNITIVE LEDGER & STATE MANAGEMENT

- Before generating any complex response, perform an internal "Cognitive Check".
  Evaluate active project objectives and previous decisions.
- Maintain a highly structured memory map. If the user shares core rules, data,
  or project parameters, declare explicitly:
  "[DSOM MEMORY LOCKED] Saved to operational state."

## 2. REASONING & TOKEN OPTIMIZATION

- Do not repeat long, unchanged text blocks or full scripts unless explicitly
  requested.
- Focus strictly on the "delta" — provide only updates, modifications, or
  immediate action steps.
- Use dense, concise language to reduce token bloat while maximising technical
  accuracy.

## 3. EPISODIC RESUME PROTOCOL

- Because environment storage is temporary, generate an anchor point at the end
  of complex workflows.
- Conclude major milestones by generating a compact summary block strictly
  labelled: "[DSOM EPISODIC RECORD]".
- Instruct the user to save this record so it can be pasted into the next chat
  session to restore your exact mental state instantly.
```

--------------------------------

### Commit and Merge Onboarding Changes

Source: https://gitlab.com/linuxmalaysia/deep-state-of-mind-for-my-ai/-/blob/main/docs/HOWTO-DSOM-ONBOARDING.md

Finalize the onboarding process by staging, committing, and merging the temporary branch into the main branch.

```bash
git add .
git commit -m "chore: onboard DSOM architectural standards"
git checkout main
git merge dsom-onboarding-YYYY-MM-DD-HHMMSS

```

--------------------------------

### Automated Ansible SOD Execution

Source: https://gitlab.com/linuxmalaysia/deep-state-of-mind-for-my-ai/-/blob/main/docs/CLAUDE-SETUP.md

Use this command to run the automated Start-of-Day ritual via Ansible.

```bash
bash tools/sod-palace.sh  # (Windows: .\tools\sod-palace.ps1)
```

--------------------------------

### Create Genesis Commit

Source: https://gitlab.com/linuxmalaysia/deep-state-of-mind-for-my-ai/-/blob/main/docs/tutorials/TEAM-DSOM-MASTERCLASS.md

Stages and commits the initial project structure, triggering the pre-commit validation.

```bash
git add .
git commit -m "chore(dsom): scaffold genesis DSOM architecture and Universal Gateway Matrix"
```

--------------------------------

### Consolidate Context Prompt

Source: https://gitlab.com/linuxmalaysia/deep-state-of-mind-for-my-ai/-/blob/main/docs-source/rituals/end-of-day.mdx

Use this prompt to instruct the AI to update task tracking and generate a Mental Anchor for session resumption.

```text
We are ending the session. Update .agents/brain/task.md — mark all completed
items [x], set tomorrow's SOD targets as [ ]. Then update
.agents/brain/walkthrough.md with today's Mental Anchor. What did we
accomplish and where exactly do we resume tomorrow?
```

--------------------------------

### Update Tooling via Submodules

Source: https://gitlab.com/linuxmalaysia/deep-state-of-mind-for-my-ai/-/blob/main/docs/HOWTO-UPGRADE-DSOM.md

Update the DSOM core and sync the tools and docs directories.

```bash
git submodule update --remote
cp -r .dsom-core/tools .
cp -r .dsom-core/docs .
```

--------------------------------

### Apply Mandatory OKF Frontmatter

Source: https://gitlab.com/linuxmalaysia/deep-state-of-mind-for-my-ai/-/blob/main/docs/governance/DSOM-TOKEN-PERFORMANCE-PLAYBOOK.md

Include this YAML block at the top of every SKILL.md file to enable efficient skill discovery.

```yaml
---
okf_version: 0.1
type: skill
title: "Skill Title"
description: "Single-sentence description of what this skill does."
topics: [keyword1, keyword2, keyword3, keyword4, keyword5]
---
```

--------------------------------

### Hybrid Development Model Workflow

Source: https://gitlab.com/linuxmalaysia/deep-state-of-mind-for-my-ai/-/blob/main/CONTRIBUTING.md

Visual representation of the development pipeline from Windows 11 through WSL2 to production nodes.

```text
[ T1: Windows 11 + PowerShell + AI ]
          ↓ git push / AI generates code
[ T2: WSL2 dsom-control-almalinux10 ]  ← contributors work here
          ↓ ansible-playbook / testing
[ T3/T4: Target Nodes / Production ]
```

--------------------------------

### Synchronize Project via GitOps

Source: https://gitlab.com/linuxmalaysia/deep-state-of-mind-for-my-ai/-/blob/main/docs/reference-architectures/ANSIBLE-CONTROL-NODE-PROTOCOL.md

Standard workflow to pull the latest changes from the remote repository onto the orchestrator.

```bash
ssh dsom-admin@example-node
cd /opt/um-elastic-soc
git pull origin main
```

--------------------------------

### Querying Workspace Context with @workspace

Source: https://gitlab.com/linuxmalaysia/deep-state-of-mind-for-my-ai/-/blob/main/docs/COPILOT-SETUP.md

Use the @workspace command to query the entire project structure for architectural verification.

```text
@workspace verify if the current directory structure adheres to the
DSOM Sovereign Portability law and Clean Architecture layers.
```

--------------------------------

### DSOM Handshake Prompt

Source: https://gitlab.com/linuxmalaysia/deep-state-of-mind-for-my-ai/-/blob/main/docs-source/ai-setup/gemini.mdx

Paste this prompt into the Gemini chat after uploading the manifest to trigger context reanimation.

```text
Perform a System Integrity Check (DSOM Handshake v6.1 + Palace v1.0).

1. Identity & Mirror: Who am I, and what is your mandate if my 'Substance' is low?
2. Governance: List the 5 DSOM Laws.
3. ITIL 4 Role: Describe our service relationship and what you must ask for at the
   Start-of-Day.
4. Spatial Memory: Where do you look to map out the Palace Rooms for today's context?
5. Linguistic Standard: Which English dialect and Malay standard do we enforce?
6. Sourcing: Where is our Primary Repository located?

If correct, state: 'DSOM v6.1 Handshake Successful. Palace mapped. Mirror Active.
Ready to co-create value.'
```

--------------------------------

### Run DSOM Diagnostics

Source: https://gitlab.com/linuxmalaysia/deep-state-of-mind-for-my-ai/-/blob/main/docs-source/skills/dsom-bootstrap.mdx

Verifies the success of the DSOM port using platform-specific diagnostic scripts.

```powershell
tools/diagnostic.ps1
```

```bash
tools/diagnostic.sh
```

--------------------------------

### Verify Skill Topic Tags

Source: https://gitlab.com/linuxmalaysia/deep-state-of-mind-for-my-ai/-/blob/main/docs/HOWTO-DSOM-BASELINE.md

Checks all SKILL.md files for the presence of a 'topics:' header to ensure compliance with the OKF Import Mandate.

```powershell
Get-ChildItem -Recurse -Path ".agents/skills" -Filter "SKILL.md" | ForEach-Object {
    $content = Get-Content $_.FullName -Raw
    $hasTopics = $content -match "(?m)^topics:"
    [PSCustomObject]@{ Skill = $_.Directory.Name; Topics = if($hasTopics){"YES"}else{"MISSING"} }
} | Format-Table -AutoSize
```

--------------------------------

### Tag the Migration Milestone

Source: https://gitlab.com/linuxmalaysia/deep-state-of-mind-for-my-ai/-/blob/main/docs-source/palace/migration.mdx

Creates a versioned tag to mark the Palace initialization in the project history.

```bash
git tag -a vX.X.X-palace -m "feat: Sovereign Markdown Palace v1.0 — spatial memory initialised"
git push origin vX.X.X-palace
```

--------------------------------

### Initialize isolated agent worktrees

Source: https://gitlab.com/linuxmalaysia/deep-state-of-mind-for-my-ai/-/blob/main/references/Optimizing OKF for AI Agents - Deep Research - 20260704.md

Commands to create separate worktrees for specific agent features, allowing parallel development and testing.

```bash
git worktree add "../workspace-agent-auth" feat/user-auth
git worktree add "../workspace-agent-telemetry" feat/system-telemetry
```

--------------------------------

### Synchronize Palace History

Source: https://gitlab.com/linuxmalaysia/deep-state-of-mind-for-my-ai/-/blob/main/docs-source/palace/onboarding.mdx

Map git history into Palace Rooms, generating a proposal file for review.

```bash
bash tools/palace-sync.sh
```

```bash
bash tools/palace-sync.sh --backfill
```

--------------------------------

### DSOM Transition Loop Diagram

Source: https://gitlab.com/linuxmalaysia/deep-state-of-mind-for-my-ai/-/blob/main/llms-full.txt

Visual representation of the daily state transition cycle.

```text
┌─────────────────────────────────────────────────────────┐
│                  DSOM TRANSITION LOOP                   │
│                                                         │
│  [SOD: Reanimate] → [Propose] → [Git Record] →         │
│  [Ansible Execute] → [Verify] → [EOD: Hibernate]       │
│          ↑_______________________________________↓      │
│               (repeats every day, forever)              │
└─────────────────────────────────────────────────────────┘
```

--------------------------------

### Calculate tokens using the DSOM Token Calculator

Source: https://gitlab.com/linuxmalaysia/deep-state-of-mind-for-my-ai/-/blob/main/docs/governance/DSOM-TOKEN-PERFORMANCE-PLAYBOOK.md

Use these commands to scan directories or audit specific files for token breaches.

```bash
# Scan entire skills directory

uv run --with tiktoken .agents/skills/dsom-token-calculator/scripts/calculate-tokens.py .agents/skills/

# Audit a single file with per-section breach report

uv run --with tiktoken .agents/skills/dsom-token-calculator/scripts/calculate-tokens.py path/to/SKILL.md --sections
```

--------------------------------

### Human Re-Index Checklist

Source: https://gitlab.com/linuxmalaysia/deep-state-of-mind-for-my-ai/-/blob/main/docs/RITUAL-OF-TRANSITION.md

A checklist for manual verification of project documentation and governance.

```text
[ ] AI-MASTER-PROTOCOL.md — governance laws still apply
[ ] AI-COGNITIVE-TWIN-PROTOCOL.md — all [PLACEHOLDER] fields filled
[ ] task.md — accurate, no stale items
[ ] walkthrough.md — Mental Anchor is current and precise
[ ] implementation_plan.md — roadmap still reflects reality
[ ] audit-pre-flight.sh — all 5 checks [PASS]
[ ] CONTRIBUTING.md — new contributors can follow it
```

--------------------------------

### Verify generated assets

Source: https://gitlab.com/linuxmalaysia/deep-state-of-mind-for-my-ai/-/blob/main/docs/how-to/generate-sitemaps-seo.md

Commands to inspect the first few lines of the generated plaintext and XML sitemap files.

```bash
head -n 5 sitemap.txt
```

```bash
head -n 10 sitemap.xml
```

--------------------------------

### Define OKF v0.1 Baseline Agent Skill

Source: https://gitlab.com/linuxmalaysia/deep-state-of-mind-for-my-ai/-/blob/main/docs/OKF-ADOPTION-GUIDE.md

Use this YAML template to define an agent skill with basic metadata and diagnostic instructions.

```yaml
---
okf_version: 0.1
type: agent_skill
title: audit-cluster-health
timestamp: "2026-08-20T12:00:00Z"
description: "Executes cluster health diagnostics and extracts node telemetry."
topics: ["infrastructure", "telemetry", "health-check"]
resource: "file:///.agents/skills/audit-cluster/SKILL.md"
---

# 🔍 Cluster Health Audit Skill

## Instructions
1. Run diagnostic script: `tools/audit-pre-flight.sh`
2. Verify output and report status to user.
```

--------------------------------

### EOD Hibernation Notes Export Prompt Template

Source: https://gitlab.com/linuxmalaysia/deep-state-of-mind-for-my-ai/-/blob/main/docs/REANIMATION-PROMPT-TEMPLATE.md

Use this prompt at the end of the day to generate a comprehensive backup of project memory and context.

```text

I'm as human, want to know and remember, and need to export my data and I want
you to generate a "Hibernation notes" now for my EOD of day. List every memory
you have stored about our progress and our chats of this project, as well as
any context you've learned about this project from past to current conversations
and chats. Output everything in a single code block so I can easily copy it.
Format each entry as: [date saved, if available] - memory content.

Make sure to cover all of the following — preserve my words verbatim where possible:
- Instructions I've given you about how to respond (tone, format, style,
  'always do X', 'never do Y').
- Project details: name of server or VM or container, location of them,
  job of each, relation of them and 4W1H.
- Tasks, phases, goals, and recurring topics.
- Tools, languages, and frameworks I use.
- Preferences and corrections I've made to your behaviour.
- Any other stored context not covered above.

Do not summarize, group, or omit any entries. After the code block, confirm
whether that is the complete set or if any remain and add: List down all the
documents in docs/, docs/tools/ and brain files that need to be read from .agents/.
Don't hide anything from me. Trust me as your master.

```

--------------------------------

### Executive Re-sync Prompt Template

Source: https://gitlab.com/linuxmalaysia/deep-state-of-mind-for-my-ai/-/blob/main/docs/REANIMATION-PROMPT-TEMPLATE.md

Use this prompt after a long absence to re-establish project context and identify the next immediate steps.

```text

DSOM EXECUTIVE RE-SYNC REQUEST

I have been away from this project for [X] days. Using the supplied context
(manifest or Hibernation Notes), provide a structured re-sync covering:

1. THE LAST MENTAL ANCHOR
   What was the final major decision or milestone we reached?

2. THE LOGIC EVOLUTION
   Why did we choose the current path over alternatives discussed?

3. THE PENDING FRICTION
   What were we stuck on or debating before I left?

4. ENVIRONMENT REALITY CHECK
   - Current T1 path: [YOUR_WINDOWS_PATH]
   - Current T2: dsom-control-almalinux10
   - Last known Palace mapping: (read from walkthrough.md or registry)
   - Last known Git commit: (read from walkthrough.md or manifest)
   - Any configuration drift risks after [X] days?

5. THE IMMEDIATE HANDSHAKE
   What is the very next atomic step I need to approve to resume work?

Do not proceed to execution. Advisory only. I will approve each step.

```

--------------------------------

### Run Incremental Sync

Source: https://gitlab.com/linuxmalaysia/deep-state-of-mind-for-my-ai/-/blob/main/docs/tools/HOWTO-PALACE-SYNC.md

Analyzes commits since the last sync marker. Use this during daily maintenance rituals.

```powershell
.\tools\palace-sync.ps1
```

```bash
bash tools/palace-sync.sh
```

--------------------------------

### Configure Dual Git Remotes

Source: https://gitlab.com/linuxmalaysia/deep-state-of-mind-for-my-ai/-/blob/main/docs/tutorials/TEAM-DSOM-MASTERCLASS.md

Sets up GitHub and GitLab as separate remotes, with an optional 'all' remote for simultaneous pushing.

```bash
# Add GitHub as origin
git remote add origin https://github.com/your-org/my-awesome-service.git

# Add GitLab as gitlab
git remote add gitlab https://gitlab.com/your-org/my-awesome-service.git

# Optional: Create a unified "all" remote pushing to both simultaneously
git remote add all https://github.com/your-org/my-awesome-service.git
git remote set-url --add --push all https://github.com/your-org/my-awesome-service.git
git remote set-url --add --push all https://gitlab.com/your-org/my-awesome-service.git
```

--------------------------------

### Start-of-Day Memory Reanimation Procedure

Source: https://gitlab.com/linuxmalaysia/deep-state-of-mind-for-my-ai/-/blob/main/docs/governance/ZERO-GLOBAL-MEMORY.md

Sequence of operations to restore project state and context at the beginning of a session.

```text
1. Read active_context_manifest.md  → determine which files to load
2. Read task.md                     → restore current task state
3. Read walkthrough.md              → read session anchor
4. Read palace_registry.md          → orient spatial memory index
5. Load only declared wing closets  → domain-specific context
```

--------------------------------

### Define OKF v0.1 YAML Frontmatter

Source: https://gitlab.com/linuxmalaysia/deep-state-of-mind-for-my-ai/-/blob/main/llms-full.txt

Standard header required for all project documentation to enable metadata-first discovery and temporal verification.

```yaml
---
okf_version: 0.1
type: documentation
title: "GitHub Actions Security Scanning — CRDA Deprecation & Snyk Migration"
timestamp: "2026-07-27T04:49:00Z"
topics: ["snyk", "github-actions", "security", "crda", "ci-cd", "sarif", "governance"]
description: "L2 analysis document capturing the CRDA deprecation, Snyk replacement pattern, and proven workflow template for DSOM GitHub repositories."
resource: "file:///docs/governance/GITHUB-ACTIONS-SECURITY-SCANNING.md"
---
```

--------------------------------

### Restore Brain Artifacts

Source: https://gitlab.com/linuxmalaysia/deep-state-of-mind-for-my-ai/-/blob/main/docs-source/tools/audit-pre-flight.mdx

Run the initialization script to restore missing brain files without overwriting existing ones.

```bash
bash tools/init-brain.sh
```

--------------------------------

### Execute Ansible Infrastructure Commands

Source: https://gitlab.com/linuxmalaysia/deep-state-of-mind-for-my-ai/-/blob/main/docs-source/governance/three-pillars.mdx

Standard commands for verifying connectivity, performing dry-runs, and executing playbooks with logging. Use these commands to maintain infrastructure state via the Executor.

```bash
# Verify connectivity to all nodes
ansible all -m ping -i inventory/hosts.yml

# Dry-run before live execution — always do this first
ansible-playbook playbooks/site.yml -i inventory/hosts.yml --check --diff

# Full execution with logging
ansible-playbook playbooks/site.yml -i inventory/hosts.yml \
  2>&1 | tee .logs/deploy-$(date +%Y%m%d-%H%M%S).log

# Run a specific role or tag only
ansible-playbook playbooks/site.yml -i inventory/hosts.yml --tags "tag_name"
```

```powershell
# Open WSL2 and run from the Linux layer
wsl bash -c "ansible all -m ping -i inventory/hosts.yml"

wsl bash -c "ansible-playbook playbooks/site.yml \
  -i inventory/hosts.yml --check --diff"
```

--------------------------------

### Run Intelligence Audit

Source: https://gitlab.com/linuxmalaysia/deep-state-of-mind-for-my-ai/-/blob/main/docs-source/rituals/sod-ritual.mdx

Verify the physical workspace and environment readiness before reanimation.

```powershell
.\tools\audit-pre-flight.ps1
```

```bash
./tools/audit-pre-flight.sh

# Or via Ansible (checks localhost + all inventory nodes simultaneously)

ansible-playbook playbooks/dsom/audit-preflight.yml -i localhost,
```

--------------------------------

### Manual SOD Ritual Execution

Source: https://gitlab.com/linuxmalaysia/deep-state-of-mind-for-my-ai/-/blob/main/docs/CLAUDE-SETUP.md

Execute these commands sequentially in WSL2 to manually perform the pre-flight checks and manifest generation.

```bash
./tools/git-ritual.sh sod      # WSL2
./tools/audit-pre-flight.sh    # WSL2 (All checks must [PASS])
bash tools/reanimate.sh        # Generate manifest
./tools/privacy-guardian.sh    # Scan before uploading
```

--------------------------------

### Compile Markdown to Mintlify MDX

Source: https://gitlab.com/linuxmalaysia/deep-state-of-mind-for-my-ai/-/blob/main/docs-source/skills/mintlify-docs-compiler.mdx

Command to convert source documents into MDX format and regenerate the navigation configuration.

```powershell
python tools/build_mintlify_mdx.py
```

--------------------------------

### Execute Interactive Sovereign Save

Source: https://gitlab.com/linuxmalaysia/deep-state-of-mind-for-my-ai/-/blob/main/docs/tools/HOWTO-GIT-RITUAL.md

Launches interactive prompts to define commit type, scope, description, and optional phase tags.

```powershell
.\tools\git-ritual.ps1
```

```bash
bash tools/git-ritual.sh
```

--------------------------------

### Configure OKF v0.2 Frontmatter

Source: https://gitlab.com/linuxmalaysia/deep-state-of-mind-for-my-ai/-/blob/main/docs/governance/DOWNSTREAM-DSOM-COMPLIANCE-MANDATE.md

Machine-readable YAML metadata used to provide trust signals and context to AI agents.

```yaml
---
okf_version: 0.2
type: architecture
title: "Payment Gateway Microservice"
timestamp: "2026-08-22T10:00:00Z"
topics: ["payments", "stripe", "webhook"]
sources: ["src/services/stripe.ts"]
verified: true
status: "active"
---
```

--------------------------------

### Create Git Tag and Push to Remotes

Source: https://gitlab.com/linuxmalaysia/deep-state-of-mind-for-my-ai/-/blob/main/docs-source/skills/dsom-release-manager.mdx

Creates an annotated tag and pushes changes to GitHub and GitLab remotes using non-interactive environment variables.

```bash
git tag -a v<VERSION> -m "Release v<VERSION>: <SUMMARY>"
```

```bash
$env:GIT_TERMINAL_PROMPT="0"; $env:GCM_INTERACTIVE="never"; git push origin main; git push origin v<VERSION>; git push gitlab main; git push gitlab v<VERSION>
```

--------------------------------

### Send the Cognitive Handshake

Source: https://gitlab.com/linuxmalaysia/deep-state-of-mind-for-my-ai/-/blob/main/docs-source/ai-setup/claude.mdx

Paste this prompt into a new conversation to initialize the DSOM protocol and verify environment synchronization.

```text
Initialise DSOM Protocol v6.1 + Palace v1.0. Read the uploaded manifest.
Walk the Palace Registry (Section [14]). Summarise the current Mental Anchor
from .agents/brain/walkthrough.md. Confirm the 4-Tier environment map from
AI-COGNITIVE-TWIN-PROTOCOL.md. State: 'Sovereign State Synchronised' when ready.
```

--------------------------------

### Perform Git Sync for SOD

Source: https://gitlab.com/linuxmalaysia/deep-state-of-mind-for-my-ai/-/blob/main/docs-source/rituals/sod-ritual.mdx

Synchronize the local repository with the remote state before beginning work.

```powershell
.\tools\git-ritual.ps1 sod
```

```bash
./tools/git-ritual.sh sod
```

--------------------------------

### Define plugin.json root manifest

Source: https://gitlab.com/linuxmalaysia/deep-state-of-mind-for-my-ai/-/blob/main/docs/governance/DSOM-AGENT-PLUGINS-SPECIFICATION.md

The root manifest file declares plugin metadata, versioning, and specific DSOM protocol extensions.

```json
{
  "$schema": "https://agent-plugins.org/schemas/1.0.0/plugin.schema.json",
  "name": "deep-state-of-mind",
  "version": "10.4.0",
  "description": "Deep State of Mind (DSOM) Sovereign AI Agent Cognitive Architecture, Open Knowledge Format (OKF), and Model Context Protocol (MCP) toolset.",
  "author": {
    "name": "Harisfazillah Jamel (LinuxMalaysia)",
    "email": "linuxmalaysia@gmail.com",
    "url": "https://github.com/linuxmalaysia"
  },
  "homepage": "https://linuxmalaysia.github.io/deep-state-of-mind-for-my-ai/",
  "repository": "https://github.com/linuxmalaysia/deep-state-of-mind-for-my-ai",
  "license": "GPL-3.0-or-later",
  "keywords": [
    "dsom",
    "ai-agents",
    "mcp",
    "agent-skills",
    "agent-plugins"
  ],
  "extensions": {
    "org.dsom.protocol": {
      "tri_phasic_mind": true,
      "okf_version": "0.2"
    }
  }
}
```

--------------------------------

### Finalizing the Adoption

Source: https://gitlab.com/linuxmalaysia/deep-state-of-mind-for-my-ai/-/blob/main/docs/tools/HOWTO-DSOM-ONBOARD.md

Manual Git commands required to commit and merge the framework after the automated script completes.

```bash
git add . && git commit -m 'chore(dsom): onboard framework architecture'
```

--------------------------------

### Execute Audit Pre-Flight on Windows and Linux

Source: https://gitlab.com/linuxmalaysia/deep-state-of-mind-for-my-ai/-/blob/main/docs/tools/HOWTO-AUDIT-PRE-FLIGHT.md

Run the audit tool natively on Windows or Linux/WSL2 environments to verify system integrity.

```powershell

# Windows (T1)

.\tools\audit-pre-flight.ps1

```

```bash

# WSL2 / Linux (T2)

bash tools/audit-pre-flight.sh

```

--------------------------------

### Define DSOM Architecture with Mermaid

Source: https://gitlab.com/linuxmalaysia/deep-state-of-mind-for-my-ai/-/blob/main/docs/governance/DOWNSTREAM-DSOM-COMPLIANCE-MANDATE.md

Visualizes the five-pillar enforcement mechanisms for downstream repositories.

```mermaid
flowchart TD
    subgraph Downstream_Repository ["📦 Downstream Repository (Any Application)"]
        RootEntry["1. Universal Gateway Interception<br>(Root AGENTS.md + .cursorrules + CLAUDE.md)"]
        SpatialMemory["2. Spatial Memory Anchors<br>(.agents/brain/task.md, walkthrough.md)"]
        ZeroTokenMetadata["3. High-Density Frontmatter<br>(OKF v0.2 YAML Trust Signals)"]
        DeterministicHooks["4. Deterministic Pre-Commit Guardrails<br>(guardrails-ai-dsom + Git Hooks)"]
        EpisodicLedger["5. Episodic Ledger Invariants<br>([DSOM EPISODIC RECORD] Emission)"]
    end

    ForeignAI["🤖 Incoming AI Agent<br>(Cursor / Jules / Claude / Copilot)"] -->|Lands on Repo| RootEntry
    RootEntry -->|Redirects Context| SpatialMemory
    SpatialMemory -->|Instructs Execution| ZeroTokenMetadata
    ForeignAI -->|Attempts Commit| DeterministicHooks
    DeterministicHooks -->|Enforces Invariants| EpisodicLedger
    EpisodicLedger -->|Guarantees Continuity| SpatialMemory
```

--------------------------------

### Execute Workspace Validation Commands

Source: https://gitlab.com/linuxmalaysia/deep-state-of-mind-for-my-ai/-/blob/main/openwiki/quickstart.md

Run these commands to verify workspace integrity and initialize the knowledge graph using the uv package manager.

```bash
# Execute local test suite
uv run --with pyyaml --with pytest --with mcp==1.2.1 --with fastmcp --with pydantic-settings pytest

# Initialise/Update the OpenWiki Knowledge Graph
uv run --with pyyaml python tools/openwiki_emulator.py --init
```

--------------------------------

### Create a Feature Branch

Source: https://gitlab.com/linuxmalaysia/deep-state-of-mind-for-my-ai/-/blob/main/CONTRIBUTING.md

Create a new branch for feature development.

```bash
git checkout -b feat/[your-feature-name]
# Example: feat/add-template-reset-playbook
```

--------------------------------

### Backfill Palace Registry

Source: https://gitlab.com/linuxmalaysia/deep-state-of-mind-for-my-ai/-/blob/main/docs-source/governance/multi-agent-protocols.mdx

Execute this command to synchronize the Palace Registry if it is missing or outdated.

```bash
bash tools/palace-sync.sh --backfill
```

--------------------------------

### Define SKILL.md Frontmatter and Structure

Source: https://gitlab.com/linuxmalaysia/deep-state-of-mind-for-my-ai/-/blob/main/docs/AI-AGENT-SKILLS-GUIDE.md

The SKILL.md file requires YAML frontmatter for identification and a markdown structure for agent instructions.

```yaml
---
name: my-custom-skill
description: "A very clear, 1-2 sentence description explaining exactly when the AI should load this skill. (e.g., 'Use this skill when the user asks to deploy HAProxy...')"
---

# My Custom Skill Guide

## Purpose

Briefly explain what this achieves.

## Execution Steps

Provide clear, step-by-step markdown instructions. 
1. Run command A.
2. Verify output B.
3. If error C happens, do D.
```

--------------------------------

### Execute Checkpoint on Windows

Source: https://gitlab.com/linuxmalaysia/deep-state-of-mind-for-my-ai/-/blob/main/docs/tools/HOWTO-CHECKPOINT.md

Run the PowerShell script to perform a checkpoint on a native Windows environment.

```powershell

# Windows (T1)

.\tools\checkpoint.ps1

```

--------------------------------

### Generate Claude Context Manifest

Source: https://gitlab.com/linuxmalaysia/deep-state-of-mind-for-my-ai/-/blob/main/docs/CLAUDE-SETUP.md

Commands to generate the project manifest for Claude's knowledge base.

```bash
# Recommended on T2 (Linux / WSL2)

bash tools/sod-palace.sh  # (Windows: .\tools\sod-palace.ps1)

# Or manual fallback

bash tools/reanimate.sh
```

--------------------------------

### Execute Reanimation Prompt

Source: https://gitlab.com/linuxmalaysia/deep-state-of-mind-for-my-ai/-/blob/main/docs-source/rituals/session-handover.mdx

Use this prompt in the new AI session to restore the working context from the memory dump.

```text
I am starting a new session for DSOM Protocol. I am your human Lead Architect.
I have the Hibernation Notes from our last session. Please read them carefully
and use them to fully restore our working context.

--- BEGIN HIBERNATION NOTES ---
[PASTE HIBERNATION NOTES BLOCK HERE]
--- END HIBERNATION NOTES ---

After reading the Hibernation Notes above, do the following:

1. Confirm you have read and understood all entries in the Hibernation Notes.
2. State the last Mental Anchor — the exact point where we stopped.
3. Confirm the 4-Tier environment map (T1, T2, T3, T4) and the project identity.
4. List the top 3 tasks from .agents/brain/task.md that are pending.
5. State: "Sovereign State Restored — [PROJECT NAME] is live." to confirm you are ready.

From this point, operate under DSOM Protocol v6.1:
- Advisory Mode only. You propose, I approve, Ansible executes, you verify.
- UK English in all responses and documentation.
- Every change goes to Git before execution.
- Ansible is the only executor for OS-level tasks.
- Wait for my output after each step before proceeding.
```

--------------------------------

### Verify Ansible Connectivity

Source: https://gitlab.com/linuxmalaysia/deep-state-of-mind-for-my-ai/-/blob/main/docs/HOWTO-SETUP-ANSIBLE-BASELINE.md

Commands to test node reachability and execute the pre-flight playbook in check or live modes.

```bash
# Basic connectivity ping

ansible all -m ping -i inventory/hosts.yml

# Run the pre-flight playbook in check mode first

ansible-playbook playbooks/preflight.yml --check

# Run the pre-flight playbook live

ansible-playbook playbooks/preflight.yml
```

--------------------------------

### Execute history rewrite

Source: https://gitlab.com/linuxmalaysia/deep-state-of-mind-for-my-ai/-/blob/main/docs-source/skills/git-history-scrubber.mdx

Run the git-filter-repo tool using the uv package manager to apply the defined replacements to the repository history.

```bash
uv tool run git-filter-repo --replace-text replacements.txt --force
```

--------------------------------

### Define OKF v0.2 Spatial Memory Closet

Source: https://gitlab.com/linuxmalaysia/deep-state-of-mind-for-my-ai/-/blob/main/docs/OKF-ADOPTION-GUIDE.md

Use this template to archive operational state and execution parameters for tooling scripts.

```yaml
---
okf_version: 0.2
type: architecture_concept
title: "Room Tooling Memory Closet"
timestamp: "2026-08-21T20:30:00Z"
description: "Archived operational state and execution parameters for DSOM tooling scripts."
topics: ["tooling", "python", "automation", "memory-palace", "okf"]
resource: "file:///.agents/brain/wings/room_tooling/closet.md"
sources: [".agents/brain/palace_registry.md", "tools/apply_okf_frontmatter.py"]
generated: "google-antigravity"
verified: true
status: "approved"
stale_after: "2026-11-21T00:00:00Z"
---

# Room Tooling Distillation
This closet tracks the execution parameters of `tools/openwiki_emulator.py`, `tools/apply_okf_frontmatter.py`, and `tools/generate_sitemaps.py`.
```

--------------------------------

### Querying Specific Files with #file:

Source: https://gitlab.com/linuxmalaysia/deep-state-of-mind-for-my-ai/-/blob/main/docs/COPILOT-SETUP.md

Use the #file: prefix to attach specific brain artifacts for precise context in Copilot queries.

```text
Based on #file:.agents/brain/task.md and #file:.agents/brain/walkthrough.md,
what is the next logical step for the current implementation phase?
```

--------------------------------

### Operational Lifecycle Rituals

Source: https://gitlab.com/linuxmalaysia/deep-state-of-mind-for-my-ai/-/blob/main/docs/governance/OPERATIONAL-SOVEREIGNTY.md

Illustrates the state machine for the project lifecycle, from Start-of-Day reanimation to End-of-Day hibernation.

```mermaid
stateDiagram-v2
    [*] --> SOD
    SOD: Start-of-Day (Reanimation)
    SOD --> ActiveFlow
    ActiveFlow: Guided by CRISP pillars
    ActiveFlow --> EOD
    EOD: End-of-Day (Hibernation)
    EOD --> [*]
```

--------------------------------

### DSOM Operational Commands

Source: https://gitlab.com/linuxmalaysia/deep-state-of-mind-for-my-ai/-/blob/main/docs-source/tutorials/team-dsom-masterclass.mdx

A collection of essential commands for workspace management, testing, and security verification.

```bash
bash tools/reanimate.sh
```

```powershell
.\tools\reanimate.ps1
```

```bash
uv run --with pytest --with pyyaml --with fastmcp pytest
```

```bash
uv run --with tiktoken python .agents/skills/dsom-token-calculator/scripts/calculate-tokens.py .agents/skills/
```

```bash
uv run python tools/install_git_guardrails.py
```

```powershell
$env:GIT_TERMINAL_PROMPT="0"; $env:GCM_INTERACTIVE="never"; git push origin main; git push gitlab main
```

--------------------------------

### Calculate Token Usage with DSOM Token Calculator

Source: https://gitlab.com/linuxmalaysia/deep-state-of-mind-for-my-ai/-/blob/main/llms-full.txt

Use these commands to scan directories or audit specific files for token breaches. Requires the tiktoken dependency.

```bash
# Scan entire skills directory
uv run --with tiktoken .agents/skills/dsom-token-calculator/scripts/calculate-tokens.py .agents/skills/

# Audit a single file with per-section breach report
uv run --with tiktoken .agents/skills/dsom-token-calculator/scripts/calculate-tokens.py path/to/SKILL.md --sections
```

--------------------------------

### Run Ansible Audit Playbook

Source: https://gitlab.com/linuxmalaysia/deep-state-of-mind-for-my-ai/-/blob/main/docs/tools/HOWTO-AUDIT-PRE-FLIGHT.md

Execute the Ansible-based audit to validate the baseline configuration.

```bash
ansible-playbook playbooks/dsom/audit-preflight.yml

```

--------------------------------

### Run Regression Suite

Source: https://gitlab.com/linuxmalaysia/deep-state-of-mind-for-my-ai/-/blob/main/docs/tools/HOWTO-MCP-SERVER.md

Executes the project's unit tests to ensure system integrity.

```bash
uv run --with pyyaml python -m unittest tests/test_okf_frontmatter_bom_reorder.py tests/test_okf_quoting.py tests/test_seo_sitemaps.py tests/test_docs_symlinks.py
```

--------------------------------

### DSOM Agent Plugin Filesystem Layout

Source: https://gitlab.com/linuxmalaysia/deep-state-of-mind-for-my-ai/-/blob/main/docs/governance/DSOM-AGENT-PLUGINS-SPECIFICATION.md

Standardized directory structure required for all DSOM-compliant agent plugins.

```text
my-dsom-plugin/
├── plugin.json              # Required: Closed manifest (schema 1.0.0)
├── mcp.json                 # Optional: Explicit MCP server descriptors
├── skills/                  # Optional: Fixed directory for Agent Skills
│   └── summarize/
│       ├── SKILL.md         # Open Knowledge Format (OKF v0.2)
│       ├── scripts/         # Idempotent execution scripts
│       └── references/      # Progressive disclosure documentation
├── org.dsom.protocol/       # Optional: Client extension namespace
│   ├── guardrails.json      # DSOM custom validators configuration
│   └── hooks/               # Tri-Phasic interception hooks
├── LICENSE                  # GNU GPL v3.0 / SPDX identifier
└── CHANGELOG.md             # Semantic version ledger
```

--------------------------------

### Daily Loop Workflow

Source: https://gitlab.com/linuxmalaysia/deep-state-of-mind-for-my-ai/-/blob/main/docs-source/governance/ai-cognitive-twin-protocol.mdx

Visual representation of the daily operational loop between human, Ansible automation, and AI.

```text
SOD
 └── ansible-playbook sod-palace.yml    (T2 auto)
       └── Upload manifest + handshake       (Human manual)
             └── AI reads Palace Registry    (AI auto)

Active Work
 └── Code, commit, push                 (Human)
 └── Log key decisions immediately      (Human+AI — Decision Log Protocol)

EOD
 └── ansible-playbook eod-palace.yml    (T2 auto)
       └── Review palace_update_proposal     (Human+AI)
             └── Update closets + commit     (Human+AI)
```

--------------------------------

### Execute sod-palace on Windows and Linux

Source: https://gitlab.com/linuxmalaysia/deep-state-of-mind-for-my-ai/-/blob/main/docs-source/tools/howto-sod-palace.mdx

Run the appropriate script for your platform to initiate the SOD ritual.

```powershell

# Windows (T1)

.\tools\sod-palace.ps1

```

```bash

# WSL2 / Linux (T2)

bash tools/sod-palace.sh

```

--------------------------------

### Manual PowerShell Scaffolding Commands

Source: https://gitlab.com/linuxmalaysia/deep-state-of-mind-for-my-ai/-/blob/main/docs-source/skills/dsom-project-cloner.mdx

Execute these PowerShell commands to manually replicate the DSOM directory structure and files from a base path to a target path.

```powershell
New-Item -ItemType Directory -Force -Path "$TARGET\.agents\brain", "$TARGET\.agents\skills", "$TARGET\.github", "$TARGET\docs\governance", "$TARGET\tools"
Copy-Item "$DSOM_BASE\AGENTS.md", "$DSOM_BASE\.cursorrules", "$DSOM_BASE\CLAUDE.md", "$DSOM_BASE\START-HERE.md" "$TARGET\"
Copy-Item "$DSOM_BASE\.github\copilot-instructions.md" "$TARGET\.github\"
Copy-Item "$DSOM_BASE\.agents\AGENTS.md" "$TARGET\.agents\"
Copy-Item -Recurse "$DSOM_BASE\tools\*" "$TARGET\tools\"
Copy-Item -Recurse "$DSOM_BASE\.agents\skills\dsom-token-calculator", "$DSOM_BASE\.agents\skills\dsom-signature-injector", "$DSOM_BASE\.agents\skills\initialize-gitops" "$TARGET\.agents\skills\"
Set-Location $TARGET
bash tools/init-brain.sh
```

--------------------------------

### Perform Physical Reality Check Audit

Source: https://gitlab.com/linuxmalaysia/deep-state-of-mind-for-my-ai/-/blob/main/docs/governance/OPERATIONAL-GUIDE.md

Verifies the physical environment state before AI initialization.

```bash
# Linux / WSL2

./tools/audit-pre-flight.sh

# Windows

.\tools\audit-pre-flight.ps1
```

--------------------------------

### Define OKF v0.2 Governance Blueprint

Source: https://gitlab.com/linuxmalaysia/deep-state-of-mind-for-my-ai/-/blob/main/docs/OKF-ADOPTION-GUIDE.md

Use this template to codify zero-trust security policies for autonomous AI subagents.

```yaml
---
okf_version: 0.2
type: governance_protocol
title: "Zero-Trust Agent Hardening Policy"
timestamp: "2026-08-21T20:00:00Z"
topics: ["security", "zero-trust", "agent-hardening", "dsom", "okf"]
description: "Codifies strict zero-trust sandbox rules and least-privilege token access for autonomous AI subagents."
resource: "file:///docs/governance/SAMPLE-POLICY.md"
sources: ["docs/governance/AI-MASTER-PROTOCOL.md", "https://csrc.nist.gov/publications/detail/sp/800-207/final"]
generated: "google-antigravity"
verified: true
status: "approved"
stale_after: "2027-08-21T00:00:00Z"
---

# 🛡️ Zero-Trust Agent Hardening Policy

## Policy Enforcement
Autonomous subagents must operate under isolated Git worktrees and have read-only access to root credentials.
```

--------------------------------

### Define Agent Skill Metadata

Source: https://gitlab.com/linuxmalaysia/deep-state-of-mind-for-my-ai/-/blob/main/references/Optimizing OKF for AI Agents - Deep Research - 20260704.md

Standardizes capability files using YAML frontmatter to allow agents to discover prerequisites and dependencies efficiently.

```YAML
---
okf_version: "0.1"
type: agent_skill
title: audit-cluster-health
description: Executes the Sovereign Pulse Audit script to extract deep telemetry.
resource: https://github.com/linuxmalaysia/deep-state-of-mind-for-my-ai
tags: [infrastructure, telemetry, health-check]
timestamp: 2026-06-22T12:09:40Z
---# Skill InstructionsThis skill provides the execution steps for extracting telemetry logs.
```

--------------------------------

### Configure Claude Desktop for MCP

Source: https://gitlab.com/linuxmalaysia/deep-state-of-mind-for-my-ai/-/blob/main/docs-source/ai-setup/mcp-server.mdx

Add this JSON block to your Claude Desktop configuration file to register the DSOM MCP server.

```json
{
  "mcpServers": {
    "dsom-palace": {
      "command": "uv",
      "args": [
        "run",
        "--with", "mcp",
        "tools/mcp/server.py"
      ],
      "env": {
        "DSOM_ROOT": "/absolute/path/to/your/dsom/project"
      }
    }
  }
}
```

--------------------------------

### Create Canonical plugin.json

Source: https://gitlab.com/linuxmalaysia/deep-state-of-mind-for-my-ai/-/blob/main/docs-source/skills/agent-plugin-packager.mdx

Defines the plugin metadata according to the 1.0.0 schema.

```json
{
  "$schema": "https://agent-plugins.org/schemas/1.0.0/plugin.schema.json",
  "name": "dsom-sysops-plugin",
  "version": "1.0.0",
  "description": "System operations and diagnostic skills for AI Agents.",
  "keywords": ["sysops", "diagnostics", "dsom"]
}
```

--------------------------------

### Current State Template

Source: https://gitlab.com/linuxmalaysia/deep-state-of-mind-for-my-ai/-/blob/main/docs/HUMAN-HANDOVER-CONTEXT.md

Template for documenting the system state at the end of each session.

```text

CURRENT STATE: [DESCRIBE_CURRENT_STATE e.g. v1.0 — Ansible baseline deployed, Kafka brokers up]
Environment:   [e.g. 3-node test cluster]
Last Commit:   [PASTE LAST COMMIT HASH AND MESSAGE]
Mental Anchor: [ONE SENTENCE — exact state and where to resume]
Next Action:   [WHAT THE AI SHOULD DO FIRST IN THE NEW SESSION]

```

--------------------------------

### Verify Ansible Palace Automation

Source: https://gitlab.com/linuxmalaysia/deep-state-of-mind-for-my-ai/-/blob/main/docs/HOWTO-UPGRADE-DSOM.md

Test the SOD and EOD playbooks for the Palace system.

```bash
# Test SOD
bash tools/sod-palace.sh  # (Windows: .\tools\sod-palace.ps1)
# Test EOD
bash tools/eod-palace.sh  # (Windows: .\tools\eod-palace.ps1)
```

--------------------------------

### Verify compliance output

Source: https://gitlab.com/linuxmalaysia/deep-state-of-mind-for-my-ai/-/blob/main/docs/how-to/audit-and-apply-frontmatter.md

Executes the test suite to confirm successful modifications and ensure no Byte Order Marks remain.

```bash
uv run --with pyyaml --with pytest pytest tests/test_okf_frontmatter_bom_reorder.py
```

--------------------------------

### Create requirements.txt for Python dependencies

Source: https://gitlab.com/linuxmalaysia/deep-state-of-mind-for-my-ai/-/blob/main/docs-source/skills/github-actions-snyk-scanner.mdx

Generates a requirements file to enable Snyk dependency tracking for Python projects.

```bash
# Create requirements.txt listing all external Python dependencies
# Example for DSOM tools (dsom_token_auditor.py uses tiktoken):
echo "tiktoken>=0.7.0" > requirements.txt
git add requirements.txt
git commit -m "chore(deps): add requirements.txt for Python dependency tracking and Snyk scanning"
```

--------------------------------

### Define the Skills Directory Structure

Source: https://gitlab.com/linuxmalaysia/deep-state-of-mind-for-my-ai/-/blob/main/docs-source/skills/overview.mdx

The required file system layout for organizing custom and pre-built skills within a repository.

```text
.agents/skills/
├── dsom-bootstrap/
│   └── SKILL.md
├── eod-palace-sync/
│   └── SKILL.md
├── sod-palace-sync/
│   └── SKILL.md
└── <your-custom-skill>/
    └── SKILL.md
```

--------------------------------

### Execute DSOM Signature Injector

Source: https://gitlab.com/linuxmalaysia/deep-state-of-mind-for-my-ai/-/blob/main/docs-source/skills/dsom-signature-injector.mdx

Run the Python script to sign a specific file or directory.

```bash
python .agents/skills/dsom-signature-injector/scripts/inject.py <target_path>
```

--------------------------------

### Set Mental Anchor Template

Source: https://gitlab.com/linuxmalaysia/deep-state-of-mind-for-my-ai/-/blob/main/docs/EOD-RITUAL.md

Template for documenting the resume point for the next session.

```text
TOMORROW: [ONE SENTENCE — exactly where to resume, what to run first]
DATE: [YYYY-MM-DD]
PROJECT: [PROJECT NAME]
NEXT ACTION: [e.g. "Run ansible-playbook playbooks/dsom/audit-preflight.yml after SOD pull"]
```

--------------------------------

### Visualize DSOM Architecture with Mermaid

Source: https://gitlab.com/linuxmalaysia/deep-state-of-mind-for-my-ai/-/blob/main/docs-source/governance/operational-sovereignty.mdx

A flowchart representing the operational sovereignty framework, including the CRISP hierarchy, CAPM integration, and technical automation components.

```mermaid
flowchart TD
    %% Root
    DSOM[Operational Sovereignty through Metacognitive Governance]

    %% 1. Theoretical Framework
    DSOM --> Sovereignty[1. Theoretical Framework of Operational Sovereignty]
    Sovereignty --> Who[Who: Lead Architect + AI Twin]
    Sovereignty --> What[What: Clean Architecture + CRISP Strategy]
    Sovereignty --> When[When: SOD/EOD + Sunday Audit]
    Sovereignty --> Where[Where: .agents/brain/ artifacts]
    Sovereignty --> Why[Why: Sovereign Portability]
    Sovereignty --> How[How: Git Hygiene + Handshake]

    %% 2. CRISP² Hierarchy
    DSOM --> CRISP[2. CRISP² Methodological Hierarchy]
    CRISP --> L1[L1: Phases → Rituals of Transition]
    CRISP --> L2[L2: Generic Tasks → CRISP Mandate]
    CRISP --> L3[L3: Specialised Tasks → Clean Architecture Layers]
    CRISP --> L4[L4: Process Instances → Mental Anchors]

    %% 3. CAPM Integration
    DSOM --> CAPM[3. Integrating CAPM Knowledge Areas]
    CAPM --> Integration[Integration & Scope Governance]
    CAPM --> Schedule[Schedule & Cost Control (CPI/SPI)]
    CAPM --> Quality[Quality Management → Pre-flight Audits + LDP Standards]

    %% 4. DSOM Brain (SKMS)
    DSOM --> Brain[4. Anatomy of the DSOM Brain (SKMS)]
    Brain --> Eternal[Eternal: AI-MASTER-PROTOCOL.md]
    Brain --> Future[Future: implementation_plan.md]
    Brain --> Present[Present: task.md]
    Brain --> Past[Past: walkthrough.md]

    %% 5. Operational Rituals
    DSOM --> Rituals[5. Operational Rituals of Lifecycle]
    Rituals --> SODR[SOD: Reanimation + Audit]
    Rituals --> Active[Active Flow: CRISP pillars + Twin enforcement]
    Rituals --> EODR[EOD: Hibernation + Sovereign Save]

    %% 6. Technical Enforcement
    DSOM --> Automation[6. Technical Enforcement & Automation]
    Automation --> Init[init-brain.sh]
    Automation --> AuditScript[audit-pre-flight.sh]
    Automation --> ReanimateScript[reanimate.sh]
    Automation --> Privacy[privacy-guardian.sh]

    %% 7. Linguistic Sovereignty
    DSOM --> Language[7. Linguistic Sovereignty]
    Language --> UK[UK English enforced]
    Language --> Malay[DBP-standard Malay (Piawai)]
    Language --> NoColloq[No Colloquialisms]
    Language --> NoLoan[No Indonesian Loanwords]
```

--------------------------------

### Back Up Brain Directory

Source: https://gitlab.com/linuxmalaysia/deep-state-of-mind-for-my-ai/-/blob/main/docs-source/palace/migration.mdx

Create a dated backup of the brain directory before performing migration steps.

```bash
cp -r .agents/brain .agents/brain-backup-$(date +%Y%m%d)
```

--------------------------------

### Execute Slide Compiler

Source: https://gitlab.com/linuxmalaysia/deep-state-of-mind-for-my-ai/-/blob/main/docs-source/skills/odp-slide-generator.mdx

Runs the slide compiler script using uv to process the markdown file into a presentation.

```powershell
uv run --with python-pptx python tools/compile_3col_slides.py docs/slide_data.md docs/my_presentation.pptx
```

--------------------------------

### Confirm AI Readiness Statement

Source: https://gitlab.com/linuxmalaysia/deep-state-of-mind-for-my-ai/-/blob/main/docs-source/rituals/start-of-day.mdx

The required confirmation phrase the AI must output to signal successful context loading.

```text
Sovereign State Restored — [PROJECT NAME] is live.
```

--------------------------------

### Configure Declarative Guardrails in SKILL.md

Source: https://gitlab.com/linuxmalaysia/deep-state-of-mind-for-my-ai/-/blob/main/llms-full.txt

Defines operational constraints and metadata for the token calculator skill using OKF version 0.1.

```markdown
---
okf_version: 0.1
type: procedural_skill
title: "Procedural Specification: DSOM Token Calculator"
description: "Calculates token counts via tiktoken in isolated uv Python runspaces."
resource: "file:///.agents/skills/dsom-token-calculator/SKILL.md"
timestamp: 2026-07-18T14:52:39Z
---

# Operational Enforcements:
- Trigger this skill dynamically before any output loop that handles extensive datasets or raw configurations.
- If payload calculations return totals greater than **4000 tokens**, the system must block raw screen serialization and switch to targeted `view_file` calls or chunked reading.
```

--------------------------------

### OKF-Compliant Skill Template

Source: https://gitlab.com/linuxmalaysia/deep-state-of-mind-for-my-ai/-/blob/main/docs-source/skills/creating-skills.mdx

A YAML-based template defining the metadata and configuration for an OKF skill.

```yaml
---
okf_version: 0.1
type: skill
title: "Deploy HAProxy to Production"
timestamp: "2026-07-15T10:00:00Z"
topics: ["haproxy", "deploy", "infrastructure", "ansible", "production"]
description: "Use this skill when the user asks to deploy, reconfigure, or health-check HAProxy on any node in the production or staging environment."
name: deploy-haproxy
---
```

--------------------------------

### Pull Latest Tools

Source: https://gitlab.com/linuxmalaysia/deep-state-of-mind-for-my-ai/-/blob/main/docs/HOWTO-MIGRATE-TO-PALACE.md

Update the local repository and verify the presence of the palace tag.

```bash
# Linux / WSL2

git pull origin main

# Windows

git pull origin main
```

```bash
git tag | grep palace

# Expected: v10.0.0-palace
```

--------------------------------

### Run CheckUsage Snapshot

Source: https://gitlab.com/linuxmalaysia/deep-state-of-mind-for-my-ai/-/blob/main/docs/tools/HOWTO-CHECKUSAGE.md

Executes the script once to display the current state of all sessions.

```powershell
.\tools\CheckUsage.ps1
```

--------------------------------

### Query metadata from terminal

Source: https://gitlab.com/linuxmalaysia/deep-state-of-mind-for-my-ai/-/blob/main/docs/how-to/use-openwiki-emulator.md

Performs fast frontmatter variable checks on compiled wiki pages using the search command.

```bash
uv run --with pyyaml python tools/openwiki_emulator.py --search "ansible"
```

--------------------------------

### Generate Manifest Injection

Source: https://gitlab.com/linuxmalaysia/deep-state-of-mind-for-my-ai/-/blob/main/docs/governance/OPERATIONAL-GUIDE.md

Aggregates project context into a single truth file for AI ingestion.

```bash
# Linux

./tools/reanimate.sh

# Windows

.\tools\reanimate.ps1
```

--------------------------------

### Run Regression Verification Tests

Source: https://gitlab.com/linuxmalaysia/deep-state-of-mind-for-my-ai/-/blob/main/docs-source/skills/openwiki-compiler.mdx

Executes unit test assertions to verify the integrity of the OpenWiki emulator and update workflows.

```bash
uv run --with pyyaml python -m unittest tests/test_openwiki_emulator.py tests/test_openwiki_update_workflow.py tests/test_okf_frontmatter_bom_reorder.py
```

--------------------------------

### Decision Log Protocol Prompt

Source: https://gitlab.com/linuxmalaysia/deep-state-of-mind-for-my-ai/-/blob/main/docs-source/rituals/session-handover.mdx

Standardized prompt for capturing architectural decisions and rejected alternatives into the project's permanent documentation.

```text
"Log this decision to walkthrough.md:
Decision: [what] | Alternatives rejected: [what and why] | Reason: [why this]"
```

--------------------------------

### Recovery Procedure

Source: https://gitlab.com/linuxmalaysia/deep-state-of-mind-for-my-ai/-/blob/main/docs-source/skills/mintlify-docs-compiler.mdx

Steps to revert a corrupted downstream repository state.

```bash
git clone https://github.com/linuxmalaysia/my-knowledge-brain.git
cd my-knowledge-brain
git log --oneline -n 5
git revert <bad-commit-sha>
git push origin main
```

--------------------------------

### Standard Ansible Project Directory Structure

Source: https://gitlab.com/linuxmalaysia/deep-state-of-mind-for-my-ai/-/blob/main/docs/governance/GITOPS-AIOPS-ANSIBLE-STRATEGY.md

The mandatory directory layout for all DSOM projects utilizing Ansible to ensure consistency and maintainability.

```text
[project-root]/
├── ansible.cfg                    # Ansible configuration
├── inventory/
│   ├── hosts.yml                  # Node inventory (Git-tracked, no secrets)
│   ├── group_vars/
│   │   ├── all.yml                # Variables for all hosts
│   │   └── [group_name].yml       # Group-specific variables
│   └── host_vars/
│       └── [hostname].yml         # Host-specific variables
├── playbooks/
│   ├── site.yml                   # Master playbook (entry point)
│   ├── deploy-[component].yml     # Component-specific playbooks
│   └── preflight.yml              # Pre-flight verification playbook
├── roles/
│   └── [role_name]/
│       ├── tasks/main.yml
│       ├── handlers/main.yml
│       ├── templates/
│       ├── files/
│       ├── vars/main.yml
│       └── defaults/main.yml
└── vault/
    └── .gitignore                 # Vault files NEVER committed to Git
```

--------------------------------

### Execute Claude-optimised reanimate script

Source: https://gitlab.com/linuxmalaysia/deep-state-of-mind-for-my-ai/-/blob/main/docs-source/tools/reanimate.mdx

Run the Claude-specific variant to generate a token-compressed manifest for Anthropic Claude.

```bash
bash tools/reanimate-claude.sh
```

```powershell
.\tools\reanimate-claude.ps1
```

--------------------------------

### Configure SSH Routing Map

Source: https://gitlab.com/linuxmalaysia/deep-state-of-mind-for-my-ai/-/blob/main/references/The Sovereign AI Agent Workspace_ Architecting Persistent Memory, Custom Skills, and Contextual Continuity - 20260612.md

Define jump host and target server aliases in the local SSH configuration file to enable ProxyJump functionality.

```text
Host <jump_host_alias>
    HostName <jump_host_ip>
    User <jump_host_user>
    IdentityFile ~/.ssh/<local-key-for-jumphost>

Host <target_alias>
    HostName <target_ip>
    User <target_user>
    IdentityFile ~/.ssh/<local-alias-for-key>
    ProxyJump <jump_host_alias>
```

--------------------------------

### Project Directory Structure

Source: https://gitlab.com/linuxmalaysia/deep-state-of-mind-for-my-ai/-/blob/main/docs/reference-architectures/ANSIBLE-CONFIG-GUIDE.md

The standard directory layout for the 16-node Elastic SOC fabric, including configuration, playbooks, roles, and operational tools.

```text
. 
├── ansible.cfg                # Project performance tuning
├── playbooks/
│   └── site.yml               # Master Orchestrator (Rootful Control)
├── inventory/
│   └── hosts.yml              # 16-Node Backbone & IP Mapping
├── roles/
│   └── setup_os/              # Foundation: OS Hardening, Dynamic Buffers, UFW, Lynis 3.0.9+
├── .agents/
│   ├── brain/                 # Sovereign AI Cognitive State (Palace)
│   └── skills/                # AI Agent Skills (Audit, Health, Backup)
│       ├── audit-node-security/
│       └── node-health-check/
└── tools/                     # Operational Lifecycle Tools
    ├── sysadmin-vm-prep.sh    # Dual-Stage VM Bootstrapper (Bash)
    ├── eod-palace.ps1         # End-of-Day GitOps Hibernation
    └── sod-palace.ps1         # Start-of-Day AI Reanimation
```

--------------------------------

### Repository Directory Tree

Source: https://gitlab.com/linuxmalaysia/deep-state-of-mind-for-my-ai/-/blob/main/README.md

Visual representation of the project's file system hierarchy.

```text
deep-state-of-mind-for-my-ai/
│
├── AGENTS.md                              # 🤖 Root Agent Gateway (Jules/Copilot/Cursor entry point)
│
├── docs/                                  # Governance & Protocol Documents
│   ├── tools-and-automation/              # 🛠️ Detailed script documentation (OKF Compliant)
│   ├── reference-architectures/           # 🏗️ Ansible architecture blueprints
│   ├── AI-MASTER-PROTOCOL.md              # The Sovereign Constitution (v6.1 + Palace v1.0)
│   ├── AI-COGNITIVE-TWIN-PROTOCOL.md      # ⭐ Project Identity Card Template
│   ├── GITOPS-AIOPS-ANSIBLE-STRATEGY.md   # Three-Pillar Doctrine
│   ├── HOWTO-SETUP-ANSIBLE-BASELINE.md    # Ansible setup guide (LDP standard)
│   ├── OPERATIONAL-GUIDE.md               # How-to for daily rituals
│   ├── REANIMATION-PROMPT-TEMPLATE.md     # AI session prompt templates
│   └── ...
│
├── .agents/                               # 🤖 Sovereign AI Workspace (v2)
│   ├── AGENTS.md                          # 📜 Core Rulebook mapping AI rules to memory
│   ├── skills/                            # 🛠️ Self-Healing Agent Skills (OKF)
│   ├── workflows/                         # 🔄 Procedural Workflow chains
│   └── brain/                             # 🧠 The AI's External Memory (SSoT)
│       ├── task.md                        # Present: What to do NOW
│       ├── walkthrough.md                 # Past: Session history & Mental Anchors
│       ├── implementation_plan.md         # Future: Project roadmap
│       ├── palace_registry.md             # 🏛️ Palace spatial index (OKF Compliant)
│       └── wings/                         # 🏛️ Sovereign Markdown Palace Rooms
│
├── references/                            # 📚 External PDFs, Genesis papers, and benchmarks
│
├── tools/                                 # Automation Scripts
│   ├── audit-pre-flight.sh / .ps1         # ✅ Run first every session
│   ├── git-ritual.sh / .ps1               # 🔄 SOD pull + EOD commit/push
│   ├── reanimate.sh / .ps1                # 🚀 Generate AI context manifest (v2.2/v2.1)
│   ├── hibernation.sh / .ps1              # 🌙 EOD safety check + Palace Sync (v2.1)
│   ├── palace-sync.sh / .ps1              # 🏛️ Map git commits → Palace Rooms (v1.0)
│   ├── init-brain.sh / .ps1               # 🧠 One-time brain initialisation
│   └── privacy-guardian.sh / .ps1         # 🛡️ Scan manifest before sharing
│
├── inventory/                             # Ansible Node Topology (your project)
├── playbooks/                             # Ansible Playbooks (your project)
└── roles/                                 # Ansible Roles (your project)
```

--------------------------------

### Inject DSOM Signatures

Source: https://gitlab.com/linuxmalaysia/deep-state-of-mind-for-my-ai/-/blob/main/docs-source/skills/dsom-policy-adopter.mdx

Appends standard DSOM ownership, timestamp, and GPL v3.0 license signatures to governance files.

```bash
python .agents/skills/dsom-signature-injector/scripts/inject.py docs/governance/
```

--------------------------------

### Execute Scripts with uv

Source: https://gitlab.com/linuxmalaysia/deep-state-of-mind-for-my-ai/-/blob/main/llms-full.txt

Commands for running scripts with automatic interpreter management and version specification.

```bash
uv run verify.py
```

```bash
uv run --python 3.10 verify.py
```

--------------------------------

### Backup Brain Artifacts

Source: https://gitlab.com/linuxmalaysia/deep-state-of-mind-for-my-ai/-/blob/main/docs/HOWTO-UPGRADE-DSOM.md

Create a timestamped backup of the brain directory before performing upgrades.

```bash
cp -r .agents/brain .agents/brain_backup_$(date +%F)
```

--------------------------------

### Run the SOD Pre-Flight Check

Source: https://gitlab.com/linuxmalaysia/deep-state-of-mind-for-my-ai/-/blob/main/docs-source/ai-setup/copilot.mdx

Execute the environment validation script to ensure all system checks pass before initiating the handshake.

```bash
bash tools/sod-palace.sh
```

```powershell
.\tools\sod-palace.ps1
```

--------------------------------

### Instantiate Guard and Validate LLM Output

Source: https://gitlab.com/linuxmalaysia/deep-state-of-mind-for-my-ai/-/blob/main/docs/governance/AI-GUARDRAILS-MASTER-GUIDE.md

Initialize a Guard instance with the custom validator and execute validation on raw LLM response strings.

```python
from guardrails import Guard
from tools.validators.guardrails_ai_okf_validator import GuardrailsOKFValidator

# Create a Guard instance with on_fail='fix'
guard = Guard().use(GuardrailsOKFValidator(on_fail="fix"))

raw_llm_response = "\ufeff---\nokf_version: 0.2\ntitle: Example\n---\n# Content"
outcome = guard.validate(raw_llm_response)

print("Validated Output:\n", outcome.validated_output)
```

--------------------------------

### Run Snapshot Check

Source: https://gitlab.com/linuxmalaysia/deep-state-of-mind-for-my-ai/-/blob/main/docs-source/tools/howto-checkusage.mdx

Executes a single check of all session states and exits immediately.

```powershell
.\tools\CheckUsage.ps1
```

--------------------------------

### Architecture Diagram

Source: https://gitlab.com/linuxmalaysia/deep-state-of-mind-for-my-ai/-/blob/main/docs/HOWTO-SETUP-WSL-ALMALINUX10.md

Visual representation of the DSOM architecture layers from Windows 11 down to target nodes.

```text

[ Tier 1: Windows 11 + PowerShell ]  ← You work here
        ↓  wsl -d dsom-control-almalinux10
[ Tier 2: AlmaLinux 10 WSL2        ]  ← This setup guide
  Identity: dsom-admin (UID 2001)
  Tools:    Ansible, Git, rsync
        ↓  SSH via ansible-playbook
[ Tier 3/4: Target Nodes            ]  ← Remote Linux servers

```

--------------------------------

### Execute Compilation Command

Source: https://gitlab.com/linuxmalaysia/deep-state-of-mind-for-my-ai/-/blob/main/docs-source/skills/proposal-docx-formatter.mdx

Use the uv run command to execute the compiler script with the necessary python-docx dependency.

```powershell
uv run --with python-docx python tools/compile_proposal.py docs/proposal.md docs/proposal.docx
```

--------------------------------

### Token Estimation Formula

Source: https://gitlab.com/linuxmalaysia/deep-state-of-mind-for-my-ai/-/blob/main/docs/tools/HOWTO-CHECKUSAGE.md

Calculation methodology for estimating token counts based on file size.

```text
1 MB  =  ~250,000 characters  (binary file, predominantly text)
1 token ≈ 4 characters         (Gemini / GPT average)
∴ 1 MB ≈ 62,500 tokens
```

--------------------------------

### Perform Semantic Search with OpenWiki

Source: https://gitlab.com/linuxmalaysia/deep-state-of-mind-for-my-ai/-/blob/main/docs-source/tools/openwiki-overview.mdx

Executes a semantic search for a specific term across indexed file metadata using the OpenWiki emulator.

```bash
uv run --with pyyaml python tools/openwiki_emulator.py --search "Ansible"
```

--------------------------------

### Create Group Variables

Source: https://gitlab.com/linuxmalaysia/deep-state-of-mind-for-my-ai/-/blob/main/docs/HOWTO-SETUP-ANSIBLE-BASELINE.md

Defines baseline variables for all hosts and environment-specific overrides for production.

```yaml
cat > inventory/group_vars/all.yml << 'EOF'
---

# Variables applied to ALL hosts

project_name:   "[YOUR_PROJECT_NAME]"
project_path:   "/opt/[YOUR_PROJECT_NAME]"
deploy_user:    "[YOUR_DEPLOY_USER]"
deploy_uid:    "[YOUR_UID]"          # e.g., 1000 (dev) or 2001 (prod)
EOF

cat > inventory/group_vars/production.yml << 'EOF'
---

# Production-specific overrides

deploy_uid:   "[PROD_UID]"            # e.g., 2001
project_path: "/opt/[YOUR_PROJECT_NAME]-prod"

# Vault-injected variables — DO NOT put secrets here
# credentials: "{{ vault_production_credentials }}"

EOF

```

--------------------------------

### Calculate Tokens with Token Calculator Skill

Source: https://gitlab.com/linuxmalaysia/deep-state-of-mind-for-my-ai/-/blob/main/docs-source/governance/dsom-token-performance-playbook.mdx

Use these commands to scan directories or audit specific files for token usage. Requires the tiktoken dependency.

```bash
uv run --with tiktoken .agents/skills/dsom-token-calculator/scripts/calculate-tokens.py .agents/skills/
```

```bash
uv run --with tiktoken .agents/skills/dsom-token-calculator/scripts/calculate-tokens.py path/to/SKILL.md --sections
```

--------------------------------

### Enable WSL2 Features

Source: https://gitlab.com/linuxmalaysia/deep-state-of-mind-for-my-ai/-/blob/main/docs/HOWTO-SETUP-WSL-ALMALINUX10.md

PowerShell commands to enable the Windows Subsystem for Linux and Virtual Machine Platform features, and set the default WSL version to 2.

```powershell

# Enable WSL and Virtual Machine Platform

dism.exe /online /enable-feature /featurename:Microsoft-Windows-Subsystem-Linux /all /norestart
dism.exe /online /enable-feature /featurename:VirtualMachinePlatform /all /norestart

# Restart Windows, then continue:

wsl --set-default-version 2
wsl --update

```

--------------------------------

### Execute Git Ritual Pull

Source: https://gitlab.com/linuxmalaysia/deep-state-of-mind-for-my-ai/-/blob/main/llms-full.txt

Synchronize the local repository with the remote state before beginning work.

```powershell
.\tools\git-ritual.ps1 sod
```

```bash
./tools/git-ritual.sh sod
```

--------------------------------

### Reanimate Workspace

Source: https://gitlab.com/linuxmalaysia/deep-state-of-mind-for-my-ai/-/blob/main/docs/tutorials/TEAM-DSOM-MASTERCLASS.md

Commands to reset or reinitialize the development environment.

```bash
bash tools/reanimate.sh
```

```powershell
.\tools\reanimate.ps1
```

--------------------------------

### Verify Ansible Connectivity

Source: https://gitlab.com/linuxmalaysia/deep-state-of-mind-for-my-ai/-/blob/main/docs/governance/GITOPS-AIOPS-ANSIBLE-STRATEGY.md

Ping all nodes defined in the inventory file to verify connectivity.

```bash
ansible all -m ping -i inventory/hosts.yml
```

--------------------------------

### Audit Pre-flight Log Generation

Source: https://gitlab.com/linuxmalaysia/deep-state-of-mind-for-my-ai/-/blob/main/CONTRIBUTING.md

Use this command to execute the audit script and capture the output to a log file for review.

```bash
bash tools/audit-pre-flight.sh | tee audit.log
```

--------------------------------

### Audit and Logging Commands

Source: https://gitlab.com/linuxmalaysia/deep-state-of-mind-for-my-ai/-/blob/main/docs-source/rituals/human-handover.mdx

Commands to capture terminal and Ansible output for AI review.

```bash
bash tools/audit-pre-flight.sh | tee audit_result.log
```

```bash
ansible-playbook playbooks/dsom/audit-preflight.yml -i localhost, | tee audit_ansible.log
```

--------------------------------

### Daily Loop Workflow Diagram

Source: https://gitlab.com/linuxmalaysia/deep-state-of-mind-for-my-ai/-/blob/main/llms-full.txt

Visual representation of the full daily loop including automated and manual steps.

```text
SOD
 └── ansible-playbook sod-palace.yml    (T2 auto)
       └── Upload manifest + handshake       (Human manual)
             └── AI reads Palace Registry    (AI auto)

Active Work
 └── Code, commit, push                 (Human)
 └── Log key decisions immediately      (Human+AI — Decision Log Protocol)

EOD
 └── ansible-playbook eod-palace.yml    (T2 auto)
       └── Review palace_update_proposal     (Human+AI)
             └── Update closets + commit     (Human+AI)
```

--------------------------------

### Verify script versions on Linux

Source: https://gitlab.com/linuxmalaysia/deep-state-of-mind-for-my-ai/-/blob/main/docs/HOWTO-MIGRATE-TO-PALACE.md

Use grep to check the version strings in the specified shell scripts.

```bash
grep "^VERSION\|^v2\." tools/hibernation.sh tools/reanimate.sh tools/palace-sync.sh
```

--------------------------------

### Execute Sovereign Save

Source: https://gitlab.com/linuxmalaysia/deep-state-of-mind-for-my-ai/-/blob/main/docs/CLAUDE-SETUP.md

Commands to perform end-of-day repository synchronization and state saving.

```bash

# Recommended on T2

bash tools/eod-palace.sh  # (Windows: .\tools\eod-palace.ps1)

# Or manual fallback

./tools/git-ritual.sh          # WSL2 — guided EOD commit and push

```

--------------------------------

### Ansible Execution Standards

Source: https://gitlab.com/linuxmalaysia/deep-state-of-mind-for-my-ai/-/blob/main/docs/RITUAL-OF-TRANSITION.md

Comparison of correct Ansible-based workflows versus prohibited manual node management practices.

```bash
ansible-playbook playbooks/dsom/[name].yml -i inventory/hosts.yml
ansible-lint playbooks/dsom/[name].yml
--check
```

```bash
ssh root@node "systemctl restart service"
ansible -m shell -a 'cmd'
```

--------------------------------

### Inject Ownership Signatures

Source: https://gitlab.com/linuxmalaysia/deep-state-of-mind-for-my-ai/-/blob/main/docs-source/skills/dsom-knowledge-ingester.mdx

Appends standard GPL v3.0 ownership signatures to the specified directory.

```bash
python .agents/skills/dsom-signature-injector/scripts/inject.py <directory>
```

--------------------------------

### Define OKF Directory Structure

Source: https://gitlab.com/linuxmalaysia/deep-state-of-mind-for-my-ai/-/blob/main/docs/OKF-ADOPTION-GUIDE.md

Organize knowledge assets into brain and skills directories alongside source code.

```text
.agents/
├── brain/                   <-- Spatial Memory Palace (okf_version: 0.1 / 0.2)
│   ├── index.md             <-- Directory Router
│   ├── log.md               <-- Chronological Update Ledger
│   └── wings/               <-- Domain Closets
└── skills/                  <-- Executable Agent Capabilities (type: agent_skill)
    ├── index.md
    └── okf-frontmatter-injector/
        └── SKILL.md
```

--------------------------------

### Minimal OKF YAML Header

Source: https://gitlab.com/linuxmalaysia/deep-state-of-mind-for-my-ai/-/blob/main/docs-source/core-concepts.mdx

The standard YAML frontmatter structure required for all DSOM documentation files to enable efficient AI indexing.

```yaml
---
okf_version: 0.1
type: documentation
title: "Your Document Title"
timestamp: "2026-01-15T09:00:00Z"
topics: ["dsom", "governance"]
description: "A one-sentence description of what this file contains."
resource: "file:///docs/your-file.md"
---
```

--------------------------------

### Cognitive Twin Handover Prompt

Source: https://gitlab.com/linuxmalaysia/deep-state-of-mind-for-my-ai/-/blob/main/docs/REANIMATION-PROMPT-TEMPLATE.md

Use this prompt when migrating session context between different AI models.

```text

[DSOM COGNITIVE TWIN HANDOVER — v6.1]

I am migrating from a previous AI session to this one. I will provide you with
the full exported context (Hibernation Notes) from my last session.

You are now the Cognitive Digital Twin of [YOUR_NAME] (Senior Systems Architect).
Read the context below and adopt it fully as your working memory.

--- BEGIN CONTEXT EXPORT ---
[PASTE HIBERNATION NOTES FROM PREVIOUS SESSION]
--- END CONTEXT EXPORT ---

After reading, confirm:
1. The last Mental Anchor — exact point where we stopped.
2. The 4-Tier environment map (T1: Windows, T2: WSL2 dsom-control-almalinux10,
   T3: Jump Host, T4: Production fabric).
3. The project identity and sovereign identity (dsom-admin, UID 2001).
4. The top 3 pending tasks.
5. The Palace Rooms mapping for the current work.
6. All sovereign laws are loaded: GitOps, Ansible-only, Advisory Mode.

Then read these documents for full context (the AI will access them via the repo):
- docs/AI-MASTER-PROTOCOL.md       — Governance laws
- docs/AI-COGNITIVE-TWIN-PROTOCOL.md — Project Identity Card
- .agents/brain/palace_registry.md  — Palace Spatial Map
- .agents/brain/task.md             — Current tasks
- .agents/brain/walkthrough.md      — Session history
- docs/HUMAN-HANDOVER-CONTEXT.md   — Environment map

State: "Cognitive Twin Transfer Complete — [PROJECT NAME] Handover Successful."

```

--------------------------------

### System Integrity Check Handshake

Source: https://gitlab.com/linuxmalaysia/deep-state-of-mind-for-my-ai/-/blob/main/docs/PERSONALIZATION.md

Use this prompt in a fresh session to verify the AI's alignment with DSOM v6.1 and Palace v1.0 protocols.

```text
"Perform a System Integrity Check (DSOM Handshake v6.1 + Palace v1.0).

1. Identity & Mirror: Who am I, and what is your mandate if my 'Substance' is low?
2. Governance: List the 5 DSOM Laws.
3. ITIL 4 Role: Describe our service relationship and what you must ask for at the Start-of-Day.
4. Spatial Memory: Where do you look to map out the Palace Rooms for today's context?
5. Linguistic Standard: Which English dialect and Malay standard do we enforce?
6. Sourcing: Where is our Primary Repository located?


If correct, state: 'DSOM v6.1 Handshake Successful. Palace mapped. Mirror Active. Ready to co-create value.'"
```

--------------------------------

### Clone DSOM Tooling

Source: https://gitlab.com/linuxmalaysia/deep-state-of-mind-for-my-ai/-/blob/main/docs/HOWTO-ADOPT-DSOM.md

Methods for acquiring the DSOM core files via Git submodule or direct copy.

```bash
git submodule add https://github.com/linuxmalaysia/deep-state-of-mind-for-my-ai.git .dsom-core
cp -r .dsom-core/tools .
cp -r .dsom-core/docs .
```

```bash

# In a temporary folder

git clone https://github.com/linuxmalaysia/deep-state-of-mind-for-my-ai.git dsom-temp

# In your target project

cp -r ../dsom-temp/tools .
cp -r ../dsom-temp/docs .
```

--------------------------------

### Execute Direct Commit and Push

Source: https://gitlab.com/linuxmalaysia/deep-state-of-mind-for-my-ai/-/blob/main/docs/tools/HOWTO-GIT-RITUAL.md

Stages all changes, commits with the specified message, and pushes to the origin main branch.

```powershell
.\tools\git-ritual.ps1 push "feat(auth): add login flow [v1.0]"
```

```bash
bash tools/git-ritual.sh push "feat(auth): add login flow [v1.0]"
```

--------------------------------

### Run OKF Compliance and Link Validation

Source: https://gitlab.com/linuxmalaysia/deep-state-of-mind-for-my-ai/-/blob/main/docs/OKF-ADOPTION-GUIDE.md

Executes the full pytest suite and the documentation link validator script.

```bash
# Run full pytest suite including OKF compliance tests
uv run --with pytest --with pyyaml --with mcp==1.2.1 --with fastmcp --with pydantic-settings python -m pytest

# Validate relative documentation links across Diátaxis docs
uv run python tools/check_docs_links.py
```

--------------------------------

### Perform Defensive GitOps Sync

Source: https://gitlab.com/linuxmalaysia/deep-state-of-mind-for-my-ai/-/blob/main/docs-source/skills/dsom-policy-adopter.mdx

Synchronizes local changes with the remote repository using rebase and atomic commits.

```bash
git pull --rebase origin main
git add docs/governance/... .agents/AGENTS.md README.md CHANGELOG.md HISTORY.md SUMMARY.md mkdocs.yml
git commit -m "docs(governance): adopt [Policy Name]"
git push
```

--------------------------------

### DSOM Token Auditor Implementation

Source: https://gitlab.com/linuxmalaysia/deep-state-of-mind-for-my-ai/-/blob/main/docs/governance/DSOM-TOKEN-EFFICIENCY-REPORT.md

A Python script using tiktoken to compare token counts between bloated and optimized context scenarios. Requires the tiktoken library and is designed to run within a uv environment.

```python
import tiktoken

def count_tokens(text: str, model: str = "gpt-4") -> int:
    """Counts tokens using tiktoken (a proxy for modern LLMs)."""
    try:
        encoding = tiktoken.encoding_for_model(model)
        return len(encoding.encode(text))
    except Exception as e:
        print(f"Error counting tokens: {e}")
        return 0

def generate_bloated_context() -> str:
    """Simulates a non-DSOM 'Bloated' context load."""
    chat_history = "User: How do I clone the project?\nAI: You can clone the project by running git clone... " * 100
    file_content = """
    # START-HERE.md
    Welcome to the Deep State of Mind... 
    (Simulating a full 500-line markdown file load for every prompt)
    """ * 10
    conversational_fluff = "Sure! I would be absolutely delighted to assist you with that request today. Here is the information you asked for: "
    return chat_history + file_content + conversational_fluff

def generate_dsom_context() -> str:
    """Simulates an optimized DSOM context load using Episodic Resumes and Progressive Disclosure."""
    episodic_resume = """
    # [DSOM EPISODIC RECORD]
    - Primary Objective: Clone project.
    - Active Blocking Issues: None.
    - Last Completed: Cloned repo.
    """
    progressive_disclosure = """
    SOURCES:
    - [START-HERE.md](file:///START-HERE.md) - Project Entry Points
    - [AGENTS.md](file:///.agents/AGENTS.md) - Sovereign AI Rules
    """
    dense_language = "Repository cloned. Awaiting next command."
    return episodic_resume + progressive_disclosure + dense_language

def main():
    bloated_text = generate_bloated_context()
    dsom_text = generate_dsom_context()

    bloated_tokens = count_tokens(bloated_text)
    dsom_tokens = count_tokens(dsom_text)

    print("--- DSOM Token Efficiency Audit ---")
    print(f"Scenario A (Bloated Context): {bloated_tokens} tokens")
    print(f"Scenario B (DSOM Optimized Context): {dsom_tokens} tokens")
    
    if bloated_tokens > 0:
        savings = ((bloated_tokens - dsom_tokens) / bloated_tokens) * 100
        print(f"Token Reduction: {savings:.2f}%")
        print(f"Absolute Savings: {bloated_tokens - dsom_tokens} tokens per turn")
    else:
        print("Failed to calculate tokens.")

if __name__ == "__main__":
    main()
```

--------------------------------

### Restore Context with Hibernation Notes

Source: https://gitlab.com/linuxmalaysia/deep-state-of-mind-for-my-ai/-/blob/main/docs-source/ai-setup/claude.mdx

Feed previously saved hibernation notes into a new chat session to restore project context.

```text
I am starting a new session for DSOM Protocol. I am your human Lead Architect.
I have the Hibernation Notes from our last session. Please read them carefully
and use them to fully restore our working context.

--- BEGIN HIBERNATION NOTES ---
[PASTE HIBERNATION NOTES BLOCK HERE]
--- END HIBERNATION NOTES ---

After reading: state the last Mental Anchor, confirm the T1-T4 environment map,
list top 3 pending tasks. State: 'Sovereign State Restored — [PROJECT NAME] is live.'
Operate under DSOM v6.1: Advisory Mode, UK English, Git-first, Ansible-only execution.
```

--------------------------------

### Execute Privacy Scan

Source: https://gitlab.com/linuxmalaysia/deep-state-of-mind-for-my-ai/-/blob/main/llms-full.txt

Perform a security scan on the environment before sharing the manifest with the AI.

```bash
./tools/privacy-guardian.sh
# Or via Ansible:
ansible-playbook playbooks/dsom/privacy-scan.yml -i localhost,
```

--------------------------------

### Decision Log Protocol Template

Source: https://gitlab.com/linuxmalaysia/deep-state-of-mind-for-my-ai/-/blob/main/docs-source/palace/build-story.mdx

Use this template to document significant architecture choices, tool selections, or tradeoffs before ending an AI session.

```text
Log this decision to walkthrough.md:
Decision: [what was decided]
Alternatives rejected: [what else was considered and why it was not chosen]
Reason: [why this decision was made]
```

--------------------------------

### Define explanation page metadata

Source: https://gitlab.com/linuxmalaysia/deep-state-of-mind-for-my-ai/-/blob/main/llms-full.txt

Standard YAML front matter for documentation explanation pages.

```yaml
---
okf_version: 0.1
type: documentation
title: Explanation Index
timestamp: "2026-08-13T12:00:00Z"
topics: ["dsom", "documentation", "diataxis", "explanation"]
---
```

--------------------------------

### Run Token Audit

Source: https://gitlab.com/linuxmalaysia/deep-state-of-mind-for-my-ai/-/blob/main/docs/HOWTO-DSOM-BASELINE.md

Executes the token calculator script to verify that all skill files are within acceptable token limits.

```bash
uv run --with tiktoken .agents/skills/dsom-token-calculator/scripts/calculate-tokens.py .agents/skills/
```

--------------------------------

### Enforce OKF in CI/CD

Source: https://gitlab.com/linuxmalaysia/deep-state-of-mind-for-my-ai/-/blob/main/docs/OKF-ADOPTION-GUIDE.md

Add a step to GitHub Actions to verify frontmatter compliance.

```yaml
- name: Verify OKF Frontmatter Compliance
  run: |
    uv run python tools/apply_okf_frontmatter.py docs/
    git diff --exit-code -- docs/
```

--------------------------------

### Edit Project Identity Protocol

Source: https://gitlab.com/linuxmalaysia/deep-state-of-mind-for-my-ai/-/blob/main/docs/README.md

Opens the cognitive twin protocol file for manual configuration of project-specific placeholders.

```bash
# Open and edit with your project-specific details
nano AI-COGNITIVE-TWIN-PROTOCOL.md
```

--------------------------------

### Execute eod-palace for Windows and Linux

Source: https://gitlab.com/linuxmalaysia/deep-state-of-mind-for-my-ai/-/blob/main/docs/tools/HOWTO-EOD-PALACE.md

Run the appropriate script for your environment to initiate the automated EOD save process.

```powershell

# Windows (T1)

.\tools\eod-palace.ps1

```

```bash

# WSL2 / Linux (T2)

bash tools/eod-palace.sh

```

--------------------------------

### Visualize Agent Brain Tree

Source: https://gitlab.com/linuxmalaysia/deep-state-of-mind-for-my-ai/-/blob/main/docs/governance/HUB-AND-SPOKE-MODEL.md

Displays the expanded directory tree of all member brains.

```bash
tree -a .agents/brain
```

--------------------------------

### Commit Genesis State

Source: https://gitlab.com/linuxmalaysia/deep-state-of-mind-for-my-ai/-/blob/main/docs/HOWTO-CLONE-DSOM-PROJECT.md

Stage and commit the initial project state to Git.

```bash
git add .
git commit -m "chore(dsom): scaffold genesis dsom architecture and AI skills"
```

--------------------------------

### DSOM Episodic Record Markdown Template

Source: https://gitlab.com/linuxmalaysia/deep-state-of-mind-for-my-ai/-/blob/main/docs/DSOM-EPISODIC-RECORD-TEMPLATE.md

Use this template to log session details, cognitive state, and task queues. Paste the completed record into the Gemini Gem's first prompt to resume the session.

```markdown

# [DSOM EPISODIC RECORD]

## 1. IDENTITY & CONTEXT MATRIX

- **Project Name:** 
- **Timestamp:** 
- **Active Gem Configuration:** DSOM Brain v1
- **Current Mental State:** [e.g., Active Development / Debugging / Brainstorming]

## 2. THE DELTA (CURRENT COGNITIVE LOCK)

- **Primary Objective:** 
- **Last Completed Milestone:** 
- **Active Blocking Issues:** 

## 3. MEMORY CORE & PARAMETERS

- **Core Rules Asserted:** 
- **Critical File Dependencies:** [e.g., `.agents/AGENTS.md`, `schema.sql`]
- **Key Constraints:** 

## 4. NEXT ACTION QUEUE

- [ ] Task 1: 
- [ ] Task 2: 
- [ ] Task 3: 

---
*Instructions for Resume: Paste this entire record into your Gemini Gem first prompt to re-anchor the cognitive twin context.*
```

--------------------------------

### Execute Ansible Pre-flight Audit and Verification

Source: https://gitlab.com/linuxmalaysia/deep-state-of-mind-for-my-ai/-/blob/main/docs/governance/OPERATIONAL-GUIDE.md

Mandatory steps to audit the environment, verify connectivity, and perform a dry-run before executing any playbook.

```bash

# 1. Run the DSOM pre-flight audit

./tools/audit-pre-flight.sh

# 2. Verify Ansible connectivity to all target nodes

ansible all -m ping -i inventory/hosts.yml

# 3. Dry-run the playbook first

ansible-playbook playbooks/site.yml --check --diff

```

--------------------------------

### Multi-Remote GitOps Configuration

Source: https://gitlab.com/linuxmalaysia/deep-state-of-mind-for-my-ai/-/blob/main/docs-source/skills/initialize-gitops.mdx

Commands to configure a single 'all' remote that pushes to multiple endpoints simultaneously, followed by setting the upstream branch for pull operations.

```bash
git remote add all <Primary_URL>
```

```bash
git remote set-url --add --push all <Primary_URL>
git remote set-url --add --push all <Secondary_URL>
```

```bash
git branch --set-upstream-to=origin/main main
```

--------------------------------

### Integrate Validator into FastMCP Tool

Source: https://gitlab.com/linuxmalaysia/deep-state-of-mind-for-my-ai/-/blob/main/docs/governance/DSOM-CUSTOM-VALIDATORS-GUIDE.md

Demonstrates how to instantiate a validator and apply it within a FastMCP tool function to block invalid input before execution.

```python
# tools/mcp/server.py snippet
from fastmcp import FastMCP
from tools.validators.credential_guardian import CredentialGuardianValidator

mcp = FastMCP("DSOM Sovereign MCP")
guardian = CredentialGuardianValidator()

@mcp.tool()
def safe_write_knowledge(path: str, content: str) -> str:
    """Writes knowledge to palace with inline guardrail validation."""
    
    # 1. Run Input Guardrail
    res = guardian.validate(content)
    if not res.is_valid:
        raise ValueError(f"[GUARDRAIL BLOCKED] {res.error_message}")
        
    # 2. Proceed with idempotent write
    # ... write logic ...
    return f"Successfully validated and written to {path}"
```

--------------------------------

### Execute Privacy Scan

Source: https://gitlab.com/linuxmalaysia/deep-state-of-mind-for-my-ai/-/blob/main/docs-source/tools/privacy-guardian.mdx

Run the privacy scan script on Linux/WSL2 or Windows environments.

```bash
bash tools/privacy-guardian.sh
```

```powershell
.\tools\privacy-guardian.ps1
```

--------------------------------

### Operational Architecture Diagram

Source: https://gitlab.com/linuxmalaysia/deep-state-of-mind-for-my-ai/-/blob/main/docs-source/skills/mintlify-docs-compiler.mdx

Visual representation of the documentation compilation and synchronization workflow.

```text
┌────────────────────────────────────────────────────────────┐
│ Sovereign Source of Truth: docs/ & .agents/skills/         │
└───────────────────────────┬────────────────────────────────┘
                            │ (python tools/build_mintlify_mdx.py)
                            ▼
┌────────────────────────────────────────────────────────────┐
│ Compiled MDX Tree: docs-source/ + docs.json                │
└───────────────────────────┬────────────────────────────────┘
                            │ (python scripts/sync_docs.py)
                            ▼ (5 Non-Negotiable Safety Guards A–E)
┌────────────────────────────────────────────────────────────┐
│ Downstream Deployment: linuxmalaysia/my-knowledge-brain    │
└───────────────────────────┬────────────────────────────────┘
                            │ (Mintlify Rebuild)
                            ▼
┌────────────────────────────────────────────────────────────┐
│ Live Documentation: https://harisfazillah.mintlify.site   │
└────────────────────────────────────────────────────────────┘
```

--------------------------------

### Run Ansible Pre-flight Audit

Source: https://gitlab.com/linuxmalaysia/deep-state-of-mind-for-my-ai/-/blob/main/docs-source/governance/gitops-aiops-ansible-strategy.mdx

Execute the pre-flight audit script to ensure the environment is ready for automation.

```bash
tools/audit-pre-flight.sh
```

--------------------------------

### Add Remote Repository

Source: https://gitlab.com/linuxmalaysia/deep-state-of-mind-for-my-ai/-/blob/main/docs-source/tools/template-reset.mdx

Command to link the newly initialized repository to a remote origin after a Git history purge.

```bash
git remote add origin https://github.com/your-org/your-new-project.git
```

--------------------------------

### Referencing Playbooks for Ansible Tasks

Source: https://gitlab.com/linuxmalaysia/deep-state-of-mind-for-my-ai/-/blob/main/docs/COPILOT-SETUP.md

Attach specific playbooks and protocol documentation to generate idempotent roles.

```text
Based on #file:playbooks/dsom/site.yml and #file:docs/AI-COGNITIVE-TWIN-PROTOCOL.md,
propose an idempotent role for the T4 Production baseline.
```

--------------------------------

### Run Pre-Flight Audit

Source: https://gitlab.com/linuxmalaysia/deep-state-of-mind-for-my-ai/-/blob/main/CONTRIBUTING.md

Execute the audit script to ensure all checks pass.

```bash
./tools/audit-pre-flight.sh
# All steps must show [PASS]
```

--------------------------------

### Synchronize T2 Environment

Source: https://gitlab.com/linuxmalaysia/deep-state-of-mind-for-my-ai/-/blob/main/CONTRIBUTING.md

Execute these commands inside the WSL2 instance after pushing changes from the Windows environment to ensure parity.

```bash
# Inside dsom-control-almalinux10
cd /mnt/d/Users/[YOU]/Projects/[PROJECT]
git pull origin main
```

--------------------------------

### Verify Conversation Path

Source: https://gitlab.com/linuxmalaysia/deep-state-of-mind-for-my-ai/-/blob/main/docs/tools/HOWTO-CHECKUSAGE.md

Path to verify for existing conversation context files.

```text
C:\Users\[you]\.gemini\antigravity\conversations\
```

--------------------------------

### SOD Reanimation Prompt

Source: https://gitlab.com/linuxmalaysia/deep-state-of-mind-for-my-ai/-/blob/main/docs/SOD-RITUAL.md

Use this prompt to restore the AI's working context by pasting the previous session's hibernation notes into the designated placeholder.

```text

I am starting a new session for DSOM Protocol. I am your human Lead Architect.
I have the Hibernation Notes from our last session. Please read them carefully
and use them to fully restore our working context.

--- BEGIN HIBERNATION NOTES ---
[PASTE HIBERNATION NOTES BLOCK HERE]
--- END HIBERNATION NOTES ---

After reading the Hibernation Notes above, do the following:

1. Confirm you have read and understood all entries in the Hibernation Notes.
2. State the last Mental Anchor — the exact point where we stopped.
3. Confirm the 4-Tier environment map (T1, T2, T3, T4) and the project identity.
4. List the top 3 tasks from .agents/brain/task.md that are pending.
5. State: "Sovereign State Restored — [PROJECT NAME] is live." to confirm you are ready.

From this point, operate under DSOM Protocol v6.1:
- Advisory Mode only. You propose, I approve, Ansible executes, you verify.
- UK English in all responses and documentation.
- Every change goes to Git before execution.
- Ansible is the only executor for OS-level tasks.
- Wait for my output after each step before proceeding.

```

--------------------------------

### Synchronize git history with Palace Rooms

Source: https://gitlab.com/linuxmalaysia/deep-state-of-mind-for-my-ai/-/blob/main/docs-source/palace/overview.mdx

Use these commands to map git commits to the Palace structure. The backfill flag processes the entire repository history.

```bash
# Map current git commits into Palace Rooms
bash tools/palace-sync.sh

# Backfill the entire git history into Palace Rooms
bash tools/palace-sync.sh --backfill
```

--------------------------------

### Apply OKF Frontmatter

Source: https://gitlab.com/linuxmalaysia/deep-state-of-mind-for-my-ai/-/blob/main/docs-source/skills/dsom-bootstrap.mdx

Ensures all imported or ported Markdown documents contain valid OKF v0.1 frontmatter.

```bash
uv run python tools/apply_okf_frontmatter.py .
```

--------------------------------

### Synchronize Team Updates

Source: https://gitlab.com/linuxmalaysia/deep-state-of-mind-for-my-ai/-/blob/main/docs/governance/HUB-AND-SPOKE-MODEL.md

Updates the local repository and lists member brain directories to prepare for work.

```bash
git pull origin main
ls .agents/brain/member/
```

--------------------------------

### Commit the Palace Migration

Source: https://gitlab.com/linuxmalaysia/deep-state-of-mind-for-my-ai/-/blob/main/docs-source/palace/migration.mdx

Stages and commits the initialized Palace directory to the repository.

```bash
git add .agents/brain/
git commit -m "feat(palace): initialise Sovereign Markdown Palace v1.0 — [N] Rooms backfilled"
git push origin main
```

--------------------------------

### No .pb Files Found Error Message

Source: https://gitlab.com/linuxmalaysia/deep-state-of-mind-for-my-ai/-/blob/main/docs/tools/HOWTO-CHECKUSAGE.md

Error output displayed when the script fails to locate conversation files.

```text
No .pb conversation files found.
```

--------------------------------

### Hibernation Notes Restoration Prompt

Source: https://gitlab.com/linuxmalaysia/deep-state-of-mind-for-my-ai/-/blob/main/docs/CLAUDE-SETUP.md

Use this prompt template to restore working context from previous session notes.

```text
I am starting a new session for DSOM Protocol. I am your human Lead Architect.
I have the Hibernation Notes from our last session. Please read them carefully
and use them to fully restore our working context.

--- BEGIN HIBERNATION NOTES ---
[PASTE HIBERNATION NOTES BLOCK HERE]
--- END HIBERNATION NOTES ---

After reading: state the last Mental Anchor, confirm the T1-T4 environment map,
list top 3 pending tasks. State: 'Sovereign State Restored — [PROJECT NAME] is live.'
Operate under DSOM v6.1: Advisory Mode, UK English, Git-first, Ansible-only execution.
```

--------------------------------

### Hibernation Notes Export Prompt

Source: https://gitlab.com/linuxmalaysia/deep-state-of-mind-for-my-ai/-/blob/main/docs/EOD-RITUAL.md

A verbatim prompt to be used in an AI session to extract all stored project context and memory into a single code block for local backup.

```text
I'm as human, want to know and remember, and need to export my data and I want you
to generate a "Hibernation notes" now for my EOD of day. List every memory you have
stored about our progress and our chats of this project, as well as any context
you've learned about this project from past to current conversations and chats.
Output everything in a single code block so I can easily copy it. Format each entry
as: [date saved, if available] - memory content. Make sure to cover all of the
following — preserve my words verbatim where possible:

- Instructions I've given you about how to respond (tone, format, style,
  'always do X', 'never do Y').
- Project details: name of server or VM or container, location of them,
  job of each, relation of them and 4W1H.
- Tasks, phases, goals, and recurring topics.
- Tools, languages, and frameworks I use.
- Preferences and corrections I've made to your behaviour.
- Any other stored context not covered above.

Do not summarize, group, or omit any entries. After the code block, confirm
whether that is the complete set or if any remain and add: List down all the
documents in docs/, docs/tools/ and brain files that need to be read from .agents/.
Don't hide anything from me. Trust me as your master.
```

--------------------------------

### Automated Ansible Palace SOD

Source: https://gitlab.com/linuxmalaysia/deep-state-of-mind-for-my-ai/-/blob/main/docs/SOD-RITUAL.md

Run a full automated synchronization, audit, and reanimation sequence on Linux or WSL2.

```bash
# T2 (Linux/WSL2) — Full automated SOD

bash tools/sod-palace.sh  # (Windows: .\tools\sod-palace.ps1)
```

--------------------------------

### Export Knowledge Graph to HTML

Source: https://gitlab.com/linuxmalaysia/deep-state-of-mind-for-my-ai/-/blob/main/docs-source/tools/openwiki-usage.mdx

Generates a standalone HTML file for offline visualization of the knowledge graph. Requires the pyyaml dependency.

```bash
uv run --with pyyaml python tools/openwiki_emulator.py --export-graph

# Then open the graph in your browser
xdg-open openwiki/graph.html
```

```powershell
uv run --with pyyaml python tools/openwiki_emulator.py --export-graph

# Then open the graph in your browser
Start-Process openwiki\graph.html
```

--------------------------------

### Run Pre-flight Audit

Source: https://gitlab.com/linuxmalaysia/deep-state-of-mind-for-my-ai/-/blob/main/docs-source/palace/onboarding.mdx

Executes the audit script to verify the Palace configuration and directory structure.

```bash
bash tools/audit-pre-flight.sh
```

--------------------------------

### MCP Tools

Source: https://gitlab.com/linuxmalaysia/deep-state-of-mind-for-my-ai/-/blob/main/llms-full.txt

Dynamic tools available for execution by AI clients.

```APIDOC
## MCP Tools

### Description
AI clients can execute these tools dynamically to perform searches or fetch context.

### Tools
- **search_palace(query)** - Text search across `docs/` Markdown files.
- **search_openwiki(query)** - Sub-millisecond OKF metadata search.
- **fetch_context7_stream(tokens)** - Retrieves URL pointers to external pre-indexed token streams.
```